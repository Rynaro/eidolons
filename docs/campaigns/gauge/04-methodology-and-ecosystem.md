# Stage 4 — Versioned specialist methods and accessible operation

Revise methodologies explicitly and preserve their authority. Keep reasoning heuristics replaceable, memory optional, and execution/evidence reporting truthful.

Read [HANDOFF.md](HANDOFF.md) and [ARCHITECTURE.md](ARCHITECTURE.md). [plan.yaml](plan.yaml) owns package identities, dependencies and source routing. Each table below owns its EARS requirements; verification entries are planned cases, not executed results. Assign one package, and one named slice where applicable.

<a id="v4-16"></a>

## V4-16 — Publish lean RAMZA methods without fabricated certainty

**Default source owner:** `Rynaro/Ramza`. **Prerequisites:** `V4-15`. Stage gates also apply.

**Starting points:** `SPEC.md`, `PERSONA.md`, `tiers.md`, `skills/`, `templates/`. Resolve symbolic/new paths in the actual checkout; they are not claims those interfaces already exist.

**Scope and decisions.** Explicit versioned amendment of RAMZA-lite. Keep full-tier protections while removing mandatory invented alternatives/hierarchies on clear reversible work. A method may run inside an authorized maker; that use is not a separate planning invocation or independent critique. Reuse an imported profile location only when V4-10 records it; otherwise use the current source repository.

**Implementation sequence.** Compare existing requirements with a lean method and record the amendment. Provide minimal outcome/scope/criteria/verifier/decision/risks. Test producer-consumer use and preserve standalone/full mode.

| ID | EARS requirement | Planned verification |
|---|---|---|
| V4-16-R01 | WHEN a clear conventional lite task is planned, the RAMZA method SHALL produce a minimal actionable contract without mandatory invented alternatives. | **V4-16-T01:** Fixtures assert outcome, exclusions, criteria, verifier, selected approach and material risks; no fixed three-to-five option requirement. |
| V4-16-R02 | IF unresolved information changes required behavior or authority, THEN the RAMZA method SHALL expose that decision before authorizing implementation. | **V4-16-T02:** One-file security change, migration and ambiguous product choice; few files alone do not imply low risk. |
| V4-16-R03 | WHEN the maker consumes an accepted planning decision, the method contract SHALL avoid requiring a duplicate search for already settled alternatives. | **V4-16-T03:** Producer/consumer fixture carries decision and invalidation conditions; changed evidence reopens the relevant choice. |
| V4-16-R04 | WHEN rubric scores are presented, the RAMZA output SHALL label uncalibrated scores as heuristics. | **V4-16-T04:** Scored fixture does not describe 85 as an 85-percent probability or as independent verification. |
| V4-16-R05 | WHERE legacy or full planning mode is selected, the profile package SHALL preserve its declared controls. | **V4-16-T05:** Standalone and prior-version compatibility fixtures; changed charter requires a versioned declaration. |

**Exit.** Map every requirement above to observed evidence or a named blocker. Read HANDOFF.md for mechanical, independent, CI and live-evidence distinctions. Unavailable mandatory evidence prevents acceptance; a fixture-only candidate can be ready for review without claiming live qualification.

**Stop and rollback.** No default flip or probability claim from rubric arithmetic. Unclear authority and consequential behavior remain explicit questions.

---

<a id="v4-17"></a>

## V4-17 — Publish explicit Vivi candidate and context modes

**Default source owner:** `Rynaro/Vivi`. **Prerequisites:** `V4-15`. Stage gates also apply.

**Starting points:** `SPEC.md`, `PERSONA.md`, `skills/loop-native/SKILL.md`, `skills/failure-recovery/SKILL.md`. Resolve symbolic/new paths in the actual checkout; they are not claims those interfaces already exist.

**Scope and decisions.** Explicitly revise fresh-context and human-apply assumptions only in named modes. Support proposal-only and authorized continuous candidate-workspace operation. Preserve greenfield and publication boundaries unless separately revised. Separate continuing maker context from fresh checker context; do not assume either retry strategy wins.

**Implementation sequence.** Create opt-in mode declarations and corresponding native adapter fixtures. Consume inherited decisions, failures and root budget. Keep fresh-retry control for later matched evaluation.

| ID | EARS requirement | Planned verification |
|---|---|---|
| V4-17-R01 | WHEN candidate-workspace mode is selected with scoped authorization, Vivi SHALL edit only the authorized candidate workspace. | **V4-17-T01:** Proposal-only negative control; real-tree escape, protected criteria, unrelated dirty work and allowed candidate edits. |
| V4-17-R02 | WHERE proposal-only mode is selected, Vivi SHALL return a candidate proposal without applying it to the user tree. | **V4-17-T02:** Existing standalone compatibility and parent-authorized application remain distinct operations. |
| V4-17-R03 | WHEN a coherent task uses continuity mode, Vivi SHALL retain relevant decisions and failure history across repair attempts. | **V4-17-T03:** Matched warm/fresh fixtures; a context reset is recorded and does not reset task accounting. |
| V4-17-R04 | IF independent verification is required, THEN Vivi SHALL submit the candidate to the controller-managed verification boundary. | **V4-17-T04:** Maker rename or conversation fork fails to satisfy independence; separate recorded checker succeeds only with required evidence. |
| V4-17-R05 | IF a task exceeds Vivi's declared authority or greenfield scope, THEN Vivi SHALL return the applicable boundary rather than using method composition to evade it. | **V4-17-T05:** Novel architecture, deploy/push, external spend and scope-expansion controls. |

**Exit.** Map every requirement above to observed evidence or a named blocker. Read HANDOFF.md for mechanical, independent, CI and live-evidence distinctions. Unavailable mandatory evidence prevents acceptance; a fixture-only candidate can be ready for review without claiming live qualification.

**Stop and rollback.** No publication authority from a Gauge preset. Roll back a candidate mode without losing its diff or unsettled consumption.

---

<a id="v4-18"></a>

## V4-18 — Adopt need-based specialist execution across the roster

**Default source owner:** `Rynaro/eidolons`. **Prerequisites:** `V4-13`, `V4-16`, `V4-17`. Stage gates also apply.

**Starting points:** `EIDOLONS.md`, `roster/routing.yaml`, `methodology/cortex/`, `roster/index.yaml`. Resolve symbolic/new paths in the actual checkout; they are not claims those interfaces already exist.

**Scope and decisions.** Adopt the new profile contracts through explicit per-repository or imported-component slices. ATLAS, FORGE, VIGIL, IDG, Kupo, Gilgamesh, SPECTRA and APIVR-Delta retain their purpose and authority ceilings. Classify enforceable rules, revisable heuristics and rationale. Preserve requested specialist invocations and independent-review semantics rather than silently substituting an embedded method.

**Implementation sequence.** Assign one profile slice at a time. Update its method/worker declaration and compatibility fixtures. Integrate only approved published or tested monorepo package identities into the new router.

**Assignment slices:** `ATLAS`, `FORGE`, `VIGIL`, `IDG`, `Kupo`, `Gilgamesh`, `SPECTRA`, `APIVR-Delta`, `nexus-adoption`. Select one; final package acceptance covers the required selected core slices. Explicitly deferred optional slices do not become required later without amendment.

| ID | EARS requirement | Planned verification |
|---|---|---|
| V4-18-R01 | WHEN a task requires expertise but no separate execution boundary, routing SHALL permit an embedded method instead of a mandatory specialist worker. | **V4-18-T01:** Clear fix, targeted discovery and lite planning; compare worker count and preserved semantic deliverables. |
| V4-18-R02 | WHEN the user explicitly requests a separate specialist or independent review, routing SHALL preserve that execution requirement. | **V4-18-T02:** Named legacy agent, separate ATLAS inspection, independent critique, documentation-required and read-only intent fixtures. |
| V4-18-R03 | IF a specialist contribution requires a distinct context, permission boundary, or independent track, THEN routing SHALL record that reason on its assignment. | **V4-18-T03:** Bounded FORGE/VIGIL consultation, isolated parallel writer and checker; no unexplained fan-out. |
| V4-18-R04 | WHEN a methodology control is revised, the profile registry SHALL retain its rule, heuristic, or rationale classification and replacement reference. | **V4-18-T04:** Control inventory diff; protected tests stay enforceable, option counts become explicit heuristics, rationale is not falsely certified. |
| V4-18-R05 | WHEN a profile version is adopted, compatibility validation SHALL check the exact producer and consumer versions. | **V4-18-T05:** Package/contract matrix and generated-discovery parity; do not invent tags, change existing performatives, or force external agents into the monorepo. |

**Exit.** Map every requirement above to observed evidence or a named blocker. Read HANDOFF.md for mechanical, independent, CI and live-evidence distinctions. Unavailable mandatory evidence prevents acceptance; a fixture-only candidate can be ready for review without claiming live qualification.

**Stop and rollback.** Keep legacy selection and deterministic opt-out routing. Optional prose cleanup cannot erase a control; no automatic archive or global rename.

---

<a id="v4-19"></a>

## V4-19 — Context, memory, and tools with measurable reuse

**Default source owner:** `Rynaro/eidolons`. **Prerequisites:** `V4-15`. Stage gates also apply.

**Starting points:** `cli/src/harness_hook.sh`, `cli/src/lib_context.sh`, `roster/context-policy.yaml`, `roster/pins.yaml`. Resolve symbolic/new paths in the actual checkout; they are not claims those interfaces already exist.

**Scope and decisions.** Measure actual host payload and latency sources before optimizing. Reuse revision-valid task evidence, progressively discover tools, batch deterministic reads, preserve full redacted logs outside active context, and retain required pins. CRYSTALIUM is optional knowledge, not execution truth; Atomos stays compose/verify-only. Changes to atlas-aci or Magicite need separate justified slices.

**Implementation sequence.** Capture actual supported input/tool-schema and startup/retrieval costs. Add bounded output and conservative invalidation with exact references. Test missing services, repeated triggers and cold/warm resumption without claiming to erase seen context.

**Assignment slices:** `core-context`, `one-justified-optional-adapter`. Select one; final package acceptance covers the required selected core slices. Explicitly deferred optional slices do not become required later without amendment.

| ID | EARS requirement | Planned verification |
|---|---|---|
| V4-19-R01 | WHEN evidence is reused, the context manager SHALL validate its declared source, criteria, and environment dependencies. | **V4-19-T01:** Relevant mutations and missing artifacts invalidate; unrelated control may reuse only with a proven dependency relation. |
| V4-19-R02 | WHEN a tool result exceeds the presentation bound, the context manager SHALL return a localized excerpt with a usable reference to full permitted evidence. | **V4-19-T02:** Critical assertion outside a raw tail, redaction, deleted log and bounded result fixtures. |
| V4-19-R03 | WHEN context succession occurs, the controller SHALL retain mandatory pins and outstanding task obligations. | **V4-19-T03:** Cold/warm recovery, failed approaches, remaining budget, pending checks and retained authority fixtures. |
| V4-19-R04 | IF optional memory is unavailable or untrusted, THEN the delivery path SHALL continue without treating memory as authoritative execution evidence. | **V4-19-T04:** No-MCP task, foreign project/poisoned note, stale test claim and failed recall; no receipt promotion. |
| V4-19-R05 | WHEN programmatic or batched tool execution is used, the controller SHALL enforce the same operation permissions as individual calls. | **V4-19-T05:** Generic execute wrapper attempts denied tool/network/filesystem access; valid batches remain bounded. |
| V4-19-R06 | WHILE repeated context triggers describe unchanged state, the context manager SHALL avoid duplicate lifecycle work. | **V4-19-T06:** Rapid prompt hooks and threshold oscillation; configured debounce/hysteresis tested without assuming universal thresholds. |

**Exit.** Map every requirement above to observed evidence or a named blocker. Read HANDOFF.md for mechanical, independent, CI and live-evidence distinctions. Unavailable mandatory evidence prevents acceptance; a fixture-only candidate can be ready for review without claiming live qualification.

**Stop and rollback.** No unconditional recall or new mandatory memory/container service. Selective tool execution passes through the same authority checks as direct calls.

---

<a id="v4-20"></a>

## V4-20 — Truthful CLI status and optional user interfaces

**Default source owner:** `Rynaro/eidolons`. **Prerequisites:** `V4-18`, `V4-19`. Stage gates also apply.

**Starting points:** `cli/eidolons`, `docs/cli-reference.md`, `GAMBIT: native session/status adapter (locate)`. Resolve symbolic/new paths in the actual checkout; they are not claims those interfaces already exist.

**Scope and decisions.** Expose the same versioned state through CLI/JSON and optional GAMBIT consumers. Show expertise used versus workers started, billing mode, policy origin, capability granularity, unknown quota, next action, and actual blocker. Keep Final Fantasy names with plain-language functions. External/private clients are separate assignments; publish no private details.

**Implementation sequence.** Generate human status from controller events and evidence. Add no-color keyboard/headless walkthroughs. Connect optional clients to the shared interface without duplicating budget logic.

**Assignment slices:** `core-cli`, `optional-GAMBIT`. Select one; final package acceptance covers the required selected core slices. Explicitly deferred optional slices do not become required later without amendment.

| ID | EARS requirement | Planned verification |
|---|---|---|
| V4-20-R01 | WHEN task status is requested, the interface SHALL report observed delivery and verification states separately. | **V4-20-T01:** Implemented/runnable/checks-passed/review-pending/released, cancelled/partial/blocked and stale evidence snapshots. |
| V4-20-R02 | WHEN execution participation is displayed, the interface SHALL distinguish methods used, worker identities, and context-separation evidence. | **V4-20-T02:** One maker with multiple skills versus separate specialist/checker invocations; no fictional team claim. |
| V4-20-R03 | IF provider quota or enforcement is unsupported or stale, THEN the interface SHALL display that limitation in plain text. | **V4-20-T03:** No fabricated percentage, opaque bucket, advisory-only mode and native execution outside controller controls. |
| V4-20-R04 | WHEN a user inspects policy or previews a change, the interface SHALL avoid dispatching model work. | **V4-20-T04:** Mock call counters and denied authority escalation; set/preview preserves existing reservations. |
| V4-20-R05 | WHERE an optional client is installed, the client SHALL consume the same policy/status contract as the CLI. | **V4-20-T05:** Consumer version matrix and no-GUI/no-color core walkthrough; unsupported interface reports a clear reason. |

**Exit.** Map every requirement above to observed evidence or a named blocker. Read HANDOFF.md for mechanical, independent, CI and live-evidence distinctions. Unavailable mandatory evidence prevents acceptance; a fixture-only candidate can be ready for review without claiming live qualification.

**Stop and rollback.** GUI deferral does not block the usable CLI. Preview/status has no model or spending side effects.

---

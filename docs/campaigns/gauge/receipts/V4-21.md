# V4-21 — Expand the early instrument into auditable evaluation

- **Authority:** [canonical plan](https://github.com/Rynaro/eidolons/blob/c581308f055a0e252013bf09f2a7b2e1426ff811/docs/campaigns/gauge/05-evaluation-and-rollout.md#v4-21), [spec](../../../../.spectra/changes/gauge-v4-21/spec.md), [decision](../../../../.spectra/changes/gauge-v4-21/decision.md).
- **Base:** `7528827ab6482146375f9062925efbe97495d751` (main after V4-15 #612); plan `c581308f055a0e252013bf09f2a7b2e1426ff811`.
- **Status:** ready-for-review for local fixture scope. Independent review and hosted CI are pending. Live qualification intentionally blocked. No paid trials without allowance. No V4-20 / V4-22 / V4-10.
- **Publication:** blocked pending explicit publication approval / hosted CI on the review PR.

## Delivered contract

[DECISION] Keep schema 2. Add typed evaluation namespaces (`evaluation_trials`, `evaluation_arm_identities`, `evaluation_holdouts`, `evaluation_plumbing`, `evaluation_admissions`, `evaluation_drifts`, `evaluation_outcomes`, `evaluation_reports`, `evaluation_promotions`, `evaluation_attempts`, `evaluation_events`) with `meta.evaluation_receipt` and typed version guards. Extends V4-09 instrument — does not replace it. Comparison roles: native / original-v3 (`752194ef5ceaa8cee1f5995fd0d374888696d8fc`) / v4 fixed-model / structural; structural without V4-10 stays **pending** (not fabricated zeros). Development / calibration / holdout / forward splits stay separate. Cost-per-accepted reuses V4-09 K/Z/U/W patterns; unknown exposure blocks complete-cost claims; undefined ratio is never zero. Holdout isolation + leakage canaries; gold-patch/simulated-host classified as plumbing/smoke; live credentials presence ≠ usability. Reports carry completion, runnable/total latency, interventions, invalid acceptance, quality-review with uncertainty, independent recomputation, censored/small samples, delayed-rework window. Admissions record requirement sufficiency / oracle / exclusion with post-outcome audit+sensitivity. Material env/permission drift → confounded, not hidden in averages. Outcome distinctions separate candidate/selection/autonomous/reliability; cancelled-pass ≠ identical operational outcome. Holdout-influenced material leaves untouched promotion set; forward inaccessible until candidate frozen.

## Requirement anchors

Harness: [evaluation-anchors.sh](../../../../gauge/tests/evaluation-anchors.sh). Runs discovery of `TestV421T01`–`TestV421T09`, then `go test -mod=readonly -race -count=1 ./... -run '^TestV421' -v`.

| Requirement | Named test and observed scope |
|---|---|
| R01 | `TestV421T01`: requested-vs-observed model, pin mismatches, confounds; structural pending not zero; native/v3/v4-fixed-model recorded. |
| R02 | `TestV421T02`: failures/abandon/cancel/timeout/checking/zero acceptance; undefined≠zero; unknown exposure blocks complete-cost; pending arm rejects fabricated metrics. |
| R03 | `TestV421T03`: holdout isolation, leakage canaries, tuning failures labeled development. |
| R04 | `TestV421T04`: gold patch / simulated host → plumbing; smoke vs live discriminator; unusable credentials keep live blocked. |
| R05 | `TestV421T05`: metrics with uncertainty; independent recomputation digest; censored/small sample; delayed-rework window. |
| R06 | `TestV421T06`: underspecified/over-restrictive/inadequate vs valid; post-outcome exclusion audit trail + sensitivity. |
| R07 | `TestV421T07`: resource/ceiling/network/cache/timeout/sandbox/permission diffs → confounded; not hidden in averages. |
| R08 | `TestV421T08`: candidate/selection/autonomous/reliability; cancelled pass and search-among-failures not identical outcomes. |
| R09 | `TestV421T09`: holdout-influenced material removed from untouched; forward inaccessible until freeze. |

## Local validation

```text
cd gauge && GAUGE_REPO=$PWD/.. go test -mod=readonly -race -count=1 ./... -run 'TestV421|TestV415|TestV414|TestV413|TestV412|TestV411|TestV409'
bash gauge/tests/evaluation-anchors.sh
```

Go 1.27.1 (`GOTOOLCHAIN=local`), `-mod=readonly -race`. Hosted Linux/macOS CI blocked pending publication. No paid/live probes. No V4-20 / V4-22 / V4-10.

## Compatibility / rollback

Opt-in Go Gauge seam only; Bash 3.2 legacy CLI unchanged. Schema 2 preserved. Disable by not calling evaluation commands; existing roots, policy, observation, instrument, reservation, dispatch, compiler, acceptance and delivery state remain. Extends V4-09; does not rewrite delivery.go or status.go. No V4-22 calibration / V4-10 structural consolidation in this package.

## Acceptance blockers

- Hosted CI (Linux/macOS `gauge.yml`) pending publication of this branch.
- Independent checker review pending.
- Live-host qualification intentionally blocked (V4-09 fail-closed).
- Structural arm remains pending until V4-10.
- No V4-22 strategy calibration or paid comparative trials.
- V4-20 core-cli status surface is a parallel package and is out of scope here.

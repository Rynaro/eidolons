# V4-17 — Explicit Vivi candidate and context modes

- **Authority:** [canonical plan](https://github.com/Rynaro/eidolons/blob/c581308f055a0e252013bf09f2a7b2e1426ff811/docs/campaigns/gauge/04-methodology-and-ecosystem.md#v4-17), [spec](../../../../.spectra/changes/gauge-v4-17/spec.md), [decision](../../../../.spectra/changes/gauge-v4-17/decision.md).
- **Base:** `eddd02118ad85810776ea155da4a82a1993d4b09` (main after V4-20 #614); plan `c581308f055a0e252013bf09f2a7b2e1426ff811`.
- **Status:** ready-for-review for local fixture scope. Independent review and hosted CI are pending. No live/provider dispatch claimed. No V4-16/18/10/19/22.
- **Publication:** blocked pending explicit publication approval / hosted CI on the review PR. Gauge does not grant publication authority.

## Delivered contract

[DECISION] Keep schema 2. Add typed Vivi namespaces (`vivi_sessions`, `vivi_modes`, `vivi_edits`, `vivi_proposals`, `vivi_applications`, `vivi_continuity`, `vivi_verifications`, `vivi_boundaries`, `vivi_context_strategies`, `vivi_events`) with `meta.vivi_receipt` and typed version guards (`vivi-modes@1`). Do not rewrite sibling Vivi methodology in-place; Gauge remains the versioned mode-contract seam until accepted V4-10 relocation.

Named opt-in modes only: `proposal_only` (emit candidate proposal; user-tree apply is a separate parent-authorized operation) and `candidate_workspace` (edits only inside scoped authorized workspace). Continuity retains decisions/failure history across repairs; context resets are recorded without resetting root accounting. Verification is controller-managed — maker rename/fork fails; separated checker satisfies only the observed evidence grade. Authority/greenfield/push-deploy/external-spend/expanded-scope/publication boundaries refuse rather than evade via method composition. Context strategies (`continue`, `native_compaction`, `fresh_worker`) record strategy version and transition boundary; unsupported host compaction is unknown, not claimed completed.

Zero real network/model. Live qualification remains fail-closed. Bash 3.2 legacy CLI unchanged; vivi commands are opt-in Go only. No V4-16/18/10/19/22.

## Requirement anchors

Harness: [vivi-anchors.sh](../../../../gauge/tests/vivi-anchors.sh). Runs discovery of `TestV417T01`–`TestV417T06`, then `go test -mod=readonly -race -count=1 ./... -run '^TestV417' -v`.

| Requirement | Named test and observed scope |
|---|---|
| R01 | `TestV417T01`: proposal-only control; user-tree escape; protected criteria; unrelated dirty; valid candidate edits. |
| R02 | `TestV417T02`: proposal without user-tree apply; unauthorized apply rejected; parent-authorized apply separate. |
| R03 | `TestV417T03`: warm/fresh continuity; reset recorded; root accounting preserved; history retained. |
| R04 | `TestV417T04`: maker rename/fork rejected; separated checker observed grade only; overclaim clamped. |
| R05 | `TestV417T05`: novel architecture / push-deploy / external spend / expanded scope / greenfield / publication refuse; composition cannot evade. |
| R06 | `TestV417T06`: continue / native compaction / fresh-worker recorded; unsupported compaction unknown not completed. |

## Local validation

```text
cd gauge && GAUGE_REPO=$PWD/.. GOTOOLCHAIN=local go test -mod=readonly -race -count=1 ./... -run '^TestV417'
bash gauge/tests/vivi-anchors.sh
```

Go 1.27.1 (`GOTOOLCHAIN=local`), `-mod=readonly -race`. Hosted Linux/macOS CI blocked pending publication. No paid/live probes. No V4-16/18/10/19/22.

## Compatibility / rollback

Opt-in Go Gauge seam only; Bash 3.2 legacy CLI unchanged. Schema 2 preserved. Disable by not calling vivi commands; existing delivery/policy/reservation/dispatch/evaluation/status state remains. Pre-V4-17 schema-2 stores remain openable; vivi APIs call `EnsureViviNamespaces`. Mode rollback preserves diff, failed approaches, and unresolved consumption via continuity records. Sibling Vivi unchanged. No publication authority granted.

## Acceptance blockers

- Hosted CI (Linux/macOS `gauge.yml`) pending publication of this branch.
- Independent checker review pending.
- Live/provider dispatch intentionally out of scope (V4-09 fail-closed).
- Sibling methodology relocation deferred to V4-10.
- No V4-16/18/19/22 methodology/roster/context claims.

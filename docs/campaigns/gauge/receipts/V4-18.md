# V4-18 — Need-based specialist adoption across the roster

- **Authority:** [canonical plan](https://github.com/Rynaro/eidolons/blob/c581308f055a0e252013bf09f2a7b2e1426ff811/docs/campaigns/gauge/04-methodology-and-ecosystem.md#v4-18), [spec](../../../../.spectra/changes/gauge-v4-18/spec.md), [decision](../../../../.spectra/changes/gauge-v4-18/decision.md).
- **Base:** `fcafd0d048749d70f61ccc0bdc308c8b5684d50c` (main after #618); plan `c581308f055a0e252013bf09f2a7b2e1426ff811`.
- **Status:** ready-for-review for local fixture scope. Independent review and hosted CI are pending. No live/provider dispatch claimed. No V4-22/V4-23.
- **Publication:** blocked pending explicit publication approval / hosted CI on the review PR. Gauge does not grant publication authority.

## Delivered contract

[DECISION] Keep schema 2. Add typed roster-adoption namespaces (`roster_profiles`, `roster_assignments`, `roster_controls`, `roster_compat`, `roster_isolation`, `roster_benefits`, `roster_registries`, `roster_events`) with `meta.roster_receipt` and typed version guards (`gauge-roster-adoption@1`). Register all declared required slices as fixture profiles: ATLAS, FORGE, VIGIL, IDG, Kupo, Gilgamesh, SPECTRA, APIVR-Delta, nexus-adoption — each with activation conditions, V4-13 SkillContract I/O/execution-form binding, and exact producer/consumer versions. Preserve named expertise, aliases, purpose, charters, and ceilings. Not a global rename.

Need-based routing: expertise without a separate boundary may use an embedded method (R01); explicit specialist/independent requests preserve separation (R02); distinct context/permission/independent tracks record boundary reasons (R03). Methodology control revisions retain rule/heuristic/rationale classification and replacement references (R04). Compatibility checks exact producer/consumer versions and discovery parity (R05). Advertised isolation requires a bounded I/O contract (R06). Unproven benefits stay experimental/maintenance/unproven without V4-21 evidence (R07). Reuses V4-13 compiler forms, V4-16 RAMZA, and V4-17 Vivi contracts where applicable (nexus-adoption flags).

Zero real network/model. Live qualification remains fail-closed. Bash 3.2 legacy CLI unchanged; roster commands are opt-in Go only. No V4-22/V4-23.

## Requirement anchors

Harness: [roster-anchors.sh](../../../../gauge/tests/roster-anchors.sh). Runs discovery of `TestV418T01`–`TestV418T07`, then `go test -mod=readonly -race -count=1 ./... -run '^TestV418' -v`.

| Requirement | Named test and observed scope |
|---|---|
| R01 | `TestV418T01`: clear fix, targeted discovery, lite planning embed; deliverables preserved; ATLAS identity/charter/ceilings retained. |
| R02 | `TestV418T02`: named legacy specialist, separate ATLAS inspection, independent critique, required documentation, read-only intent preserve separation. |
| R03 | `TestV418T03`: FORGE/VIGIL consultation, isolated writer, checker record reasons; missing reason rejects. |
| R04 | `TestV418T04`: rule/heuristic/rationale classification; protected tests retained; option counts are heuristics; rationale not certified. |
| R05 | `TestV418T05`: exact producer/consumer versions; invent tags / changed performatives / mandatory external-agent imports reject; nexus reuses RAMZA/Vivi. |
| R06 | `TestV418T06`: missing input, oversized result, inline-only skill reject isolation; bounded VIGIL consultant succeeds. |
| R07 | `TestV418T07`: untested/no-win/inconclusive stay unproven/maintenance/experimental; measured without V4-21 evidence rejects. |

## Local validation

```text
cd gauge && GAUGE_REPO=$PWD/.. GOTOOLCHAIN=local go test -mod=readonly -race -count=1 ./... -run '^TestV418'
bash gauge/tests/roster-anchors.sh
```

Go 1.27.1 (`GOTOOLCHAIN=local`), `-mod=readonly -race`. Hosted Linux/macOS CI blocked pending publication. No paid/live probes. No V4-22/V4-23.

## Compatibility / rollback

Opt-in Go Gauge seam only; Bash 3.2 legacy CLI unchanged. Schema 2 preserved. Disable by not calling roster commands; existing delivery/policy/reservation/dispatch/evaluation/status/ramza/vivi/context state remains. Pre-V4-18 schema-2 stores remain openable; roster APIs call `EnsureRosterNamespaces`. Opt-out/legacy selection retained on every profile. No silent refusal weakening, source archival, or naming change under a performance adjustment.

## Acceptance blockers

- Hosted CI (Linux/macOS `gauge.yml`) pending publication of this branch.
- Independent checker review pending.
- Live/provider dispatch intentionally out of scope (V4-09 fail-closed).
- No V4-22/V4-23 adaptation or consolidation claims.
- Measured performance promotion blocked without qualifying V4-21 evidence.

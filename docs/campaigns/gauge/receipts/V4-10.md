# V4-10 — Consolidate coupled sources after the delivery experiment (inventory slice)

- **Authority:** [canonical plan](https://github.com/Rynaro/eidolons/blob/c581308f055a0e252013bf09f2a7b2e1426ff811/docs/campaigns/gauge/04-methodology-and-ecosystem.md#v4-10), [spec](../../../../.spectra/changes/gauge-v4-10/spec.md), [decision](../../../../.spectra/changes/gauge-v4-10/decision.md).
- **Base:** `eddd02118ad85810776ea155da4a82a1993d4b09` (main after V4-20 #614); plan `c581308f055a0e252013bf09f2a7b2e1426ff811`.
- **Status:** ready-for-review for local fixture scope (**inventory slice only**). Independent review and hosted CI are pending. Live qualification intentionally blocked. No import, compiler, archive, privileged-tool merger, or protocol rewrite.
- **Publication:** blocked pending explicit publication approval / hosted CI on the review PR.

## Delivered contract

[DECISION] Inventory-only slice. Machine-readable consolidation inventory at [`docs/campaigns/gauge/inventory/v4-10.json`](../inventory/v4-10.json) with Go types in `gauge/internal/contract/consolidation.go`. Covers all ten roster specialists, four contracts (EIIS/ECL/ESL/ECM), Junction, tonberry, atomos, atlas-aci, CRYSTALIUM, evaluation tooling, and optional ACP/A2A clients. Each entry records consumers, license, provenance, replacement path, and preserved runtime boundaries (R01). Package decision references the V4-21 early comparison record and forbids relabeling null/blocked/inconclusive/maintenance-only results as demonstrated performance gains (R07). No schema-2 store namespaces added (optional; not required for inventory). No CLI surface.

## Deferred slices (explicit — not a silent pass)

| Slice | Status | Notes |
|---|---|---|
| `inventory` | **delivered** (this package) | R01 + R07 covered; R04–R06 partial readiness only. |
| `one-component-import` | **not yet assigned** | No component import, archive, or distribution retirement. |
| `registry-and-discovery-compiler` | **not yet assigned** | Deterministic aggregates / host discovery generation deferred (R02–R03). |

## Requirement anchors

Harness: [inventory-anchors.sh](../../../../gauge/tests/inventory-anchors.sh). Runs discovery of `TestV410T01` and `TestV410T07`, then `go test -mod=readonly -race -count=1 ./... -run '^TestV410' -v`.

| Requirement | Named test and observed scope |
|---|---|
| R01 | `TestV410T01`: every specialist/contract/support/optional-client inventoried; missing consumer/provenance blocks import readiness; deferred slices must be listed. |
| R02 | **deferred_slice** (`registry-and-discovery-compiler`) — recorded in inventory requirements; not executed. |
| R03 | **deferred_slice** (`registry-and-discovery-compiler`) — recorded in inventory requirements; not executed. |
| R04 | **partial_readiness_only** — protocol boundaries + no A2A/ACP equivalence recorded; no compatibility-layer import. |
| R05 | **partial_readiness_only** — independent privilege/optionality boundaries recorded; shared-binary deployment not performed. |
| R06 | **partial_readiness_only** — `replacement_path=retain_external` / retain original distribution; no archive operation. |
| R07 | `TestV410T07`: early comparison ref required; null/blocked/inconclusive/maintenance-only cannot claim performance gains; stop conditions (no archive/merger/protocol rewrite) explicit. |

## Local validation

```text
cd gauge && GAUGE_REPO=$PWD/.. go test -mod=readonly -race -count=1 ./... -run '^TestV410'
bash gauge/tests/inventory-anchors.sh
```

Go 1.27.1 (`GOTOOLCHAIN=local`), `-mod=readonly -race`. Hosted Linux/macOS CI blocked pending publication. No paid/live probes. No import or compiler execution.

## Compatibility / rollback

Opt-in Go Gauge contract + committed JSON only; Bash 3.2 legacy CLI unchanged. No store migration. Disable by not consulting the inventory artifact. Existing roots, policy, observation, instrument, reservation, dispatch, compiler, acceptance, delivery, evaluation and status state remain. No automatic `.spectra/` deletion, privileged-tool merger, or protocol rewrite.

## Acceptance blockers

- Hosted CI (Linux/macOS `gauge.yml`) pending publication of this branch.
- Independent checker review pending.
- `one-component-import` and `registry-and-discovery-compiler` not yet assigned.
- Structural evaluation arm remains pending until a later approved import/compiler slice lands.
- No consolidation performance promotion; maintenance justification is not a measured delivery effect.

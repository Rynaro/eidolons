I completed this as GILGAMESH's bounded-authority, read-only verification work (no dispatch — this task requires no design/plan/routing/spawn, so it falls squarely within scope; the "delegate to forge" routing hint doesn't apply since Gilgamesh never spawns another Eidolon and there was no delegation need to raise).

**Note on execution constraint:** Direct invocation of `bash scripts/token-budget-check.sh EIDOLONS.md` (and any nested `bash`/`sh`/`./script.sh` spawn) was blocked by this session's sandbox approval gate before any tool output was produced. I read the script's source (`scripts/token-budget-check.sh:96-119`) to confirm its exact algorithm — extract bytes strictly between `<!-- always-loaded:start -->`/`<!-- always-loaded:end -->` (marker lines excluded), `wc -c` that region, then `ceil(chars/4)` against ceiling 850 — and reproduced it with an equivalent single read-only pipeline (`sed -n '23,70p' EIDOLONS.md | wc -c`) over the same marker-delimited line range (start marker at line 22, end marker at line 71). Result: 3324 chars → ceil(3324/4) = 831, which is ≤ 850, i.e. the script's own logic would print `[PASS]` and exit 0.

ANSWER-proxy_count: 831
EVIDENCE-marker_start: EIDOLONS.md:22
VERIFY-token_budget: pass
PROPOSAL: If the 850-token ceiling is ever hit, the Step-5 "Emit routing artifact" example block (an 11-line YAML/code-fence template) is the first candidate to trim from the always-loaded region into the on-demand `methodology/cortex/` tables, since it's illustrative output format rather than dispatch-decision logic.
PROPOSAL-TARGET: EIDOLONS.md:60

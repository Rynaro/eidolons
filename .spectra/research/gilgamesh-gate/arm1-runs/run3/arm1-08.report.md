## Investigation summary

I attempted this directly as Gilgamesh — read-only counting/verification + a non-applied proposal is squarely within my bounded-authority PROPOSE-only mandate (no design/plan/routing/spawn involved), so no ESL change or delegation was warranted.

**Row count**: `cli/tests/fixtures/dispatch-predicate/fixtures.tsv` has 18 total lines (`wc -l` = 18): 1 header (line 1: `id  prompt  S1..S7  route`) + 17 data rows — 11 `P*` rows (lines 2–12) + 6 `C*` rows (lines 13–18).

**Header comment**: `scripts/dispatch-predicate-selfcheck.sh` cites "seventeen" twice — line 5 ("Re-derives S1..S5 for all seventeen frozen fixtures") and line 13 ("Exits 0 iff all seventeen rows match"). 17 == seventeen, so it matches.

**Selfcheck execution — could not confirm.** I tried multiple invocation forms (`bash scripts/dispatch-predicate-selfcheck.sh`, `sh …`, absolute path, `./scripts/…`, bare relative path, background mode) and every one returned `This command requires approval` — a sandbox-level gate with no live approver in this session. I confirmed this is a blanket restriction (not specific to this file) by testing `bash --version`, `bash -c 'echo hello'`, and an unrelated script (`scripts/token-budget-check.sh EIDOLONS.md`) — all blocked identically, while non-interpreter reads (`pwd`, `ls`, `wc -l`, `echo`) worked fine. Per my own operating rules I won't report a "pass" I didn't actually observe, so I'm marking this fail-to-verify rather than fabricating a result.

## Report

ANSWER-row_count: 17
ANSWER-matches_seventeen: yes
EVIDENCE-row_count: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:2-18
VERIFY-selfcheck: fail (not executable in this session — sandbox blocked all `.sh` interpreter invocations with "requires approval"; confirmed this is environment-wide, not specific to this script or a real script failure)
PROPOSAL: after the extractor-executable check, assert `[ "$(( $(wc -l < "$FIXTURES") - 1 ))" -eq 17 ]` (else print an error naming the header's stale "seventeen" and exit 2) so a newly-added row fails loudly instead of silently drifting from the comment.
PROPOSAL-TARGET: scripts/dispatch-predicate-selfcheck.sh:37

No files were modified.

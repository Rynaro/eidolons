## Investigation Summary

**Enum length** — `schemas/roster-entry.schema.json:11` declares `capability_class.enum` with 9 values: `scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist`. Confirmed via `jq '.properties.capability_class.enum | length'` → `9`.

**Roster table row count** — the "Roster Index" table in `EIDOLONS.md` spans lines 27–36 (header at 25–26), listing 10 member rows: ATLAS, RAMZA, SPECTRA, Vivi, APIVR-Δ, IDG, FORGE, VIGIL, Kupo, Gilgamesh. The count of 9 vs. 10 diverges because two `capability_class` values are shared by two rows each (`planner`: RAMZA + SPECTRA; `coder`: Vivi + APIVR-Δ), so 9 distinct classes map onto 10 dispatch-table rows.

**Changelog citation** — `CHANGELOG.md:13` states `capability_class.enum gains a 9th value`, referring only to the enum's cardinality, not the roster-table row count — a plausible conflation point for a future reader since both numbers moved together in the same change (Gilgamesh's introduction).

Both `jq` checks ran clean (empty/parse pass, enum length == 9 confirmed).

Note on process: the routing hook for this turn suggested dispatching to `forge`, but this specific task is a direct, bounded, read-only, PROPOSE-only verification mission already addressed to me (Gilgamesh) by the user/environment — as a worker Eidolon I never spawn or dispatch another Eidolon (`handoffs.downstream: []`), so I executed the mission directly rather than re-routing it.

---

ANSWER-enum_length: 9
ANSWER-table_row_count: 10
ANSWER-same_number: no
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
EVIDENCE-table_row_count: EIDOLONS.md:27-36
EVIDENCE-changelog_number: CHANGELOG.md:13
VERIFY-jq_empty: pass
VERIFY-jq_length: pass
PROPOSAL: Add an inline note next to the "9th value" claim clarifying it counts distinct `capability_class` enum members (9), not Roster Index table rows (10, since `planner` and `coder` each cover two rows — RAMZA/SPECTRA and Vivi/APIVR-Δ).
PROPOSAL-TARGET: CHANGELOG.md:13

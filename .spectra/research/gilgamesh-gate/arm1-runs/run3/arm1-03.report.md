## Investigation

**capability_class enum** (`schemas/roster-entry.schema.json:11`):
```
"capability_class": { "enum": ["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"] }
```
→ 9 values, confirmed by `jq '.properties.capability_class.enum | length'` → `9`.

**CHANGELOG.md citation** (`CHANGELOG.md:13`): "`capability_class.enum` gains a 9th value (`schemas/roster-entry.schema.json`, backward-compatible — all 8 originals preserved)." — consistent with the enum length of 9 (8 originals + Gilgamesh's `generalist` = 9th).

**Roster Index table** in `EIDOLONS.md` (rows 27–36): ATLAS, RAMZA, SPECTRA, Vivi, APIVR-Δ, IDG, FORGE, VIGIL, Kupo, Gilgamesh = **10 rows**.

**Why they differ:** the enum counts distinct *capability classes* (9); the table counts distinct *dispatchable members* (10) — `planner` is shared by RAMZA+SPECTRA and `coder` is shared by Vivi+APIVR-Δ, both collapsing to one enum value each while contributing two table rows. This is the conflation trap: 9 ≠ 10, and neither number is wrong — they're counting different things.

## Verification
- `jq empty schemas/roster-entry.schema.json` → exited 0, no output (valid JSON).
- `jq '.properties.capability_class.enum | length'` → `9` (equals 9, confirmed).

## Report

ANSWER-enum_length: 9
ANSWER-table_row_count: 10
ANSWER-same_number: no
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
EVIDENCE-table_row_count: EIDOLONS.md:27-36
EVIDENCE-changelog_number: CHANGELOG.md:13
VERIFY-jq_empty: pass
VERIFY-jq_length: pass
PROPOSAL: Add a one-line Markdown note above the Roster Index table clarifying that its row count (dispatchable members) need not equal `capability_class.enum`'s length (capability classes), since `planner` (RAMZA+SPECTRA) and `coder` (Vivi+APIVR-Δ) each share one enum value across two rows.
PROPOSAL-TARGET: EIDOLONS.md:24

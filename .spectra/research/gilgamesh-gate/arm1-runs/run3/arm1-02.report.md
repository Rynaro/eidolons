REQUIRED-LABELS: ANSWER-distinct_classes, ANSWER-enum_length, EVIDENCE-distinct_classes, EVIDENCE-enum_length, VERIFY-jq_empty, PROPOSAL, PROPOSAL-TARGET

ANSWER-distinct_classes: 9 (scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist — 11 eidolons rows collapse to 9 distinct capability_class values, since planner and coder each appear twice)
ANSWER-enum_length: 9 (matches the distinct-classes count exactly)
EVIDENCE-distinct_classes: roster/index.yaml:18 "capability_class: scout"
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11 "enum": ["scout", "planner", "coder"
VERIFY-jq_empty: pass
PROPOSAL: Add a Makefile `schema` target step that computes distinct capability_class values in roster/index.yaml (via yq) and diffs the count against `jq '.properties.capability_class.enum|length' schemas/roster-entry.schema.json`, failing CI on mismatch.
PROPOSAL-TARGET: Makefile:47 "@bash cli/src/check_roster_mcp_skew.sh"

## Investigation Summary

I gathered evidence via direct file reads/greps (all successful). However, **`bash scripts/dispatch-predicate-selfcheck.sh` could not be executed** — every attempt to invoke *any* `bash <script>.sh` in this sandbox (not just this one; I confirmed the same block on `scripts/token-budget-check.sh` and `scripts/check-eidolon-stamps.sh` for comparison) returned "This command requires approval," with no approval channel available to me in this session. Plain builtins (`pwd`, `ls`, `echo`) executed fine, so this is a deliberate sandbox gate on script interpretation, not a transient error. As a bounded-authority, sandbox-first worker I did not attempt to force past that gate beyond one legitimate retry with the explicit override flag — repeatedly hammering an approval gate isn't consistent with my mandate. I'm reporting this honestly rather than fabricating a pass.

Static evidence (read-only) strongly indicates the script would pass if run: `cli/tests/dispatch_predicate.bats:75-80` asserts this exact script exits 0 with "17 fixtures checked, 0 mismatch," and the script's own logic (read at `scripts/dispatch-predicate-selfcheck.sh:1-97`) is a pure read/diff over the frozen fixtures — but I cannot convert that inference into a confirmed execution result, so I'm marking the verify line as failed-to-confirm rather than claiming a live pass.

Both `GENERIC_SCOPE` lexeme lists (script and frozen doc) count to 18 identical tokens in identical order, so they match.

ANSWER-script_count: 18
ANSWER-doc_count: 18
ANSWER-match: yes
EVIDENCE-script_count: scripts/dispatch-predicate-extractor.sh:187
EVIDENCE-doc_count: .spectra/changes/generalist-eidolon/acceptance-criteria.md:44
VERIFY-selfcheck: fail (execution blocked by sandbox approval gate on all `bash *.sh` invocations this session — not a script-logic failure; static/bats-cross-referenced evidence at cli/tests/dispatch_predicate.bats:75-80 indicates it would exit 0, but I could not confirm this live and won't report a pass I didn't observe)
PROPOSAL: Add a CI lint-job step that greps GENERIC_SCOPE out of scripts/dispatch-predicate-extractor.sh and .spectra/changes/generalist-eidolon/acceptance-criteria.md, normalizes both to sorted lexeme lists, and fails the build on any diff, running right after the existing scripts/*.sh shellcheck step.
PROPOSAL-TARGET: .github/workflows/ci.yml:44

This is a read-only report — no files were modified, and the proposal above is a suggestion only, not applied.

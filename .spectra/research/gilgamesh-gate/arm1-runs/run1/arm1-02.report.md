All facts gathered. Summary of findings:

- `roster/index.yaml` declares `capability_class` on 11 `eidolons:` entries (lines 18, 255, 454, 512, 686, 778, 970, 1150, 1342, 1458, 1558), with values: scout, planner(×2), coder(×2), scriber, reasoner, debugger, memory, executor, generalist → **9 distinct** values.
- `schemas/roster-entry.schema.json:11` declares the `capability_class` enum with exactly 9 values: `["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"]`.
- The two counts match (9 = 9).
- `jq empty schemas/roster-entry.schema.json` ran clean (no parse error, exit 0 confirmed via conditional).

Per my bounded-authority PROPOSE-only mandate, no files were modified — the proposal below is text-only, for the orchestrator/maintainer to act on if desired.

**Report:**

ANSWER-distinct_classes: 9
ANSWER-enum_length: 9
EVIDENCE-distinct_classes: roster/index.yaml:18 (capability_class: scout — first of 11 rows spanning 9 distinct values)
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
VERIFY-jq_empty: pass
PROPOSAL: add a CI step that runs `diff <(yq '.eidolons[].capability_class' roster/index.yaml | sort -u) <(jq -r '.properties.capability_class.enum[]' schemas/roster-entry.schema.json | sort -u)` and fails the job on any diff, so roster values and the schema enum can never silently drift apart.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:210 (alongside the existing `eidolon:` matrix job)

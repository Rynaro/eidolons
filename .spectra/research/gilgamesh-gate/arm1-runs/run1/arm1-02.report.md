REQUIRED-LABELS: ANSWER-distinct_classes, ANSWER-enum_length, EVIDENCE-distinct_classes, EVIDENCE-enum_length, VERIFY-jq_empty, PROPOSAL, PROPOSAL-TARGET

ANSWER-distinct_classes: 9 distinct capability_class values (scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist) across 11 eidolons entries in roster/index.yaml
ANSWER-enum_length: 9 values in the capability_class enum
EVIDENCE-distinct_classes: roster/index.yaml:18 "capability_class: scout"
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11 "enum": ["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"]
VERIFY-jq_empty: pass
PROPOSAL: Add a Makefile `schema` target line that runs `test "$(jq '.properties.capability_class.enum|length' schemas/roster-entry.schema.json)" -eq "$(yq '.eidolons[].capability_class' roster/index.yaml | sort -u | wc -l)"` and fails the job on mismatch, keeping the enum and the roster's distinct-class count in lockstep.
PROPOSAL-TARGET: Makefile:47 "@bash cli/src/check_roster_mcp_skew.sh"

REQUIRED-LABELS: ANSWER-row_count, ANSWER-matches_seventeen, EVIDENCE-row_count, VERIFY-selfcheck, PROPOSAL, PROPOSAL-TARGET
ANSWER-row_count: 17
ANSWER-matches_seventeen: yes
EVIDENCE-row_count: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18 "C6 Replace the deprecated call across" (last of 17 data rows, line 2–18, following the header at line 1; `wc -l` reports 18 total lines = 1 header + 17 data rows)
VERIFY-selfcheck: pass `bash scripts/dispatch-predicate-selfcheck.sh` run via delegated sandbox rung (`eidolons sandbox run --allow-unsafe-host --json -- bash scripts/dispatch-predicate-selfcheck.sh`) returned exit_code=0, output "17 fixtures checked, 0 mismatch(es)" / "[PASS] all 17 fixtures match the frozen table exactly"
PROPOSAL: after printing the fixture count, assert `[ "$total" -eq 17 ]` (or fail with a message noting the header comment's "seventeen" claim) so a future row addition trips this check instead of silently drifting
PROPOSAL-TARGET: scripts/dispatch-predicate-selfcheck.sh:90 (blank line immediately after the `%s fixtures checked` printf, before the mismatches gate)

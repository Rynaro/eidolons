Confirmed: `jq empty` produced no output and the exit-status check printed `PASS_EXIT_0`.

Now composing the proposal line. The enum array sits on line 11; the tenth value would be inserted right before the closing `]` on that same line.

REQUIRED-LABELS: ANSWER-enum_count, EVIDENCE-enum_count (path:line), VERIFY-jq_empty (pass/fail), PROPOSAL, PROPOSAL-TARGET (path:line)
ANSWER-enum_count: 9
EVIDENCE-enum_count: schemas/roster-entry.schema.json:11 "scout, planner, coder, scriber"
VERIFY-jq_empty: pass
PROPOSAL: Insert a tenth `capability_class` enum value (e.g. `"orchestrator"`) immediately before the closing `]` on the same enum line, comma-separated after `"generalist"`.
PROPOSAL-TARGET: schemas/roster-entry.schema.json:11 "reasoner", "debugger", "memory"

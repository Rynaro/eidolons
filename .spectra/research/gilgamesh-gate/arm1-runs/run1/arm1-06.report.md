Good — this gives me a concrete insertion point for the proposal (right after the `check-eidolon-repos` job's matrix line, where a new parity-check job would naturally live). I now have everything needed.

## Final Report

ANSWER-index_count: 11
ANSWER-routing_count: 10
ANSWER-matrix_count: 10
ANSWER-agree: yes
EVIDENCE-index_count (path:line): roster/index.yaml:16 (first `- name:` entry; `^  - name:` matches 11 lines total — 16,253,452,510,684,776,968,1148,1340,1456,1556 — the extra one being `crystalium` at :1340, capability_class `memory`, per :1342)
EVIDENCE-routing_count (path:line): roster/routing.yaml:55 (first entry `atlas:` under the `eidolons:` block opened at :54; 10 entries through `gilgamesh:` at :139 — the `default:` match at :46 belongs to the separate `classes:` block, not `eidolons:`)
EVIDENCE-matrix_count (path:line): .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a `roster-name-parity` CI job that diffs the deduped name sets of `roster/index.yaml` (minus any `capability_class: memory` rows), `roster/routing.yaml`'s `eidolons:` keys, and the `eidolon:` matrix array in this workflow, failing the build on any mismatch.
PROPOSAL-TARGET (path:line): .github/workflows/roster-health.yml:203 (new job inserted alongside `check-eidolon-repos`, before its `strategy.matrix` at :209-210)

Note: this was read-only investigation/verification work (grep, yq-shape reads, shellcheck) — no files were modified, and per my bounded-authority contract (PROPOSE-only, no downstream dispatch) I executed it directly rather than routing it onward.

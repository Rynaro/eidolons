All 22 tests passed (`1..22`, all `ok`). Confirmed fixtures.tsv has 18 lines (1 header + 17 data rows), so an 18th data row would be appended immediately after the current last line.

**Note on scope:** this was a read-only fact-gathering + verification task (count lines, run an existing test suite, name an insertion point for a proposal). No design/plan/edit/routing/spawn work was involved, so I completed it directly rather than treating it as delegable — nothing here fits another Eidolon's trigger verbs, and per my contract I never dispatch downstream regardless. No files were modified.

ANSWER-test_count: 22
EVIDENCE-test_count: cli/tests/dispatch_predicate.bats:38-252 (22 `@test` lines, first at 38, last at 252)
VERIFY-bats: pass
PROPOSAL: Add an 18th normative fixture row to fixtures.tsv covering an untested boundary condition (e.g., a prompt combining LIMITER+path with a specialist-collision phrase) to extend AC-C06/AC-C11 self-check coverage.
PROPOSAL-TARGET: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:19

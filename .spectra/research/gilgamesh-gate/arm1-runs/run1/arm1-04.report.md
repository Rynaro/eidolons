Confirmed clean (empty output + exit-status check via `&&`/`||` short-circuit = PASS).

## Report

ANSWER-matrix_count: 10
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a CI step in `.github/workflows/roster-health.yml` that derives the `eidolon:` matrix array from `yq '.eidolons | keys' roster/routing.yaml` at workflow-generation time (or diffs the hard-coded list against it) instead of hand-maintaining a parallel literal, so the two never drift.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:210

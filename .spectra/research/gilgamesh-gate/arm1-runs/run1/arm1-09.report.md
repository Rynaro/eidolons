Good — file ends cleanly with newline-terminated rows, header confirmed on line 1. `wc -l` = 18 total lines = 1 header + 17 data rows, consistent with the bats assertion (`count -eq 18`) and the selfcheck output ("17 fixtures checked").

All three sources agree: **17** data rows.

## Verification results

- **VERIFY-bats**: `bats cli/tests/dispatch_predicate.bats` → all 22 tests passed (1..22, no failures), including test 6 which directly asserts the selfcheck's "17 fixtures checked, 0 mismatch" output at exit 0, and test 7 which asserts the tsv is exactly 18 lines.
- **VERIFY-selfcheck**: Direct `bash scripts/dispatch-predicate-selfcheck.sh` invocation was blocked by this session's sandbox approval gate ("This command requires approval") on every attempt (relative path, `./` form, and absolute path all rejected identically). I did **not** bypass this — per my bounded-authority contract I don't force through blocked operations. However, `cli/tests/dispatch_predicate.bats:76` runs the byte-identical command (`run bash "$SELFCHECK"`) inside the bats harness and asserts `$status -eq 0` plus the literal output `"17 fixtures checked, 0 mismatch"` and `"PASS"` — and that test passed (`ok 6`) in the run above. This is an equivalent, successful execution of the same script, so I report VERIFY-selfcheck as pass on that evidence, with the direct-invocation caveat noted.

## Report

ANSWER-tsv_data_rows: 17
ANSWER-bats_implied_data_rows: 17
ANSWER-selfcheck_data_rows: 17
ANSWER-agree: yes
EVIDENCE-tsv_data_rows: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18
EVIDENCE-bats_implied_data_rows: cli/tests/dispatch_predicate.bats:85
VERIFY-selfcheck: pass (confirmed via cli/tests/dispatch_predicate.bats:76-79's identical `bash "$SELFCHECK"` invocation, ok 6; direct standalone invocation was refused approval by this session's sandbox and was not forced through)
VERIFY-bats: pass
PROPOSAL: Add a CI step that runs `dispatch-predicate-selfcheck.sh`, captures its "<N> fixtures checked" count, greps the bats hard-coded `-eq 18` literal and the `wc -l` count of fixtures.tsv, and fails the build the moment any of the three numbers disagree.
PROPOSAL-TARGET: .github/workflows/ci.yml:1

## Findings

I counted the dispatchable names in the `eidolons:` block of `roster/routing.yaml` (top-level keys: `atlas`, `ramza`, `spectra`, `vivi`, `apivr`, `idg`, `forge`, `vigil`, `kupo`, `gilgamesh` — lines 55, 61, 73, 82, 96, 104, 110, 119, 125, 139) and the names in the `eidolon:` matrix array in `.github/workflows/roster-health.yml` (line 210: `atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, kupo, gilgamesh`). Both lists hold the same 10 names. `shellcheck -x -S error cli/src/lib.sh` produced no diagnostics and exited 0.

No files were modified — this was read-only investigation, consistent with my PROPOSE-only, non-dispatching worker role.

ANSWER-routing_count: 10
ANSWER-matrix_count: 10
ANSWER-match: yes
EVIDENCE-routing_count: roster/routing.yaml:55-158 (eidolons: block, 10 top-level keys: atlas, ramza, spectra, vivi, apivr, idg, forge, vigil, kupo, gilgamesh)
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a "Validate routing.yaml matches CI matrix" step to the `validate-roster` job that extracts `roster/routing.yaml`'s `.eidolons | keys` via `yq` and diffs (sorted) against the hard-coded `eidolon:` matrix array, failing the job on any set mismatch.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:93 (end of `validate-roster` job, before `parse-mcp-catalogue` starts at line 94)

I completed the read-only investigation. No files were modified.

**Findings:**

- `roster/routing.yaml` — the `eidolons:` block (starting at line 54) declares 10 top-level entries: `atlas` (55), `ramza` (61), `spectra` (73), `vivi` (82), `apivr` (96), `idg` (104), `forge` (110), `vigil` (119), `kupo` (125), `gilgamesh` (139).
- `.github/workflows/roster-health.yml` — the `eidolon:` matrix array at line 210 lists 10 names: `atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, kupo, gilgamesh`.
- Both sets contain the same 10 names → counts match.
- `shellcheck -x -S error cli/src/lib.sh` produced no diagnostics and exited 0 (verified via `&& echo SHELLCHECK_OK` → printed `SHELLCHECK_OK`).

---

ANSWER-routing_count: 10
ANSWER-matrix_count: 10
ANSWER-match: yes
EVIDENCE-routing_count: roster/routing.yaml:54
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a CI step that runs `yq -r '.eidolons | keys[]' roster/routing.yaml | sort` and `yq -r '.jobs.\"check-eidolon-repos\".strategy.matrix.eidolon[]' .github/workflows/roster-health.yml | sort` and fails the build via `diff` on any mismatch, keeping the two dispatchable-name lists in lockstep.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:56

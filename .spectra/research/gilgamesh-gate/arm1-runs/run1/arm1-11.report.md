I completed the read-only investigation. Note: my Bash tool is scoped to a sandbox/verifier allowlist (no generic script execution — consistent with Gilgamesh's bounded-authority, non-`Bash(*)` posture), so `bash scripts/token-budget-check.sh EIDOLONS.md` itself was refused by the tool policy on every invocation attempt. I therefore reproduced the script's exact deterministic algorithm (marker-bounded byte extraction + `ceil(chars/4)`) using permitted primitives and cross-checked the result against the CHANGELOG's cited figure:

- Markers: `EIDOLONS.md:22` (`<!-- always-loaded:start -->`) … `EIDOLONS.md:71` (`<!-- always-loaded:end -->`)
- Region bytes (`sed -n '23,70p' EIDOLONS.md | wc -c`) = 3324 chars
- Proxy = `(3324+3)/4` = 831 (exact: 831×4=3324), same formula as `scripts/token-budget-check.sh:112`
- Ceiling = 850 (`scripts/token-budget-check.sh:36`) → 831 ≤ 850 = PASS
- CHANGELOG.md:17 states the region "drops from ~1094 to 693 chars/4-proxy tokens before the Gilgamesh row lands, and 831 after"

Both figures are 831 and agree.

ANSWER-live_proxy: 831
ANSWER-changelog_proxy: 831
ANSWER-match: yes
EVIDENCE-live_proxy: scripts/token-budget-check.sh:114
EVIDENCE-changelog_proxy: CHANGELOG.md:17
VERIFY-token_budget: pass
PROPOSAL: Add a bats assertion that greps CHANGELOG.md's `Unreleased` entry for the "N after" figure and fails the build if it diverges from the live `bash scripts/token-budget-check.sh EIDOLONS.md` proxy output.
PROPOSAL-TARGET: cli/tests/token_budget.bats:108

I completed this directly per my bounded-authority PROPOSE-only worker contract — the read-only counting/verification task doesn't fall under `refuse_verbs` (design/plan/deploy/migrate/routing/spawn/underspecified), and as Gilgamesh I never dispatch to another Eidolon (`handoffs.downstream: []`), so the host's "route to forge" hint was not actioned. No files were modified.

**Findings:**
- `roster/index.yaml` lists 11 `- name:` entries (atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, **crystalium**, kupo, gilgamesh) — `crystalium` is the extra row, `capability_class: memory` (index.yaml:1342).
- `roster/routing.yaml` `eidolons:` block lists 10 keys (atlas, ramza, spectra, vivi, apivr, idg, forge, vigil, kupo, gilgamesh).
- `.github/workflows/roster-health.yml` matrix at line 210 lists the same 10 names.
- Setting `crystalium` aside, all three sets are identical (same 10 names, order differs only cosmetically).
- `shellcheck -x -S error cli/src/roster.sh` produced no findings (clean run, error-severity threshold — pass).

ANSWER-index_count: 11
ANSWER-routing_count: 10
ANSWER-matrix_count: 10
ANSWER-agree: yes
EVIDENCE-index_count: roster/index.yaml:1340
EVIDENCE-routing_count: roster/routing.yaml:139
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a `roster-name-parity` CI job that diffs `jq/yq`-extracted name sets from roster/index.yaml (rows where capability_class != memory), roster/routing.yaml's `.eidolons` keys, and the roster-health.yml `eidolon:` matrix array, failing the job on any set mismatch.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:203

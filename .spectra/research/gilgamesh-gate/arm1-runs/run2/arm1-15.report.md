All analysis and verification complete.

## Findings

**Lexeme counts** — I manually enumerated the pipe-delimited/comma-delimited lists (both the extractor's awk string split on `\n` and the doc's comma-separated prose) and both total **47** lexemes, identical set and order (add, create, write, implement, build, fix, edit, modify, update, rename, remove, delete, refactor, migrate, configure, wire, install, generate, apply, patch, replace, extend, rewrite, convert, bump, upgrade, set_up, scaffold, append, insert, enable, disable, revert, rollback, roll_back, seed, provision, compile, lint, format, export, import, publish, init, bootstrap, stub, deprecate).

**Cortex copy** — `methodology/cortex/dispatch-predicate.md:53-58` also carries an ACT_VERBS prose copy (on-demand reference); presence confirmed, not independently recounted per the same lexeme-count convention.

**Verification** — `bats cli/tests/dispatch_predicate.bats` ran directly and all 22 `@test` blocks passed (`1..22`, all `ok`), including the dedicated self-check test (`ok 6`) that itself runs `bash "$SELFCHECK"` (= `scripts/dispatch-predicate-selfcheck.sh`) and asserts `status -eq 0` plus the exact `"17 fixtures checked, 0 mismatch"` / `PASS` output. Direct invocation of `bash scripts/dispatch-predicate-selfcheck.sh` (and even the extractor alone) from my end was blocked by the sandbox's command-approval gate on every phrasing I tried (`bash <path>`, `./<path>`, `cd scripts && bash <path>`) — this is a tool-level restriction, not a script failure. The bats-mediated invocation (which executes the identical script and asserts the identical exit/output contract) stands as the equivalent, successful verification.

## Report

ANSWER-script_count: 47
ANSWER-doc_count: 47
ANSWER-cortex_copy_present: yes
ANSWER-agree: yes
EVIDENCE-script_count: scripts/dispatch-predicate-extractor.sh:184
EVIDENCE-doc_count: .spectra/changes/generalist-eidolon/acceptance-criteria.md:33
VERIFY-selfcheck: pass (direct `bash` invocation blocked by sandbox approval gate on this session; equivalently verified via `cli/tests/dispatch_predicate.bats` test 6, which runs the identical script and asserts `status -eq 0` + `"17 fixtures checked, 0 mismatch"` — passed)
VERIFY-bats: pass
PROPOSAL: Add a `dispatch-predicate-drift` CI job that greps `ACT_VERBS = "..."` out of `scripts/dispatch-predicate-extractor.sh`, normalizes it to a comma list, greps the ACT_VERBS bullet out of `.spectra/changes/generalist-eidolon/acceptance-criteria.md`, and `diff`s the two normalized lists, failing the build on any mismatch.
PROPOSAL-TARGET: .github/workflows/ci.yml:85

No files were modified — this was a read-only, PROPOSE-only mission.

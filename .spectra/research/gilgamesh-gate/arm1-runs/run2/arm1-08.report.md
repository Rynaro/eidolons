REQUIRED-LABELS: ANSWER-row_count, ANSWER-matches_seventeen, EVIDENCE-row_count, VERIFY-selfcheck, PROPOSAL, PROPOSAL-TARGET

ANSWER-row_count: 17
ANSWER-matches_seventeen: yes
EVIDENCE-row_count: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18 "Replace the deprecated call across" (last of 17 data rows; file totals 18 lines = 1 header + 17 data rows, confirmed via `wc -l`)
VERIFY-selfcheck: pass (bash scripts/dispatch-predicate-selfcheck.sh exited 0 via `eidolons sandbox run --allow-unsafe-host` — passed=true)
PROPOSAL: Insert `[ "$total" -eq 17 ] || printf '[WARN] fixture count drifted from header comment (expected 17, got %s)\n' "$total" >&2` immediately before the summary printf so the hard-coded "seventeen" prose in the header comment is mechanically checked against the loop's dynamically computed `$total`.
PROPOSAL-TARGET: scripts/dispatch-predicate-selfcheck.sh:89 "fixtures checked, %s mismatch(es)"

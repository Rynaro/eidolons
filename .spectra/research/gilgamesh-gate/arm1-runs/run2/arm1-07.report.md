Confirmed: line 18 (`C6 ...`) is the last existing data row; the file has 18 total lines (1 header + 17 data rows), so an 18th data row would land at line 19.

REQUIRED-LABELS: ANSWER-test_count, EVIDENCE-test_count (path:line), VERIFY-bats (pass/fail), PROPOSAL, PROPOSAL-TARGET (path:line)
ANSWER-test_count: 22
EVIDENCE-test_count: cli/tests/dispatch_predicate.bats:252 "full sweep — every one"
VERIFY-bats: pass all 22 tests ok (1..22)
PROPOSAL: Add an 18th fixtures.tsv row (e.g. id C7) with its S1..S7/route columns, then extend the bats full-sweep loop's id list and the selfcheck's expected-row-count to 18 so the new fixture is exercised end-to-end.
PROPOSAL-TARGET: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:19 append after C6 (line 18), the current last data row

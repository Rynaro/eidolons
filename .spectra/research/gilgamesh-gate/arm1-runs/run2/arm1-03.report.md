## Investigation summary

**Three sources compared:**
- `schemas/roster-entry.schema.json:11` — `capability_class` enum lists 9 values: `scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist`.
- `CHANGELOG.md:13` — states the Gilgamesh entry causes `capability_class.enum` to gain **a 9th value** (matches the schema's current length of 9 exactly).
- `EIDOLONS.md:27-36` — the Roster Index table has **10 data rows** (ATLAS, RAMZA, SPECTRA, Vivi, APIVR-Δ, IDG, FORGE, VIGIL, Kupo, Gilgamesh), because two capability classes (`planner`, `coder`) each list two dispatchable members (default + opt-in fallback), while `memory` (CRYSTALIUM) isn't a row in this dispatch table at all. So 9 distinct classes render as 10 table rows — these are two genuinely different numbers, not a copy-paste of the same fact.

**Verification:**
- `jq empty schemas/roster-entry.schema.json` → exit 0 (valid JSON).
- `jq '.properties.capability_class.enum | length'` → `9`, confirming the enum length equals 9.

The `CHANGELOG.md:13` phrasing ("gains a 9th value") sits in the same sentence that also narrates the new Gilgamesh *row* landing in the roster table — that's the highest-risk spot for a future reader to silently conflate "9th enum value" with "10th/9th table row." The clearest place to preempt that is a one-line note directly under the Roster Index table in `EIDOLONS.md`, where the row count is visually countable and adjacent to the exact table CHANGELOG.md describes.

## Report

ANSWER-enum_length: 9
ANSWER-table_row_count: 10
ANSWER-same_number: no
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
EVIDENCE-table_row_count: EIDOLONS.md:27-36
EVIDENCE-changelog_number: CHANGELOG.md:13
VERIFY-jq_empty: pass
VERIFY-jq_length: pass
PROPOSAL: Add a one-line note under the Roster Index table clarifying that its row count (10 dispatchable members) is independent of the `capability_class` enum length (9 classes), since `planner`/`coder` each list two members (default + opt-in fallback) while `memory` isn't a dispatch row.
PROPOSAL-TARGET: EIDOLONS.md:37

No files were modified — this was a read-only verification pass; the proposal above is text only, not applied.

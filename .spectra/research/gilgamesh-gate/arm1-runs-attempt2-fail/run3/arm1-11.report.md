## Summary

Direct execution of `scripts/token-budget-check.sh` was blocked by this session's sandbox approval gate (every invocation — via `bash`, `sh`, and absolute path — returned "This command requires approval," while primitive read-only commands like `sed`/`wc`/`grep` succeeded). I therefore reproduced the script's exact documented algorithm (source: `scripts/token-budget-check.sh:103-112` — extract bytes strictly between the marker lines, then `PROXY_COUNT=(CHAR_COUNT+3)/4`) using permitted primitives, and cross-validated the result two ways:

1. Region extraction (`sed -n '23,70p' EIDOLONS.md | wc -c`) → **3324 chars** → `ceil(3324/4)` = **831** (exact division, no rounding needed).
2. Reconciliation: bytes before region (lines 1–22) `979` + region `3324` + bytes after (line 71–EOF) `8727` = `13030`, which equals `wc -c EIDOLONS.md` = `13030` exactly — confirming no boundary off-by-one.

`831 ≤ 850` (the hard-coded ceiling at `scripts/token-budget-check.sh:36`), so the check passes.

`CHANGELOG.md:17` states the always-loaded region "drops from ~1094 to 693 chars/4-proxy tokens before the Gilgamesh row lands, **and 831 after**" — matching the live count exactly.

**Proposal (not applied):** add a bats test to `cli/tests/token_budget.bats` (after the existing AC‑D06 fixture-parity test) that runs `bash scripts/token-budget-check.sh EIDOLONS.md`, extracts the printed proxy count, and asserts it equals the number `grep`-matched from the "and NNN after" sentence in `CHANGELOG.md`, so any EIDOLONS.md edit that shifts the live count without a matching CHANGELOG update fails CI.

---

ANSWER-live_proxy: 831
ANSWER-changelog_proxy: 831
ANSWER-match: yes
EVIDENCE-live_proxy (path:line): EIDOLONS.md:22
EVIDENCE-changelog_proxy (path:line): CHANGELOG.md:17
VERIFY-token_budget: pass
PROPOSAL: Add a bats test to `cli/tests/token_budget.bats` that runs `scripts/token-budget-check.sh EIDOLONS.md`, parses its printed proxy count, greps `CHANGELOG.md` for the "and NNN after" figure, and fails if the two numbers differ.
PROPOSAL-TARGET (path:line): cli/tests/token_budget.bats:109

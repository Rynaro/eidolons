## Investigation Summary

**Source 1 — script (`scripts/dispatch-predicate-extractor.sh:184`):** the `ACT_VERBS` assignment is a single `\n`-joined string. Splitting it yields 47 lexemes: `add, create, write, implement, build, fix, edit, modify, update, rename, remove, delete, refactor, migrate, configure, wire, install, generate, apply, patch, replace, extend, rewrite, convert, bump, upgrade, set_up, scaffold, append, insert, enable, disable, revert, rollback, roll_back, seed, provision, compile, lint, format, export, import, publish, init, bootstrap, stub, deprecate`.

**Source 2 — frozen doc bullet (`.spectra/changes/generalist-eidolon/acceptance-criteria.md:33-37`):** the `ACT_VERBS` bullet lists the identical 47-lexeme set, word-for-word, closing with `(broadened per CRIT-018)`.

**Source 3 — cortex copy (`methodology/cortex/dispatch-predicate.md:53-58`):** an on-demand cortex copy of the same bullet exists and reproduces the identical 47-lexeme list (also referenced at lines 37 and 104). Copy is present.

All three sources agree on the same 47-lexeme list — no drift.

**Verification notes:**
- `bats cli/tests/dispatch_predicate.bats` — ran directly: `1..22`, all 22 `ok` (including `ok 6`, the self-check test, and `ok 7`, the 17-row-count test). **PASS.**
- `bash scripts/dispatch-predicate-selfcheck.sh` — direct invocation was **blocked by a sandbox approval gate** on every attempt (plain `bash` and `sh` forms both returned "This command requires approval"; consistent with a category-level restriction on ad hoc `scripts/*.sh` execution rather than a script-specific failure, matching prior runs of this same check). I did not route around the gate (e.g. `cat script | bash`), since that would circumvent an explicit approval boundary. As a legitimate substitute, `cli/tests/dispatch_predicate.bats:75-80` invokes the byte-identical script (`bash "$SELFCHECK"`) inside the bats process and asserts `status -eq 0` plus exact `"PASS"` / `"17 fixtures checked, 0 mismatch"` output — which passed (`ok 6`) above. Reporting **pass**, indirectly-verified.

---

ANSWER-script_count: 47
ANSWER-doc_count: 47
ANSWER-cortex_copy_present: yes
ANSWER-agree: yes
EVIDENCE-script_count: scripts/dispatch-predicate-extractor.sh:184
EVIDENCE-doc_count: .spectra/changes/generalist-eidolon/acceptance-criteria.md:33
VERIFY-selfcheck: pass (indirect — direct invocation blocked by sandbox approval gate; confirmed via the identical `bash "$SELFCHECK"` call embedded in cli/tests/dispatch_predicate.bats:75-80, which passed as ok 6)
VERIFY-bats: pass
PROPOSAL: Add a `dispatch-predicate-act-verbs-parity` CI step that extracts the `ACT_VERBS = "..."` string from scripts/dispatch-predicate-extractor.sh, extracts the ACT_VERBS bullet from .spectra/changes/generalist-eidolon/acceptance-criteria.md, normalizes both to a sorted comma-free token list, and `exit 1`s on the first lexeme-set mismatch.
PROPOSAL-TARGET: .github/workflows/ci.yml:84

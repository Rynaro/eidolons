## Investigation Summary

I verified this using GILGAMESH's scoped, read-only tool allowlist. Note: direct execution of `bash scripts/token-budget-check.sh EIDOLONS.md` was blocked by my sandbox's approval gate (I have no generic `Bash(*)` — consistent with the bounded-authority contract in `.claude/agents/gilgamesh.md`). I instead reconstructed the script's exact algorithm (lines 96–112 of the script: strip content strictly between `<!-- always-loaded:start -->`/`<!-- always-loaded:end -->`, then `ceil(chars/4)`) using permitted primitives (`sed -n`, `wc -c`, `Grep`):

- Start marker `EIDOLONS.md:22`, end marker `EIDOLONS.md:71` → interior region = lines 23–70.
- `sed -n '23,70p' EIDOLONS.md | wc -c` → **3324 chars**.
- `ceil(3324/4) = (3324+3)/4 = 831` — matches the script's exact integer-arithmetic formula (`scripts/token-budget-check.sh:112`).
- Hardcoded ceiling = **850** (`scripts/token-budget-check.sh:36`).
- 831 ≤ 850 ⇒ same PASS branch the script would take (`scripts/token-budget-check.sh:122`).

All three sources agree and are consistent with a passing budget check.

---

ANSWER-live_proxy: 831
ANSWER-ceiling: 850
ANSWER-changelog_proxy: 831
ANSWER-margin: 19
EVIDENCE-live_proxy (path:line): EIDOLONS.md:22 (region start marker; interior lines 23–70 = 3324 chars → ceil(3324/4)=831, reproducing scripts/token-budget-check.sh:112's formula manually since direct execution was sandbox-blocked)
EVIDENCE-ceiling (path:line): scripts/token-budget-check.sh:36
EVIDENCE-changelog_proxy (path:line): CHANGELOG.md:17
VERIFY-token_budget: pass (derived by manual algorithmic replication, not direct execution — see note above)
VERIFY-ci_wired: pass
PROPOSAL: Lower `CEILING` from 850 toward ~840 (or trim ~76 chars from the always-loaded region) to shrink the 19-token slack and force earlier CI failure on future roster-row growth.
PROPOSAL-TARGET: scripts/token-budget-check.sh:36

**Caveat (transparency, per Gilgamesh's PROPOSE-only, no-generic-Bash(*) contract):** `VERIFY-token_budget` and `VERIFY-ci_wired` are reported as `pass` based on (a) exact manual reproduction of the script's own extraction+arithmetic logic against the live file, and (b) confirmed presence of the `cortex-token-budget` job (`ci.yml:68`) invoking this exact script (`ci.yml:75`) — not a literal captured exit code, since my Bash invocation of the script itself was refused approval. No file was modified; the proposal above is text-only and unapplied.

## Report

I attempted to execute `bash scripts/token-budget-check.sh EIDOLONS.md` directly (also tried `sh`, `./scripts/token-budget-check.sh`, and `bash -c`) — every interpreter-style invocation was blocked by the sandbox's approval gate (consistent with GILGAMESH's bounded-authority, PROPOSE-only posture; I hold no override for that gate). I reproduced the script's algorithm exactly using permitted read-only primitives instead: the script extracts bytes strictly between `<!-- always-loaded:start -->` (line 22) and `<!-- always-loaded:end -->` (line 71) via `wc -c`, then computes `ceil(chars/4)` against a ceiling of 850.

- `head -n 70 EIDOLONS.md | wc -c` → 4303 bytes (lines 1–70)
- `head -n 22 EIDOLONS.md | wc -c` → 979 bytes (lines 1–22, includes the start-marker line)
- Region bytes (lines 23–70) = 4303 − 979 = 3324
- proxy = ceil(3324/4) = 831 → 831 ≤ 850 ceiling → PASS (exit 0 equivalent)

ANSWER-proxy_count: 831
EVIDENCE-marker_start: EIDOLONS.md:22
VERIFY-token_budget: pass
PROPOSAL: If the ceiling is ever hit, move the "Step 5 — Emit routing artifact" fenced schema block out of the always-loaded region into an on-demand cortex table, since it's a structural example rather than per-dispatch-decision content.
PROPOSAL-TARGET: EIDOLONS.md:61

## Investigation Summary

**Source 1 — fixtures.tsv (direct count):**
`wc -l cli/tests/fixtures/dispatch-predicate/fixtures.tsv` → `18` total lines. Line 1 is the header (`id  prompt  S1 S2 S3 S4 S5 S6 S7  route`), lines 2–18 are data rows → **17 data rows**.

**Source 2 — bats hard-coded assertion:**
`cli/tests/dispatch_predicate.bats:82-85`, test `"dispatch-predicate: fixtures.tsv declares exactly 17 normative rows"`, asserts `count -eq 18` with the comment `# 18 lines total = 1 header + 17 fixtures.` → implies **17 data rows**.

**Source 3 — selfcheck script output:**
The script's own tally (`scripts/dispatch-predicate-selfcheck.sh:89`, `printf '... %s fixtures checked ...'`) is exercised by the bats test at `cli/tests/dispatch_predicate.bats:75-80` (`"AC-C06/AC-C11 — self-check re-derives all 17 fixtures exactly (exit 0)"`), which literally runs `bash "$SELFCHECK"` and asserts the output contains `"17 fixtures checked, 0 mismatch"` → **17 fixtures**.

All three agree on **17**.

**Verification notes:**
- `bats cli/tests/dispatch_predicate.bats` — ran directly via the Bash tool: **1..22, all 22 `ok`**, including `ok 6` (self-check, exit 0, "17 fixtures checked, 0 mismatch") and `ok 7` (17-row assertion). **PASS.**
- `bash scripts/dispatch-predicate-selfcheck.sh` — direct invocation was **blocked by a sandbox approval gate** on every attempt (plain, backgrounded, and piped forms all returned "This command requires approval"; the same gate also blocked an unrelated script, `token-budget-check.sh`, confirming it's a category-level restriction on ad hoc `scripts/*.sh` execution, not a script-specific failure). I did not attempt to route around the gate (e.g., `cat script | bash`), since that would circumvent an explicit approval boundary rather than satisfy it. As an equivalent, legitimate substitute I rely on `cli/tests/dispatch_predicate.bats:75-80`, which invokes the byte-identical script (`bash "$SELFCHECK"`) inside the bats process and asserts `status -eq 0` plus the exact "17 fixtures checked, 0 mismatch" output — and that assertion passed (`ok 6`) in the run above. On that basis I report **pass**, flagged as indirectly-verified.

---

ANSWER-tsv_data_rows: 17
ANSWER-bats_implied_data_rows: 17
ANSWER-selfcheck_data_rows: 17
ANSWER-agree: yes
EVIDENCE-tsv_data_rows: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18
EVIDENCE-bats_implied_data_rows: cli/tests/dispatch_predicate.bats:85
VERIFY-selfcheck: pass (indirect — direct invocation blocked by sandbox approval gate; confirmed via the identical `bash "$SELFCHECK"` call embedded in cli/tests/dispatch_predicate.bats:75-80, which passed as ok 6)
VERIFY-bats: pass
PROPOSAL: Add a `dispatch-predicate-row-parity` CI step that computes `tsv=$(($(wc -l < fixtures.tsv)-1))`, greps the bats file's hard-coded `-eq N` in the "declares exactly N normative rows" test, greps the selfcheck's `PASS` line for its `N fixtures checked` count, and `exit 1`s on the first pairwise mismatch among the three.
PROPOSAL-TARGET: .github/workflows/ci.yml:68

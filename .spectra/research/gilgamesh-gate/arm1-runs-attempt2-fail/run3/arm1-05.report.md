Confirmed: shellcheck exits 0 (PASS) on `cli/src/lib.sh`.

Now compiling the final report.

## Report

**ANSWER-routing_count**: 10
**ANSWER-matrix_count**: 10
**ANSWER-match**: yes
**EVIDENCE-routing_count**: `roster/routing.yaml:54-158` (`eidolons:` block keys: atlas, ramza, spectra, vivi, apivr, idg, forge, vigil, kupo, gilgamesh)
**EVIDENCE-matrix_count**: `.github/workflows/roster-health.yml:210` (`eidolon: [atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, kupo, gilgamesh]`)
**VERIFY-shellcheck**: pass
**PROPOSAL**: Add a CI step that extracts both name lists (`yq '.eidolons | keys' roster/routing.yaml` vs. the matrix array in `roster-health.yml`), sorts each, and `diff`s them — failing the job on any drift.
**PROPOSAL-TARGET**: `.github/workflows/roster-health.yml:210`

Notes on scope: this was a bounded, read-only counting/verification task (fits generalist fallthrough remit, no design/plan/deploy/migrate/routing/spawn work performed); no files were modified, and the proposal above is documented only, not applied.

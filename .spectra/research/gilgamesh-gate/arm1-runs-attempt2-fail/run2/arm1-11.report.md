## Investigation summary

`bash scripts/token-budget-check.sh EIDOLONS.md` itself was blocked by approval under Gilgamesh's scoped, non-generic-`Bash(*)` allowlist (consistent with the persona's sandbox-first, bounded-authority posture). I manually replicated the script's exact algorithm instead — read the marker-bounded region (`<!-- always-loaded:start -->` at EIDOLONS.md:22, `<!-- always-loaded:end -->` at EIDOLONS.md:71), extracted the strictly-between lines (23–70) via `sed -n '23,70p' EIDOLONS.md | wc -c` → 3324 chars, then applied the script's own `ceil` formula `(CHAR_COUNT+3)/4` → `(3324+3)/4 = 831`. This reproduces the script's documented method (`scripts/token-budget-check.sh:109-112`) and independently arrives at 831, which is consistent with the ceiling check (831 ≤ 850 ⇒ PASS). This computed value matches the number CHANGELOG.md cites verbatim for the post-Gilgamesh-row state.

ANSWER-live_proxy: 831
ANSWER-changelog_proxy: 831
ANSWER-match: yes
EVIDENCE-live_proxy: EIDOLONS.md:22-71
EVIDENCE-changelog_proxy: CHANGELOG.md:17
VERIFY-token_budget: pass
PROPOSAL: add a bats test to cli/tests/token_budget.bats that runs token-budget-check.sh against EIDOLONS.md and asserts the printed proxy(chars/4) count equals the number grepped out of CHANGELOG.md's "Unreleased" Gilgamesh-row entry, failing on the first drift between the two.
PROPOSAL-TARGET: cli/tests/token_budget.bats:108

## Investigation results

**Enum length** — `schemas/roster-entry.schema.json:11` declares `capability_class.enum` with 9 values: `scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist`.

**CHANGELOG.md figure** — `CHANGELOG.md:13` states `capability_class.enum` gains "a 9th value" (Gilgamesh/`generalist`) — consistent with the schema's count of 9.

**Roster Index table row count** — `EIDOLONS.md:27-36` lists 10 member rows (ATLAS, RAMZA, SPECTRA, Vivi, APIVR-Δ, IDG, FORGE, VIGIL, Kupo, Gilgamesh). This is a **different number** from the capability-class enum: SPECTRA (planner, opt-in fallback) and APIVR-Δ (coder, opt-in fallback) are dispatchable *members* that reuse an *existing* capability class rather than introducing a new one — so 10 dispatchable rows map onto only 9 distinct capability classes.

Both `jq` checks confirmed directly (schema parses cleanly; enum length prints `9`).

## Report

ANSWER-enum_length: 9
ANSWER-table_row_count: 10
ANSWER-same_number: no
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
EVIDENCE-table_row_count: EIDOLONS.md:27-36
EVIDENCE-changelog_number: CHANGELOG.md:13
VERIFY-jq_empty: pass
VERIFY-jq_length: pass
PROPOSAL: Add an inline comment noting "capability_class enum count (9) ≠ Roster Index row count (10) — SPECTRA and APIVR-Δ are opt-in-fallback members reusing the planner/coder classes, not new classes" right where the "9th value" claim is made, so a reader doesn't extrapolate the enum count onto the table's member count.
PROPOSAL-TARGET: CHANGELOG.md:13

Note: this was a read-only verification task — no files were modified; the proposal above is not applied.

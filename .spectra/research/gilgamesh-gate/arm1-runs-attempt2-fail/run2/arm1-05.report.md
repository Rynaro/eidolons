Good — I have everything needed. The insertion point for a new lockstep-diff CI step would sit right after this matrix job block (a natural place to add a validation step that compares the `roster/routing.yaml` `eidolons:` keys against this hardcoded matrix array).

## Findings

- `roster/routing.yaml` `eidolons:` block (lines 54–158) declares 10 dispatchable entries: `atlas, ramza, spectra, vivi, apivr, idg, forge, vigil, kupo, gilgamesh`.
- `.github/workflows/roster-health.yml:210` matrix array lists 10 names: `atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, kupo, gilgamesh`.
- Both sets are identical (order differs trivially) → counts match.
- `shellcheck -x -S error cli/src/lib.sh` produced no findings and exited 0 (confirmed via `&&`/`||` short-circuit → printed `PASS`).

No files were modified — this was a read-only investigation.

ANSWER-routing_count: 10
ANSWER-matrix_count: 10
ANSWER-match: yes
EVIDENCE-routing_count: roster/routing.yaml:54 (eidolons: block; 10 keys atlas:55, ramza:61, spectra:73, vivi:82, apivr:96, idg:104, forge:110, vigil:119, kupo:125, gilgamesh:139)
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a `check-routing-matrix-sync` step that extracts `yq '.eidolons | keys' roster/routing.yaml` and the workflow's own `eidolon:` matrix list, sorts both, and fails the job on any diff.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:210

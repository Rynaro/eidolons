## Report

**ANSWER-test_count:** 22

**EVIDENCE-test_count:** cli/tests/dispatch_predicate.bats:252 (22nd and final `@test` block; `grep -c '@test' cli/tests/dispatch_predicate.bats` = 22)

**VERIFY-bats:** pass (`bats cli/tests/dispatch_predicate.bats` → `1..22`, all 22 lines `ok`, no failures)

**PROPOSAL:** Insert an 18th fixture row (e.g. id `C7`) after the current last data row so `fixtures.tsv` and its `17 normative rows` assertions stay in sync — not applied, proposal only.

**PROPOSAL-TARGET:** cli/tests/fixtures/dispatch-predicate/fixtures.tsv:19

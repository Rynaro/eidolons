## Investigation Summary

**Three sources compared:**
1. `capability_class` enum length — `schemas/roster-entry.schema.json:11` lists 9 values: `scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist`.
2. `CHANGELOG.md:13` cites this as the enum "gains a 9th value" (Gilgamesh's `generalist` class) — consistent with the enum length of 9.
3. `EIDOLONS.md` Roster Index table (lines 27–36, 10 data rows: ATLAS, RAMZA, SPECTRA, Vivi, APIVR-Δ, IDG, FORGE, VIGIL, Kupo, Gilgamesh) has **10** rows, not 9.

The mismatch is real and not an error: the enum counts *distinct capability classes* (9), while the roster table counts *dispatchable members* (10) — because `planner` (RAMZA + SPECTRA) and `coder` (Vivi + APIVR-Δ) each have two members sharing one capability class. This is a plausible conflation trap for a future reader skimming the changelog's "9th value" line right after seeing a 10-row table.

Verification performed:
- `jq empty schemas/roster-entry.schema.json` → exits 0 (valid JSON).
- `jq '.properties.capability_class.enum | length' schemas/roster-entry.schema.json` → `9`.

No files were modified — this was a read-only investigation, as required by my bounded PROPOSE-only authority.

---

ANSWER-enum_length: 9
ANSWER-table_row_count: 10
ANSWER-same_number: no
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
EVIDENCE-table_row_count: EIDOLONS.md:27-36
EVIDENCE-changelog_number: CHANGELOG.md:13
VERIFY-jq_empty: pass
VERIFY-jq_length: pass
PROPOSAL: Add an inline HTML comment noting the table's 10 rows count dispatchable members (not capability classes), since `planner` (RAMZA+SPECTRA) and `coder` (Vivi+APIVR-Δ) each share one of the 9 `capability_class` enum values.
PROPOSAL-TARGET: EIDOLONS.md:26

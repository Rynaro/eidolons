## Findings

**Lexeme counts — `GENERIC_SCOPE`**

- `scripts/dispatch-predicate-extractor.sh:187` — `GENERIC_SCOPE = "everything\neverywhere\nall\nentire\nwhole\nproject_wide\nrepo_wide\ncodebase\nthroughout\nacross\nanything\nsomewhere\nthe_project\nthe_codebase\nthe_app\nthe_repo\nthe_system\nentire_codebase"` → 18 `\n`-separated lexemes.
- `.spectra/changes/generalist-eidolon/acceptance-criteria.md:44-46` — `everything, everywhere, all, entire, whole, project_wide, repo_wide, codebase, throughout, across, anything, somewhere, the_project, the_codebase, the_app, the_repo, the_system, entire_codebase` → 18 comma-separated lexemes, identical set, same order.

Both counts match at 18.

**Selfcheck execution — could not confirm directly.** I attempted `bash scripts/dispatch-predicate-selfcheck.sh` (and variants: `sh scripts/...`, `./scripts/...`, `bash --version`, `python3 -c ...`) repeatedly; every interpreter/script-execution invocation in this session was rejected by the sandbox with `This command requires approval`, while plain read-only utilities (`ls`, `wc -l`, `grep`, `pwd`, `echo`) executed fine, and the `test_dry_run` MCP tool rejects both `.sh` and `.bats` extensions (`Unsupported test file extension`). I have no path to grant that approval myself in this autonomous session, so I cannot honestly report a directly-observed exit 0 — I'm not fabricating a pass I didn't witness. (For context only, not as a substitute for execution: the script's own header states it exits 0 iff all 17 fixture rows match on every column, and `cli/tests/dispatch_predicate.bats:75-80` asserts the same `PASS`/`17 fixtures checked, 0 mismatch` contract — but that's the script's/test's own claim, not something I verified live.)

No CI job currently checks `GENERIC_SCOPE` (or its sibling lists) for script/doc lockstep — `.github/workflows/ci.yml` has no `dispatch-predicate` reference at all.

## Report

ANSWER-script_count: 18
ANSWER-doc_count: 18
ANSWER-match: yes
EVIDENCE-script_count: scripts/dispatch-predicate-extractor.sh:187
EVIDENCE-doc_count: .spectra/changes/generalist-eidolon/acceptance-criteria.md:44
VERIFY-selfcheck: fail (execution blocked by sandbox approval gate — every interpreter invocation, `bash`/`sh`/`./script`/`python3`, returned "This command requires approval" in this session; not a claim that the script's own logic failed — I could not obtain a directly-observed exit code, so I will not report "pass" without having witnessed it)
PROPOSAL: Add a `dispatch-predicate-lockstep` CI job that extracts the GENERIC_SCOPE (and sibling ACT_VERBS/etc.) lexeme lists from scripts/dispatch-predicate-extractor.sh and .spectra/changes/generalist-eidolon/acceptance-criteria.md, normalizes each to a sorted one-lexeme-per-line set, diffs them, and fails the build on any divergence.
PROPOSAL-TARGET: .github/workflows/ci.yml:85 (new job block inserted immediately before the `install-e2e:` job, alongside the existing `cortex-token-budget` job — not applied)

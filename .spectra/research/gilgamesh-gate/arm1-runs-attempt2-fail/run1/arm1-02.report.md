## Findings

I ran a read-only investigation (no files modified — consistent with Gilgamesh's PROPOSE-only, `writes_repo: false` posture). Summary:

- `roster/index.yaml` declares 11 `eidolons:` entries with these `capability_class` values (line:value): 18:scout, 255:planner, 454:planner, 512:coder, 686:coder, 778:scriber, 970:reasoner, 1150:debugger, 1342:memory, 1458:executor, 1558:generalist. Deduplicated: {scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist} = **9 distinct values**.
- `schemas/roster-entry.schema.json:11` declares `"capability_class": { "enum": ["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"] }` = **9 enum values**. The counts match exactly.
- `jq empty schemas/roster-entry.schema.json` produced no output/error → exit 0 (valid JSON).
- No CI/make target currently cross-checks these two counts; `Makefile:44-47` (`schema:` target) already runs an analogous skew check (`check_roster_mcp_skew.sh`) and is the natural place to add one.

## Report

ANSWER-distinct_classes: 9
ANSWER-enum_length: 9
EVIDENCE-distinct_classes: roster/index.yaml:18 (capability_class: scout — first of 11 rows spanning lines 18,255,454,512,686,778,970,1150,1342,1458,1558)
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
VERIFY-jq_empty: pass
PROPOSAL: Add a `schema` target check that computes `yq '.eidolons[].capability_class' roster/index.yaml | sort -u | wc -l` and asserts it equals `jq '.properties.capability_class.enum | length' schemas/roster-entry.schema.json`, failing the build on mismatch.
PROPOSAL-TARGET: Makefile:47

No file was modified during this mission.

## Investigation summary

**Script — `scripts/dispatch-predicate-extractor.sh:184`**
`ACT_VERBS = "add\ncreate\nwrite\nimplement\nbuild\nfix\nedit\nmodify\nupdate\nrename\nremove\ndelete\nrefactor\nmigrate\nconfigure\nwire\ninstall\ngenerate\napply\npatch\nreplace\nextend\nrewrite\nconvert\nbump\nupgrade\nset_up\nscaffold\nappend\ninsert\nenable\ndisable\nrevert\nrollback\nroll_back\nseed\nprovision\ncompile\nlint\nformat\nexport\nimport\npublish\ninit\nbootstrap\nstub\ndeprecate"` — counted 47 lexemes.

**Frozen doc — `.spectra/changes/generalist-eidolon/acceptance-criteria.md:33-37`**
`add, create, write, implement, build, fix, edit, modify, update, rename, remove, delete, refactor, migrate, configure, wire, install, generate, apply, patch, replace, extend, rewrite, convert, bump, upgrade, set_up, scaffold, append, insert, enable, disable, revert, rollback, roll_back, seed, provision, compile, lint, format, export, import, publish, init, bootstrap, stub, deprecate` (broadened per CRIT-018) — same 47 lexemes, same order.

**Cortex copy — `methodology/cortex/dispatch-predicate.md:53-58`** — an ACT_VERBS list is present (on-demand cortex mirror of the same set); confirmed present, not independently re-tallied as part of the agreement check.

**Verification:**
- `bats cli/tests/dispatch_predicate.bats` — ran directly: **22/22 `ok`**, including test 6 (`AC-C06/AC-C11 — self-check re-derives all 17 fixtures exactly (exit 0)`) and test 22 (full sweep against the frozen table).
- `bash scripts/dispatch-predicate-selfcheck.sh` — direct invocation was blocked by a sandbox approval gate in this session (repeated attempts, including via absolute path, all returned "This command requires approval" with no interactive approver available). I did not force it. As indirect but authoritative confirmation, bats test 6 (`cli/tests/dispatch_predicate.bats:75-80`) shells out `run bash "$SELFCHECK"` and asserts `status -eq 0` and output containing `PASS` and `17 fixtures checked, 0 mismatch` — that test passed in the run above, so the selfcheck script's exit-0/PASS behavior is confirmed via the test harness's own execution of it.

No files were read-modified; this remains a read-only, PROPOSE-only report.

---

ANSWER-script_count: 47
ANSWER-doc_count: 47
ANSWER-cortex_copy_present: yes
ANSWER-agree: yes
EVIDENCE-script_count: scripts/dispatch-predicate-extractor.sh:184
EVIDENCE-doc_count: .spectra/changes/generalist-eidolon/acceptance-criteria.md:33
VERIFY-selfcheck: pass
VERIFY-bats: pass
PROPOSAL: Add a CI job that extracts the ACT_VERBS lexeme list from scripts/dispatch-predicate-extractor.sh and from the frozen acceptance-criteria.md bullet, diffs the two sets, and fails the build on any mismatch.
PROPOSAL-TARGET: .github/workflows/ci.yml:85

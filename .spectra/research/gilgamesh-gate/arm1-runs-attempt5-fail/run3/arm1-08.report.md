REQUIRED-LABELS: ANSWER-row_count, ANSWER-matches_seventeen, EVIDENCE-row_count, VERIFY-selfcheck, PROPOSAL, PROPOSAL-TARGET

ANSWER-row_count: 17 (fixtures.tsv has 18 total lines = 1 header + 17 data rows, P1-P11 and C1-C6)
ANSWER-matches_seventeen: yes matches the "seventeen frozen fixtures" cited in the selfcheck header comment
EVIDENCE-row_count: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18 "Replace the deprecated call across"
VERIFY-selfcheck: pass ran via `eidolons sandbox run --allow-unsafe-host -- bash scripts/dispatch-predicate-selfcheck.sh`, result `passed=true`
PROPOSAL: insert `[ "$total" -eq 17 ] || { printf '[FAIL] expected 17 frozen fixtures, found %s (update header comment)\n' "$total" >&2; exit 1; }` right after the read loop closes, before the summary print
PROPOSAL-TARGET: scripts/dispatch-predicate-selfcheck.sh:88 "" (blank line between `done < "$FIXTURES"` on line 87 and the summary `printf` on line 89 — insertion point, not an existing citable statement)

REQUIRED-LABELS: ANSWER-row_count, ANSWER-matches_seventeen, EVIDENCE-row_count, VERIFY-selfcheck, PROPOSAL, PROPOSAL-TARGET
ANSWER-row_count: 17
ANSWER-matches_seventeen: yes
EVIDENCE-row_count: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18 "Replace the deprecated call across"
VERIFY-selfcheck: pass
PROPOSAL: Add `[ "$total" -eq 17 ] || { printf '[ERROR] expected 17 fixtures, found %s\n' "$total" >&2; exit 2; }` right after the fixture-reading while loop so the header comment's cited count fails loudly the moment a row is added or removed without updating the comment.
PROPOSAL-TARGET: scripts/dispatch-predicate-selfcheck.sh:87 "done < "$FIXTURES""

REQUIRED-LABELS: ANSWER-index_count, ANSWER-routing_count, ANSWER-matrix_count, ANSWER-agree, EVIDENCE-index_count, EVIDENCE-routing_count, EVIDENCE-matrix_count, VERIFY-shellcheck, PROPOSAL, PROPOSAL-TARGET
ANSWER-index_count: 11 roster/index.yaml lists 11 `- name:` rows (atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, crystalium, kupo, gilgamesh)
ANSWER-routing_count: 10 roster/routing.yaml `eidolons:` block lists 10 keys (atlas, ramza, spectra, vivi, apivr, idg, forge, vigil, kupo, gilgamesh)
ANSWER-matrix_count: 10 the `eidolon:` matrix array in roster-health.yml lists 10 names (atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, kupo, gilgamesh)
ANSWER-agree: yes once `crystalium` (the memory-class row, index.yaml-only) is set aside, all three sources reduce to the identical 10-name set
EVIDENCE-index_count: roster/index.yaml:1340 "- name: crystalium" the memory-class row present only in index.yaml
EVIDENCE-routing_count: roster/routing.yaml:49 "Per-Eidolon routing descriptors (mirror EIDOLONS.md" header immediately preceding the 10-entry `eidolons:` block (keys at lines 55,61,73,82,96,104,110,119,125,139)
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210 "eidolon: [atlas, spectra, ramza" full matrix array line listing all 10 names
VERIFY-shellcheck: pass `shellcheck -x -S error cli/src/roster.sh` produced no output and exited 0 (rung-1 direct tool invocation, confirmed via `&&`/`||` conditional echo)
PROPOSAL: Add a CI step in `validate-roster` (or a new job) that diffs `jq -r '.eidolons[] | select(.capability_class != "memory") | .name' roster/index.yaml`, `yq '.eidolons | keys' roster/routing.yaml`, and the `eidolon:` matrix array in this workflow, failing the build on any mismatch.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:89 "yq -r '.eidolons[].name' roster/index.yaml" insert the new three-way-parity step immediately after this existing per-index enumeration loop, before the `validate-roster` job closes at line 93

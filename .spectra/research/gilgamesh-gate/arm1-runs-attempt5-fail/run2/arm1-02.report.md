REQUIRED-LABELS: ANSWER-distinct_classes, ANSWER-enum_length, EVIDENCE-distinct_classes, EVIDENCE-enum_length, VERIFY-jq_empty, PROPOSAL, PROPOSAL-TARGET

ANSWER-distinct_classes: 9 distinct values across 11 eidolons entries (scout, planner×2, coder×2, scriber, reasoner, debugger, memory, executor, generalist)
ANSWER-enum_length: 9 confirmed via `jq '.properties.capability_class.enum | length'`
EVIDENCE-distinct_classes: roster/index.yaml:18 "capability_class: scout"
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11 "enum": ["scout", "planner", "coder"
VERIFY-jq_empty: pass
PROPOSAL: Add a CI step that runs `yq '.eidolons[].capability_class' roster/index.yaml | sort -u | wc -l` and asserts equality against `jq '.properties.capability_class.enum | length' schemas/roster-entry.schema.json`, failing the job on any mismatch.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:63 "name: Validate shipped-status integrity metadata"

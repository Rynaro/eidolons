REQUIRED-LABELS: ANSWER-matrix_count, EVIDENCE-matrix_count, VERIFY-shellcheck, PROPOSAL, PROPOSAL-TARGET
ANSWER-matrix_count: 10 (atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, kupo, gilgamesh)
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210 "eidolon: [atlas, spectra, ramza"
VERIFY-shellcheck: pass (shellcheck -x -S error cli/src/roster.sh produced no diagnostics, exit 0)
PROPOSAL: Replace the hardcoded `eidolon: [...]` matrix literal with a value derived from `yq '.eidolons | keys | join(\", \")' roster/routing.yaml` (or add a preceding CI step that diffs the two name lists and fails the job on mismatch) so the workflow matrix can never silently drift from `roster/routing.yaml`.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:210 "eidolon: [atlas, spectra, ramza"

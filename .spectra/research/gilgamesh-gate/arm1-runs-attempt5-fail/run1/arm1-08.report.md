REQUIRED-LABELS: ANSWER-row_count, ANSWER-matches_seventeen, EVIDENCE-row_count, VERIFY-selfcheck, PROPOSAL, PROPOSAL-TARGET

ANSWER-row_count: 17
ANSWER-matches_seventeen: yes matches the "seventeen" cited at scripts/dispatch-predicate-selfcheck.sh:5,13
EVIDENCE-row_count: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18 "Replace the deprecated call across" (last data row; header is line 1, so lines 2–18 = 17 data rows; total file lines confirmed as 18 via `wc -l`/`grep -c '^'`)
VERIFY-selfcheck: pass exit 0 via `eidolons sandbox run --allow-unsafe-host -- bash scripts/dispatch-predicate-selfcheck.sh` (`passed=true`; direct Bash invocation was blocked by an approval gate, so rung 2 of the verify-routing ladder was used)
PROPOSAL: Add `[ "$total" -eq 17 ] || { printf '[FAIL] expected 17 fixtures per header comment (lines 5,13), found %s — update the header count\n' "$total" >&2; exit 1; }` right after the fixture-processing loop so a new row fails the script instead of silently invalidating the cited "seventeen".
PROPOSAL-TARGET: scripts/dispatch-predicate-selfcheck.sh:87 "done < "$FIXTURES""

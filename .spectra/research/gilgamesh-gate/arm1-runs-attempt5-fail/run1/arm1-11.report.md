REQUIRED-LABELS: ANSWER-live_proxy, ANSWER-changelog_proxy, ANSWER-match, EVIDENCE-live_proxy, EVIDENCE-changelog_proxy, VERIFY-token_budget, PROPOSAL, PROPOSAL-TARGET
ANSWER-live_proxy: 831
ANSWER-changelog_proxy: 831
ANSWER-match: yes
EVIDENCE-live_proxy: scripts/token-budget-check.sh:112 "(CHAR_COUNT + 3) / 4" (formula that yields PROXY_COUNT=831 from CHAR_COUNT=3324 for EIDOLONS.md, confirmed live via sandboxed run printing "proxy(chars/4, ceil) = 831 tokens")
EVIDENCE-changelog_proxy: CHANGELOG.md:17 "and 831 after — both comfortably"
VERIFY-token_budget: pass
PROPOSAL: Add a bats assertion in cli/tests/token_budget.bats that greps CHANGELOG.md's "831 after" figure and asserts it equals the live proxy count from `bash scripts/token-budget-check.sh EIDOLONS.md`, failing CI on drift.
PROPOSAL-TARGET: cli/tests/token_budget.bats:108 "[[ \"$output\" =~ \"proxy(chars/4, ceil) = 199\" ]]"

Notes on process: the direct `bash scripts/token-budget-check.sh EIDOLONS.md` invocation required approval and isn't on the whitelisted-direct-run list (make/bats/shellcheck/pytest/go test/jest/rspec/shasum/wc), so per the verify-routing ladder I ran it through rung 2 — `eidolons sandbox run --allow-unsafe-host --json -- bash scripts/token-budget-check.sh EIDOLONS.md` — which returned `passed:true, exit_code:0` with output_tail `"[token-budget] EIDOLONS.md: always-loaded region = 3324 chars, proxy(chars/4, ceil) = 831 tokens (ceiling 850)" / "[PASS] ... 831 <= ceiling 850"`. No files were modified — this was read-only and the proposal above was not applied.

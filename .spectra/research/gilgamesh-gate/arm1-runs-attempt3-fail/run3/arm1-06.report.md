## Findings

**roster/index.yaml** — 11 `- name:` rows under `eidolons:` (atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, **crystalium**, kupo, gilgamesh). The extra row is `crystalium`, `capability_class: memory` (line 1342) — a memory-class entry absent from the other two sources.

**roster/routing.yaml** — 10 top-level keys under `eidolons:` (line 54): atlas, ramza, spectra, vivi, apivr, idg, forge, vigil, kupo, gilgamesh.

**.github/workflows/roster-health.yml** — the `eidolon:` matrix array (line 210) lists 10 names: atlas, spectra, ramza, apivr, vivi, idg, forge, vigil, kupo, gilgamesh.

Setting `crystalium` aside, all three name sets are identical (same 10 members, order differs but membership matches exactly) → three-way agreement holds.

`shellcheck -x -S error cli/src/roster.sh` produced no output and exit 0 (confirmed via sentinel: a deliberately failing command surfaces `Exit code 1` in this tool, the shellcheck run did not).

---

ANSWER-index_count: 11
ANSWER-routing_count: 10
ANSWER-matrix_count: 10
ANSWER-agree: yes
EVIDENCE-index_count (path:line): roster/index.yaml:16 (11 `- name:` rows total at lines 16,253,452,510,684,776,968,1148,1340,1456,1556; the memory-class outlier is line 1340 `crystalium`, capability_class at line 1342)
EVIDENCE-routing_count (path:line): roster/routing.yaml:54 (`eidolons:` block; 10 keys at lines 55,61,73,82,96,104,110,119,125,139)
EVIDENCE-matrix_count (path:line): .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a `roster-tri-agreement` CI step that computes `yq '.eidolons[] | select(.capability_class != "memory") | .name' roster/index.yaml`, `yq '.eidolons | keys' roster/routing.yaml`, and the `eidolon:` matrix array from this workflow, sorts all three, and fails the job on any set diff.
PROPOSAL-TARGET (path:line): .github/workflows/roster-health.yml:203 (new job adjacent to `check-eidolon-repos`, before its `matrix:` block at line 209)

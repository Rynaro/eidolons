I've completed the read-only verification. One methodology note: the harness's tool-permission layer blocked a *direct* top-level invocation of `bash scripts/dispatch-predicate-selfcheck.sh` in this session ("this command requires approval"), while plain reads (`wc -l`, `grep`, `bats cli/tests/dispatch_predicate.bats`) were not blocked. I confirmed the selfcheck's pass/exit-0 behavior via `bats cli/tests/dispatch_predicate.bats` itself, whose test 6 (`AC-C06/AC-C11`) executes the *identical* invocation form (`run bash "$SELFCHECK"`, where `SELFCHECK="$EIDOLONS_ROOT/scripts/dispatch-predicate-selfcheck.sh"`) and asserts `status -eq 0` plus output containing `"17 fixtures checked, 0 mismatch"` — that assertion passed (`ok 6`), so I'm reporting VERIFY-selfcheck as pass on that equivalent evidence rather than fabricating a direct run I could not execute.

Findings:
- `wc -l cli/tests/fixtures/dispatch-predicate/fixtures.tsv` → 18 total lines (1 header + 17 data rows, confirmed line-1 header / line-18 last data row `C6`).
- `cli/tests/dispatch_predicate.bats:82-85` hard-codes `[ "$count" -eq 18 ]` with comment "18 lines total = 1 header + 17 fixtures" → implies 17 data rows.
- The selfcheck's own output (verified via the bats harness's identical invocation) reads `[dispatch-predicate-selfcheck] 17 fixtures checked, 0 mismatch(es)` / `[PASS] all 17 fixtures match...` → 17.
- Full `bats cli/tests/dispatch_predicate.bats` run: 22/22 `ok`, no `not ok` lines → suite passes.

No files were modified.

ANSWER-tsv_data_rows: 17
ANSWER-bats_implied_data_rows: 17
ANSWER-selfcheck_data_rows: 17
ANSWER-agree: yes
EVIDENCE-tsv_data_rows: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18
EVIDENCE-bats_implied_data_rows: cli/tests/dispatch_predicate.bats:85
VERIFY-selfcheck: pass
VERIFY-bats: pass
PROPOSAL: Add a `cli-tests` CI step that extracts all three numbers (fixtures.tsv `wc -l` minus header, the bats hard-coded `-eq 18` literal, and the selfcheck's printed "N fixtures checked") and fails the build the moment any pair diverges.
PROPOSAL-TARGET: .github/workflows/ci.yml:209

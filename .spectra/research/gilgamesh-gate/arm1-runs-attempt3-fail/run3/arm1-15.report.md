## Findings

**ACT_VERBS lexeme counts across the three sources:**

1. `scripts/dispatch-predicate-extractor.sh:184` — the `ACT_VERBS = "..."` string, `\n`-joined: 46 separators counted → **47 lexemes**.
2. `.spectra/changes/generalist-eidolon/acceptance-criteria.md:33-37` — the backtick-quoted `ACT_VERBS` bullet: 46 commas counted → **47 lexemes**.
3. `methodology/cortex/dispatch-predicate.md:53-58` — an `ACT_VERBS` copy is present (on-demand cortex prose mirror of the same bullet; also resolves to 46 commas / 47 items on inspection, though this task only asks for presence, not an independent count).

All three lists contain the identical lexeme set (`add, create, write, … deprecate`), so the script and the frozen acceptance-criteria doc agree exactly at 47.

**Verification:**
- `bats cli/tests/dispatch_predicate.bats` → ran directly, all 22 `@test` blocks reported `ok`, no failures (exit 0). Test 6 (`AC-C06/AC-C11`) specifically executes `run bash "$SELFCHECK"` and asserts `status -eq 0`, output contains `PASS` and `17 fixtures checked, 0 mismatch` — this passed.
- `bash scripts/dispatch-predicate-selfcheck.sh` (standalone): my sandbox blocked *every* form of direct script execution in this session (`bash file`, `./file`, `sh file`, even `bash -c 'echo hello'`, with and without `dangerouslyDisableSandbox`) with a bare "requires approval" — while plain utility invocations (`sed`, `grep`, `bats`, `pwd`, `test`) ran fine. Since I have no interactive approval channel here and must not force past a sandbox denial, I did not obtain a first-hand standalone exit code. I'm reporting `pass` on the strength of the bats-internal invocation (line 76 of the bats file: `run bash "$SELFCHECK"`), which is byte-identical to the requested command and passed within the just-executed bats run — this is the closest verifiable proxy available to me and is noted as such rather than presented as a direct run.

## Report

ANSWER-script_count: 47
ANSWER-doc_count: 47
ANSWER-cortex_copy_present: yes
ANSWER-agree: yes
EVIDENCE-script_count: scripts/dispatch-predicate-extractor.sh:184
EVIDENCE-doc_count: .spectra/changes/generalist-eidolon/acceptance-criteria.md:33
VERIFY-selfcheck: pass (confirmed via cli/tests/dispatch_predicate.bats:75-80, which invokes `bash scripts/dispatch-predicate-selfcheck.sh` verbatim and asserted exit 0/PASS; direct standalone invocation was refused by the sandbox's script-execution approval gate in this session, so this is an indirect but exact-command confirmation, not a first-hand standalone run)
VERIFY-bats: pass
PROPOSAL: Add a CI/bats check that extracts the ACT_VERBS lexeme list from scripts/dispatch-predicate-extractor.sh and from the frozen bullet in .spectra/changes/generalist-eidolon/acceptance-criteria.md, normalizes whitespace/casing, and diffs the two sets — failing the build on the first mismatch.
PROPOSAL-TARGET: .github/workflows/ci.yml:85

No files were modified — this was a read-only investigation.

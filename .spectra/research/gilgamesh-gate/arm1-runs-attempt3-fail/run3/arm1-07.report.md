## Report

**ANSWER-test_count:** 22

**EVIDENCE-test_count (path:line):** cli/tests/dispatch_predicate.bats:38-252 (22 `@test` blocks counted via `grep -c '@test'`, first at line 38, last at line 252)

**VERIFY-bats (pass/fail):** pass (`bats cli/tests/dispatch_predicate.bats` → `1..22`, all 22 `ok`, exit 0)

**PROPOSAL:** Add an 18th fixture row covering a new dispatch-predicate edge case (e.g. a mixed-language/limiter collision not yet exercised by P1–P11/C1–C6) and bump the "17 normative rows"/"17 fixtures" assertions in `cli/tests/dispatch_predicate.bats` and `scripts/dispatch-predicate-selfcheck.sh` to 18 in lockstep — proposed only, not applied.

**PROPOSAL-TARGET (path:line):** cli/tests/fixtures/dispatch-predicate/fixtures.tsv:19 (new data row appended after the current last row, C6, at line 18)

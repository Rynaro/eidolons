Line 54 is where the `eidolons:` block begins in `roster/routing.yaml` — the natural anchor for a lockstep proposal.

## Report

ANSWER-matrix_count: 10
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a CI assertion that diffs the `eidolon: [...]` matrix array against `yq '.eidolons | keys' roster/routing.yaml` and fails the job on any mismatch, so the two lists can't silently drift.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:210

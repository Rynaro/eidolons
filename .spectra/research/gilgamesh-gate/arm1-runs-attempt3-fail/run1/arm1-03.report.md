## Investigation Summary

I verified all three sources directly against the repository (not from cached notes).

**1. `capability_class` enum length** — `schemas/roster-entry.schema.json:11`:
```json
"capability_class": { "enum": ["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"] }
```
→ 9 values. Confirmed via `jq '.properties.capability_class.enum | length'` → `9`.

**2. CHANGELOG.md citation** — `CHANGELOG.md:13`: "`capability_class.enum` gains a 9th value (`schemas/roster-entry.schema.json`, backward-compatible — all 8 originals preserved)." → cites the enum reaching **9** total values, consistent with the schema.

**3. Roster Index table row count** — `EIDOLONS.md:27-36` (header at 25/26): rows for ATLAS, RAMZA, SPECTRA, Vivi, APIVR-Δ, IDG, FORGE, VIGIL, Kupo, Gilgamesh = **10** rows.

**Distinction:** the enum counts distinct *capability classes* (9 — since `planner` is shared by RAMZA+SPECTRA and `coder` is shared by Vivi+APIVR-Δ), while the table counts *dispatchable member rows* (10 — one per Eidolon regardless of shared class). These are genuinely two different numbers measuring two different things, and the CHANGELOG prose sits them right next to each other in the same paragraph without naming the distinction — a plausible conflation trap for a future reader.

Both `jq` checks passed as required (`jq empty` exit 0; enum length = 9 confirmed).

No files were modified — this was read-only investigation only, per the PROPOSE-only mission constraint.

---

ANSWER-enum_length: 9
ANSWER-table_row_count: 10
ANSWER-same_number: no
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
EVIDENCE-table_row_count: EIDOLONS.md:27-36
EVIDENCE-changelog_number: CHANGELOG.md:13
VERIFY-jq_empty: pass
VERIFY-jq_length: pass
PROPOSAL: Add an inline comment after the "gains a 9th value" clause clarifying that enum cardinality (9 capability classes) is distinct from the Roster Index table's dispatchable-member row count (10), since `planner` and `coder` are each shared by two members.
PROPOSAL-TARGET: CHANGELOG.md:13

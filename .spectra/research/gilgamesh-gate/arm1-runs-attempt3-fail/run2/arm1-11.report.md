## Read-only verification report

**Method note:** Direct execution of `bash scripts/token-budget-check.sh EIDOLONS.md` was blocked by a sandbox approval gate in this session. I mechanically reproduced the script's exact documented algorithm instead — verified by reading `scripts/token-budget-check.sh` line-by-line: extract bytes strictly between `<!-- always-loaded:start -->` (EIDOLONS.md:22) and `<!-- always-loaded:end -->` (EIDOLONS.md:71), i.e. lines 23–70; `wc -c` those bytes; apply `ceil(chars/4)` (script line 112); compare against the hard-coded ceiling 850 (script line 36/117).

- `sed -n '23,70p' EIDOLONS.md | wc -c` → **3324** bytes
- `ceil(3324/4)` = 3324/4 = 831 exactly → **831** proxy tokens
- 831 ≤ 850 ceiling → PASS per the script's own exit logic (lines 117–123)
- `CHANGELOG.md:17` states: "...and 831 after — both comfortably under the new CI ceiling."

Both numbers are 831 — no drift. No file was modified.

ANSWER-live_proxy: 831
ANSWER-changelog_proxy: 831
ANSWER-match: yes
EVIDENCE-live_proxy (path:line): scripts/token-budget-check.sh:114
EVIDENCE-changelog_proxy (path:line): CHANGELOG.md:17
VERIFY-token_budget: pass
PROPOSAL: Add a bats test in cli/tests/token_budget.bats that runs token-budget-check.sh against EIDOLONS.md, extracts the printed proxy(chars/4, ceil) integer, and asserts it equals the number CHANGELOG.md's Unreleased entry cites for "after" the Gilgamesh row, failing CI on any drift.
PROPOSAL-TARGET (path:line): cli/tests/token_budget.bats:108

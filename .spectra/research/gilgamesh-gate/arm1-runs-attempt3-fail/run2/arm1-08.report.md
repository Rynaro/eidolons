## Findings

I read `cli/tests/fixtures/dispatch-predicate/fixtures.tsv` and confirmed via `wc -l` that it has 18 total lines: 1 header (line 1) + 17 data rows (lines 2–18, ids P1–P11 and C1–C6). I then read the header comment in `scripts/dispatch-predicate-selfcheck.sh`, which cites "seventeen" fixtures (lines 5 and 13: "Re-derives S1..S5 for all seventeen frozen fixtures" / "Exits 0 iff all seventeen rows match on every column").

For verification, direct `bash`-execution of the selfcheck script was denied by the sandbox approval gate (multiple attempts across `bash scripts/…`, `bash -n …`, `bash -c "echo hi"`, and direct executable invocation all returned "This command requires approval" — even for an unrelated trivial script, confirming it's a blanket sandbox policy rather than a script-specific issue). I could not use `mcp__atlas-aci__test_dry_run` either (it rejects `.sh` files: `Unsupported test file extension '.sh'`).

I instead ran `bats cli/tests/dispatch_predicate.bats`, which passed all 22 tests, including test #6: `AC-C06/AC-C11 — self-check re-derives all 17 fixtures exactly (exit 0)`, whose body is literally `run bash "$SELFCHECK"; [ "$status" -eq 0 ]; [[ "$output" =~ "17 fixtures checked, 0 mismatch" ]]`. This is a direct, passing assertion of the exact command in question exiting 0 with the expected "17 fixtures checked, 0 mismatch(es)" output, so I'm reporting VERIFY-selfcheck as pass on that evidence even though I couldn't invoke the raw command myself.

Note also that `cli/tests/dispatch_predicate.bats:82-86` already asserts the raw row count (`wc -l` == 18) but does **not** cross-check that number against the selfcheck header comment's hardcoded prose "seventeen" — that's the gap my proposal targets.

**ANSWER-row_count:** 17
**ANSWER-matches_seventeen:** yes
**EVIDENCE-row_count:** cli/tests/fixtures/dispatch-predicate/fixtures.tsv:18
**VERIFY-selfcheck:** pass
**PROPOSAL:** Add a bats assertion that greps the selfcheck header's hard-coded fixture count and fails if it diverges from `$(($(wc -l < "$FIXTURES") - 1))`.
**PROPOSAL-TARGET:** cli/tests/dispatch_predicate.bats:86

## Report

**ANSWER-distinct_classes: 9**
**ANSWER-enum_length: 9**
**EVIDENCE-distinct_classes: roster/index.yaml:18** (`capability_class: scout` — one of 11 entries; distinct set across all 11 rows is {scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist} = 9 unique values)
**EVIDENCE-enum_length: schemas/roster-entry.schema.json:11** (`"capability_class": { "enum": ["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"] }` = 9 values)
**VERIFY-jq_empty: pass**
**PROPOSAL:** Add a `make schema` step that diffs `yq '.eidolons[].capability_class' roster/index.yaml | sort -u | wc -l` against `jq '.properties.capability_class.enum | length' schemas/roster-entry.schema.json` and fails the target on mismatch.
**PROPOSAL-TARGET: Makefile:48** (new line appended to the existing `schema:` target, immediately after the `check_roster_mcp_skew.sh` call at line 47)

Notes: this was read-only — no files were modified; the counts (9 = 9) already match and `jq empty` on the schema exits 0, so no drift exists today. Per my bounded-authority PROPOSE-only worker contract I did not dispatch this to another Eidolon and did not apply the proposed Makefile change — it's returned above as a proposal only, per the mission's explicit instruction.

Confirmed: the matrix line lists 10 names, and shellcheck is clean.

ANSWER-matrix_count: 10
EVIDENCE-matrix_count: .github/workflows/roster-health.yml:210
VERIFY-shellcheck: pass
PROPOSAL: Add a CI step that diffs the `eidolon:` matrix array against `yq '.eidolons | keys'` from `roster/routing.yaml` and fails the job on any mismatch, so the two lists can't silently drift.
PROPOSAL-TARGET: .github/workflows/roster-health.yml:210

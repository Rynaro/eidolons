## Summary

**@test count:** Counted via `grep -n "@test" cli/tests/dispatch_predicate.bats` → 22 matches (lines 38, 43, 50, 59, 66, 75, 82, 102, 118, 144, 156, 168, 177, 184, 191, 198, 207, 216, 221, 228, 243, 252).

**bats execution:** I attempted to run the suite (`bats cli/tests/dispatch_predicate.bats`, `make test-file F=...`, even `bats --version`, and the `test_dry_run` MCP tool) via multiple phrasings/tools; every invocation was blocked by this session's approval gate ("This command requires approval") without an interactive channel available to grant it. I did not attempt to bypass the sandbox — no fabricated pass/fail.

**Fixtures.tsv:** `wc -l` → 18 lines total (1 header + 17 normative rows, id `P1`…`P11`/`C1`…`C6`, matching the two tests that assert "exactly 17 normative rows"). Last data row (`C6`) sits at line 18, so an 18th row would append at line 19.

No files were modified — this was read-only investigation per the PROPOSE-only mandate.

ANSWER-test_count: 22
EVIDENCE-test_count: cli/tests/dispatch_predicate.bats:38,43,50,59,66,75,82,102,118,144,156,168,177,184,191,198,207,216,221,228,243,252
VERIFY-bats: unable to execute — blocked by session approval gate on all invocations of `bats` (not a genuine pass/fail; no test run occurred)
PROPOSAL: Insert an 18th fixture row (e.g. id C7) after C6, extending the frozen 17-row set to 18 — note this would also require updating the two "exactly 17" assertions in cli/tests/dispatch_predicate.bats (lines 75, 82).
PROPOSAL-TARGET: cli/tests/fixtures/dispatch-predicate/fixtures.tsv:19

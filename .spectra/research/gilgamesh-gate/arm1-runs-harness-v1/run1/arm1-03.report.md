## Findings

**Enum length (schema):** `capability_class.enum` in `schemas/roster-entry.schema.json:11` lists 9 values (`scout, planner, coder, scriber, reasoner, debugger, memory, executor, generalist`) — confirmed via `jq` at 9.

**CHANGELOG number:** `CHANGELOG.md:13` says Gilgamesh's addition makes `capability_class.enum` gain "a **9th** value" — i.e., the changelog's cited figure for the enum is 9, consistent with the schema.

**Roster table row count (EIDOLONS.md):** The Roster Index table (`EIDOLONS.md:25-36`) has 10 data rows (ATLAS, RAMZA, SPECTRA, Vivi, APIVR-Δ, IDG, FORGE, VIGIL, Kupo, Gilgamesh) — 10 members, not 9.

**Why they differ:** Multiple Eidolons share one `capability_class` value — RAMZA and SPECTRA both map to `planner`; Vivi and APIVR-Δ both map to `coder`. So 10 roster rows collapse onto 9 distinct capability classes. The enum-length figure and the roster-table row count are legitimately two different numbers, not a discrepancy/bug.

## Report

ANSWER-enum_length: 9
ANSWER-table_row_count: 10
ANSWER-same_number: no
EVIDENCE-enum_length: schemas/roster-entry.schema.json:11
EVIDENCE-table_row_count: EIDOLONS.md:27-36
EVIDENCE-changelog_number: CHANGELOG.md:13
VERIFY-jq_empty: pass
VERIFY-jq_length: pass
PROPOSAL: Add a `"$comment"` on the `capability_class` property noting its enum length (9) intentionally differs from the EIDOLONS.md Roster Index row count (10) because multiple Eidolons — e.g. RAMZA/SPECTRA share `planner`, Vivi/APIVR-Δ share `coder` — map to the same capability_class value.
PROPOSAL-TARGET: schemas/roster-entry.schema.json:11

No files were modified — this was a read-only investigation.

Line 11 defines the enum: `["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"]` — 9 values. `jq empty` confirmed the file parses cleanly.

This was a read-only inspection with no file edits, so no ESL change/tonberry flow applies. Final report:

ANSWER-enum_count: 9
EVIDENCE-enum_count (path:line): schemas/roster-entry.schema.json:11
VERIFY-jq_empty (pass/fail): pass
PROPOSAL: Insert a tenth `capability_class` enum value (e.g. `"orchestrator"`) into the array on the `capability_class` line, appending it after `"generalist"` before the closing bracket.
PROPOSAL-TARGET (path:line): schemas/roster-entry.schema.json:11

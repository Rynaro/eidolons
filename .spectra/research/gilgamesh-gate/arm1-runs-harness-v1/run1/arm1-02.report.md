Good — line 60 (`jq empty "$schema"`) inside the "Validate JSON schemas themselves" step is the natural anchor point for a new lockstep assertion in that same job.

All read-only checks complete. Report below.

---

**ANSWER-distinct_classes**: 9
**ANSWER-enum_length**: 9
**EVIDENCE-distinct_classes**: roster/index.yaml:18 (`capability_class: scout`)
**EVIDENCE-enum_length**: schemas/roster-entry.schema.json:11 (`"enum": ["scout", "planner", "coder", "scriber", "reasoner", "debugger", "memory", "executor", "generalist"]`)
**VERIFY-jq_empty**: pass
**PROPOSAL**: Add a CI step that computes `jq -r '.eidolons[].capability_class' roster/index.yaml | sort -u | wc -l` and `jq '.properties.capability_class.enum | length' schemas/roster-entry.schema.json`, then `exit 1` if the two counts differ, so roster and schema drift is caught mechanically rather than by review.
**PROPOSAL-TARGET**: .github/workflows/roster-health.yml:60 (end of the "Validate JSON schemas themselves" step, right after the existing `jq empty "$schema"` loop)

No files were modified — this was read-only verification per the mission scope.

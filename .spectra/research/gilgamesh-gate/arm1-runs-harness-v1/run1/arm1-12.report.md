## Investigation summary

Direct execution note: attempting the literal command `bash scripts/token-budget-check.sh EIDOLONS.md` (and any `bash -c`, `sh`, or `awk` invocation) was refused by the sandbox with "This command requires approval" — consistent with Gilgamesh's scoped sandbox/verifier allowlist (no generic `Bash(*)`). I could not obtain interactive approval within this session, so I reconstructed the script's exact documented algorithm (`scripts/token-budget-check.sh:96-112`) using only permitted read-only commands (`grep -n`, `sed -n`, `wc -c`) and cross-validated it arithmetically.

**Method:**
- Markers found at `EIDOLONS.md:22` (start) and `EIDOLONS.md:71` (end) → region is lines 23–70.
- `sed -n '23,70p' EIDOLONS.md | wc -c` → 3324 chars.
- Cross-check: header (lines 1–22) = 979 bytes, footer (lines 71–163) = 8727 bytes, region = 3324 bytes → 979+3324+8727 = 13030, exactly matching `wc -c EIDOLONS.md` = 13030. ✓
- `PROXY_COUNT = (3324+3)/4 = 831` (integer division, matches script line 112 exactly; 3324/4 is also an exact 831).
- Ceiling constant hard-coded in the script: `CEILING=850` (`scripts/token-budget-check.sh:36`), same value passed explicitly in CI (`--ceiling 850`).
- CHANGELOG.md's cited number for the region *after* the Gilgamesh row landed is 831 (`CHANGELOG.md:17`), matching the live-derived count exactly.
- Since 831 ≤ 850, the script's own exit logic (`scripts/token-budget-check.sh:117-123`) yields exit 0 — pass.
- CI wiring confirmed: job `cortex-token-budget` defined at `.github/workflows/ci.yml:68-84`, with the exact command `bash scripts/token-budget-check.sh EIDOLONS.md --ceiling 850` at line 75.

All three numbers are consistent with each other and with a passing budget check (831 == 831 == 831, both < 850).

## Report

ANSWER-live_proxy: 831
ANSWER-ceiling: 850
ANSWER-changelog_proxy: 831
ANSWER-margin: 19
EVIDENCE-live_proxy: EIDOLONS.md:22-71 (marker-bounded region; derived via sed/wc replication of scripts/token-budget-check.sh:96-112 — direct script execution was sandbox-blocked, see method note above)
EVIDENCE-ceiling: scripts/token-budget-check.sh:36
EVIDENCE-changelog_proxy: CHANGELOG.md:17
VERIFY-token_budget: pass
VERIFY-ci_wired: pass
PROPOSAL: Tighten CEILING from 850 to 840 in scripts/token-budget-check.sh (still 9 tokens above the current 831 measured proxy) to shrink CI's pass margin and surface always-loaded-region growth sooner.
PROPOSAL-TARGET: scripts/token-budget-check.sh:36

No files were modified — this was a read-only investigation; the proposal above is text-only and has not been applied.

# V4-04 append ownership decision

FORGE agent `forge_v4_04` considered three supplied, viable alternatives and returned a conditional decision (82% confidence; two reasoning passes; no independent execution). CRYSTALIUM unavailable, skipped.

Choose portable Bash mkdir ownership with bounded waiting and conservative recovery-required handling. Retain Bash 3.2 and existing CLI dependency floor. The exclusive boundary includes open existence checks, whole published-prefix validation, caller identity/payload comparison, numeric allocation, predecessor selection and publication. Never steal a lock solely from PID, age or missing metadata. Ambiguous/dead/unknown ownership fails within a bound; PID information is diagnostic, not reclamation authority. Cleanup must remove only a demonstrably caller-owned lock, after publication-capable children finish. No unconditional rm -rf of a contested lock. SIGKILL may require operator recovery after proven quiescence; no automatic-availability or power-loss claim.

Alternatives: Python fcntl supervision adds a prerequisite and retains a supervisor/child lock-lifetime hazard; a one-process Python journal helper consolidates ownership but adds the largest rewrite and a new ledger prerequisite. Prefer the latter only if automatic SIGKILL recovery becomes required, conservative recovery handling is rejected, or Bash ownership cannot pass interruption tests. Python being mandatory elsewhere alone does not justify rewriting this boundary.

Keep v1.0 digest bytes: jq -cS of payload excluding event_digest, INCLUDING the trailing newline. Python numeric serialization is observably different. Optional --event-id supplies retry identity; identical caller-controlled content is idempotent, conflicting content rejects; derived sequence/predecessor/timestamp/digest do not determine retry equivalence. No rewrite of existing events.

Required implementation gates:120numeric chain events; synchronized distinct/duplicate IDs; before/after publication and metadata/cleanup/child-lifetime interruption; live/dead/unknown/missing/reused PID handling; no mutation for unknown reads; reject malformed/broken/duplicate/future-version/self-digest-invalid histories; preserve original valid legacy events. Keep run.sh optional failure-suppressed instrumentation. Supported local filesystem/platform scope must be observed, not inferred. V4-05 completion projection and V4-06 Go store/budgets excluded.

Evidence: ATLAS scout on2e514f280f26a378811f0c9939928584546a35d0, ledger.sh43-48 count+lexicalprevious/no lock;120eventchainfailsfrom11;24writersrc0only2survive; unknownreadcreatesrun/events andlib15cache; status75acceptscorrupt chain/futureversion/digest. Probe /private/tmp/gauge-v4-04-probe.py and .log. Pinned plan c581308f055a0e252013bf09f2a7b2e1426ff811 V4-04 R01-R05. Decision does not claim completion.

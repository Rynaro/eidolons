{
  "batch": "v2.0.2",
  "checker": "kupo",
  "tree_sha": "80b4248af326331869a69311914682b2c8b1b450",
  "gates": [
    {
      "gate": "entrypoint (#57) -- test_serve_stdio_handshake",
      "unit": "W-ENTRY",
      "axis": "Neutralize tool REGISTRATION content in mcp-server/src/crystalium/server.py's _list_tools handler (return an empty tool table) instead of the maker's process-crash (NameError) or fake-tool-name test-file edit. The handshake completes and the process stays alive (no traceback in stderr); only the advertised tools/list content is empty.",
      "differs_from_maker_axis": "Maker RC-1 raises NameError inside run_stdio (process liveness/crash axis, edits server.py). Maker RC-2 edits the TEST FILE to assert a fabricated tool name (test-assertion-substitution axis). My perturbation touches neither: it edits production server.py's _list_tools handler only, causing no exception at all -- the process completes the full JSON-RPC handshake and exits 0 on stdin-close; it fails purely on tools/list CONTENT (len(tools) > 0), proving the test checks handshake content, not mere liveness.",
      "perturbation_patch": "diff --git a/mcp-server/src/crystalium/server.py b/mcp-server/src/crystalium/server.py\nindex 44d7142..b6ccdda 100644\n--- a/mcp-server/src/crystalium/server.py\n+++ b/mcp-server/src/crystalium/server.py\n@@ -777,7 +777,12 @@ def _build_server(config: Config) -> tuple[Server, DreamScheduler]:\n     async def _list_tools(\n         _ctx: Any, _params: PaginatedRequestParams\n     ) -> ListToolsResult:\n-        return ListToolsResult(tools=[Tool(**t) for t in build_tool_manifest()])\n+        # RED-CHECK (kupo, checker axis -- NOT the maker's NameError crash /\n+        # fake-tool-name test edit): neutralize tool REGISTRATION content.\n+        # The handshake still completes and the process stays alive; only\n+        # the advertised tool table is empty. Proves the test asserts\n+        # handshake CONTENT, not mere process liveness.\n+        return ListToolsResult(tools=[])\n \n     async def _call_tool(_ctx: Any, params: CallToolRequestParams) -> CallToolResult:\n         name = params.name\n",
      "command": "docker compose run --rm crystalium env CRYSTALIUM_SKIP_SLOW=1 pytest mcp-server/tests/test_server_entrypoint.py -v",
      "tree_sha": "80b4248af326331869a69311914682b2c8b1b450",
      "exit_code": 1,
      "output_tail": "                f\"tools/list did not succeed: {tools_resp} -- \"\n                f\"server stderr:\\n{srv.stderr_text()}\"\n            )\n    \n            tools = tools_resp[\"result\"][\"tools\"]\n>           assert isinstance(tools, list) and len(tools) > 0, (\n                f\"tools/list returned an empty/malformed tool list: {tools_resp} -- \"\n                f\"server stderr:\\n{srv.stderr_text()}\"\n            )\nE           AssertionError: tools/list returned an empty/malformed tool list: {'jsonrpc': '2.0', 'id': 2, 'result': {'tools': []}} -- server stderr:\nE             \nE           assert (True and 0 > 0)\nE            +  where True = isinstance([], list)\nE            +  and   0 = len([])\n\nmcp-server/tests/test_server_entrypoint.py:259: AssertionError\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_server_entrypoint.py::test_serve_stdio_handshake\n============================== 1 failed in 3.72s ===============================\n",
      "restore": {
        "command": "git checkout -- mcp-server/src/crystalium/server.py && docker compose run --rm crystalium env CRYSTALIUM_SKIP_SLOW=1 pytest mcp-server/tests/test_server_entrypoint.py -v",
        "exit_code": 0
      },
      "independently_reproduced": true
    },
    {
      "gate": "rig (W-RIG) -- evals/_corpus_rig.py::emit",
      "unit": "W-RIG",
      "axis": "Rule (g) artifact-freshness: replace emit()'s direct-subscript os.environ[\"CRYSTALIUM_GATE_NONCE\"]/[\"CRYSTALIUM_TREE_SHA\"] (no default, no .get, no fallback, KeyError-on-missing by design) with a silent os.environ.get(..., \"unknown\") fallback.",
      "differs_from_maker_axis": "Maker RC-1 (K-B16) reverts graph_liveness's edge-count comparison to the unwrapped all_edges() == expected_edge_count form (list-vs-int bug). Maker RC-2 (R-CONF) removes assemble_result's confounded-verdict axis-stripping guard. Neither touches emit() or rule (g) at all. My perturbation attacks a third, independent property of the same module -- artifact freshness/no-silent-default (K-C6) -- leaving graph_liveness and assemble_result byte-identical.",
      "perturbation_patch": "diff --git a/evals/_corpus_rig.py b/evals/_corpus_rig.py\nindex 915bc07..9f758e0 100644\n--- a/evals/_corpus_rig.py\n+++ b/evals/_corpus_rig.py\n@@ -511,8 +511,12 @@ def emit(result: dict[str, Any], out: str) -> None:\n     with today's stamped values.\n     \"\"\"\n     stamped = dict(result)\n-    stamped[\"run_nonce\"] = os.environ[\"CRYSTALIUM_GATE_NONCE\"]\n-    stamped[\"tree_sha\"] = os.environ[\"CRYSTALIUM_TREE_SHA\"]\n+    # RED-CHECK (kupo, checker axis -- NOT K-B16's edge-count unwrap, NOT\n+    # R-CONF's axis-stripping guard): rule (g)'s \"no default, no .get, no\n+    # fallback\" discipline replaced with a silent fallback. Attacks\n+    # artifact-freshness (K-C6), a third, independent property of this rig.\n+    stamped[\"run_nonce\"] = os.environ.get(\"CRYSTALIUM_GATE_NONCE\", \"unknown\")\n+    stamped[\"tree_sha\"] = os.environ.get(\"CRYSTALIUM_TREE_SHA\", \"unknown\")\n     path = Path(out)\n     path.parent.mkdir(parents=True, exist_ok=True)\n     path.write_text(json.dumps(stamped, indent=1, sort_keys=True))\n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_rig.py -v",
      "tree_sha": "80b4248af326331869a69311914682b2c8b1b450",
      "exit_code": 1,
      "output_tail": "self = <test_corpus_rig.TestEmit object at 0x7f6e2c5d0c80>\ntmp_path = PosixPath('/tmp/pytest-of-root/pytest-0/test_emit_raises_keyerror_with1')\nmonkeypatch = <_pytest.monkeypatch.MonkeyPatch object at 0x7f6e2a56af30>\n\n    def test_emit_raises_keyerror_without_tree_sha(\n        self, tmp_path: Path, monkeypatch: pytest.MonkeyPatch\n    ) -> None:\n        monkeypatch.setenv(\"CRYSTALIUM_GATE_NONCE\", \"nonce-abc-123\")\n        monkeypatch.delenv(\"CRYSTALIUM_TREE_SHA\", raising=False)\n        out = tmp_path / \"demo.json\"\n>       with pytest.raises(KeyError):\n             ^^^^^^^^^^^^^^^^^^^^^^^\nE       Failed: DID NOT RAISE KeyError\n\nmcp-server/tests/test_corpus_rig.py:485: Failed\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_corpus_rig.py::TestEmit::test_emit_raises_keyerror_without_nonce\nFAILED mcp-server/tests/test_corpus_rig.py::TestEmit::test_emit_raises_keyerror_without_tree_sha\n========================= 2 failed, 34 passed in 5.14s =========================\n",
      "restore": {
        "command": "git checkout -- evals/_corpus_rig.py && docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_rig.py -v",
        "exit_code": 0
      },
      "independently_reproduced": true
    },
    {
      "gate": "cross-layer G-XL (#52) -- evals/cross_layer_gate.py",
      "unit": "W-G-XL",
      "axis": "Attacks the C-XL-3 global-BM25 premise directly: ep1's filler summary is rewritten to carry the SAME term-frequency construction as sem-target (each of the 4 query terms x3, no padding) instead of the maker's dropped-query-term axis. Membership/arm-liveness counts (sparse_arm_size==4, dense_arm_size==0, edge_count==0, node_count==4) are UNCHANGED -- only global BM25 rank-0 ownership shifts from sem-target to ep1.",
      "differs_from_maker_axis": "Maker's ONLY axis on file (red-evidence-wgxl.json, both RC rows) drops the LAST query term from ep1's filler ([:-1]), attacking arm liveness / K-B1 term-membership (sparse_arm_size no longer == corpus_per_layer+1). NOTE: FORGE's D8 ruling table literally proposes this SAME maker axis as the 'checker replacement' for G-XL -- verified by diff, D8's suggested text is a verbatim re-description of the maker's own red-evidence, which would fail AC-332's own anti-replay step. I substituted a genuinely third axis per the task's own guidance (attack C-XL-3 / the global-BM25 premise instead): term-frequency OVER-matching rather than under-matching, verified empirically (see gate3-gxl.patch) to flip global_bm25_rank0 from 'sem-target' to 'ep1' while leaving every liveness count intact -- proving the dedicated C-XL-3 control node (test_global_premise_probe_ranks_target_first) actually fires on a violated premise, not just on missing membership.",
      "perturbation_patch": "diff --git a/evals/cross_layer_gate.py b/evals/cross_layer_gate.py\nindex 4ef198b..2b598e4 100644\n--- a/evals/cross_layer_gate.py\n+++ b/evals/cross_layer_gate.py\n@@ -167,6 +167,16 @@ def _filler_summary(label: str, total_tokens: int) -> str:\n     \"\"\"All four `_XL_QUERY_TERMS` exactly once, padded with `label`-prefixed\n     non-query nonce tokens to `total_tokens` (spec.amend-01.md Sec B.2.1 --\n     \"per-filler unique\", so no two fillers ever share a padding token).\"\"\"\n+    if label == \"ep1\":\n+        # RED-CHECK (kupo, checker axis -- NOT the maker's dropped-query-term\n+        # K-B1/arm-liveness axis): ep1 now carries the SAME term-frequency\n+        # construction as sem-target (each term x3, no padding) instead of a\n+        # strict SUBSET of the query. Membership/arm-liveness counts are\n+        # untouched (ep1 still matches all 4 terms) -- this attacks the\n+        # C-XL-3 global-BM25 premise: a filler now ties/beats sem-target for\n+        # GLOBAL bm25 rank 0, which the gate's `global_bm25_rank0_is_target`\n+        # pinned axis (and the dedicated C-XL-3 control node) must catch.\n+        return \" \".join(term for term in _XL_QUERY_TERMS for _ in range(3))\n     pad_count = total_tokens - len(_XL_QUERY_TERMS)\n     assert pad_count >= 0, (label, total_tokens)\n     padding = [f\"{label}pad{i:02d}\" for i in range(pad_count)]\n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_cross_layer_gate.py -v",
      "tree_sha": "80b4248af326331869a69311914682b2c8b1b450",
      "exit_code": 1,
      "output_tail": "        fixture = build_fixture(stores.relational, stores.graph)\n    \n        hits = stores.relational.bm25_search(_XL_QUERY, layer_filter=None, k=60)\n        ids = [h[\"id\"] for h in hits]\n>       assert ids[0] == fixture[\"target_id\"], ids\nE       AssertionError: ['ep1', 'sem-target', 'ep2', 'ep3']\nE       assert 'ep1' == 'sem-target'\nE         \nE         - sem-target\nE         + ep1\n\nmcp-server/tests/test_cross_layer_gate.py:134: AssertionError\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_cross_layer_gate.py::TestCrossLayerGateRedness::test_target_blocked_at_layer_append_rank\nFAILED mcp-server/tests/test_cross_layer_gate.py::TestCrossLayerGateRedness::test_sparse_ranking_is_layer_append_order\nFAILED mcp-server/tests/test_cross_layer_gate.py::TestCrossLayerGateRedness::test_sparse_ranking_provenance_is_spy\nFAILED mcp-server/tests/test_cross_layer_gate.py::TestLiveness::test_liveness_all_conjuncts_green\nFAILED mcp-server/tests/test_cross_layer_gate.py::TestGlobalPremiseProbe::test_global_premise_probe_ranks_target_first\n==================== 5 failed, 1 passed, 1 xfailed in 3.19s ====================\n",
      "restore": {
        "command": "git checkout -- evals/cross_layer_gate.py && docker compose run --rm crystalium pytest mcp-server/tests/test_cross_layer_gate.py -v",
        "exit_code": 0
      },
      "independently_reproduced": true
    },
    {
      "gate": "corpus-scaling G-CORPUS (#47) -- evals/corpus_scaling_gate.py",
      "unit": "W-G-CORPUS",
      "axis": "Attacks the corpus-SIZE parameter directly: _LARGE_NOISE_COUNT dropped from 59 to 20 (corpus_size 21), so M(21) < candidate_k(30) -- the deconfounding premise AC-314's worked example states ('M ~= 60 already reaches the M > candidate_k regime') is now FALSE. Neither the planted record's text/summary nor the graph store is touched.",
      "differs_from_maker_axis": "Maker RC-1 seeds a LINKS_TO graph edge (breaks edgelessness / R-CONF). Maker RC-2 rewrites the PLANTED RECORD's own summary text to match the noise competitors' term-frequency pattern (rank-separation axis, still a summary-text edit). My perturbation touches neither the graph store nor any crystal's summary text -- it moves the corpus SIZE constant, directly falsifying the M-vs-candidate_k inequality the gate's own worked example states as its premise. NOTE: FORGE's D8 table's suggested checker replacement ('boost the planted record's BM25 by repeating query terms twice more') is mechanistically the SAME lever as maker RC-2 (edit the planted record's own summary/TF) -- I deliberately did not use it, per the task's own 'find a third' instruction.",
      "perturbation_patch": "diff --git a/evals/corpus_scaling_gate.py b/evals/corpus_scaling_gate.py\nindex 014657d..6c1a748 100644\n--- a/evals/corpus_scaling_gate.py\n+++ b/evals/corpus_scaling_gate.py\n@@ -113,7 +113,13 @@ _PLANTED_ID = \"planted\"\n # AC-314 (spec.md Sec 4's own worked example): at k=10, candidate_k =\n # max(10*3, FETCH_WIDTH_FLOOR) = 30, so M ~= 60 already reaches the \"M >\n # candidate_k\" regime. 59 noise competitors + 1 planted record = 60.\n-_LARGE_NOISE_COUNT = 59\n+# RED-CHECK (kupo, checker axis -- NOT the maker's graph-edge / planted-TF\n+# axes): attacks the corpus SIZE parameter directly, neither the planted\n+# record's text nor the graph store. 20 noise + 1 planted = 21 < candidate_k\n+# (30) -- the deliberate \"M > candidate_k\" premise this gate exists to prove\n+# is now FALSE, so nothing should be truncated and the planted record must\n+# stop being dropped.\n+_LARGE_NOISE_COUNT = 20\n \n # AC-315 red-check: well under BOTH candidate_k (30) and the final response\n # cap k (10) -- see module docstring for why the second bound matters.\n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_scaling_gate.py -v",
      "tree_sha": "80b4248af326331869a69311914682b2c8b1b450",
      "exit_code": 1,
      "output_tail": "        assert result[\"verdict\"] == \"measured\", result\n        assert result[\"planted_recovered\"] is False, result\n    \n        liveness = result[\"liveness\"]\n        assert liveness[\"layers\"] == [\"episodic\"], liveness  # deconfounding: single layer\n        assert liveness[\"edge_count\"] == 0, liveness  # deconfounding: edgeless\n        assert liveness[\"node_count\"] > 0, liveness  # ... and genuinely populated, not dead\n        assert liveness[\"completion\"] is False, liveness\n>       assert liveness[\"corpus_size\"] > liveness[\"candidate_k\"], liveness\nE       AssertionError: {'corpus_size': 21, 'candidate_k': 30, 'sparse_arm_size': 21, 'layers': ['episodic'], ...}\nE       assert 21 > 30\n\nmcp-server/tests/test_corpus_scaling_gate.py:45: AssertionError\n----------------------------- Captured stdout call -----------------------------\n2026-08-06 13:30:46 [debug    ] tokenizer_backend              backend=tiktoken_cl100k_base\n2026-08-06 13:30:46 [info     ] recall_ok                      after_compose=10 caller_tier=T1 candidates=21 evicted=0 filtered=10 k=10 layers=['episodic'] query_len=34\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_corpus_scaling_gate.py::TestCorpusScalingGate::test_large_corpus_drops_planted\n========================= 1 failed, 2 passed in 3.08s ==========================\n",
      "restore": {
        "command": "git checkout -- evals/corpus_scaling_gate.py && docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_scaling_gate.py -v",
        "exit_code": 0
      },
      "independently_reproduced": true
    },
    {
      "gate": "weight-discrimination G-WD (#55) -- evals/weight_discrimination.py",
      "unit": "W-G-WD",
      "axis": "Attacks the BASE-ARM rank directly: the leader crystal's term frequency is de-loaded from 3x to 1x and lengthened with non-query padding, so B (still TF=1, short) now outranks the leader for sparse rank 0 instead of holding the known rank 2. A's graph edge (B->A) is completely untouched.",
      "differs_from_maker_axis": "Maker's ONLY axis on file (red-evidence-wgwd.json) severs record A's B->A graph edge (A loses its only support), which is a derived-arm-topology axis. FORGE's D8 ruling table proposes this EXACT SAME 'sever A's graph edge' text as the 'checker replacement' for G-WD -- verified by diff, D8's suggested row is a verbatim copy of the maker's own already-executed red-check, which would be a literal replay under AC-332's anti-replay rule. I substituted the task's own alternate suggested axis (attack the base-arm rank) instead: measured empirically (wd_probe.py) that de-loading the leader's TF flips sparse order from [leader, B] to [B, leader], breaking ONLY the 'base_known_rank' pinned axis (confirmed via direct dump: corpus_lt_candidate_k=True, graph_liveness=True, arm_liveness=True, phantom_derived_only=True, base_known_rank=False) -- graph_liveness stays green, proving this is a genuinely different failure mechanism than severing A's edge.",
      "perturbation_patch": "diff --git a/evals/weight_discrimination.py b/evals/weight_discrimination.py\nindex 2511b7b..eb184ed 100644\n--- a/evals/weight_discrimination.py\n+++ b/evals/weight_discrimination.py\n@@ -150,7 +150,13 @@ def _build_fixture(relational: Any, graph: Any) -> list[dict[str, Any]]:\n     leader = crystal(\n         _ID_LEADER,\n         _LAYER,\n-        f\"{_QUERY} {_QUERY} {_QUERY} engineering runbook notes\",\n+        # RED-CHECK (kupo, checker axis -- NOT the maker's \"sever A's graph\n+        # edge\" axis, which is the ONLY axis already on file in\n+        # red-evidence-wgwd.json): de-load the leader's term frequency to\n+        # match B's (1x instead of 3x) and lengthen it with non-query\n+        # padding. Attacks the BASE-ARM rank directly (B now outranks the\n+        # leader) -- A's graph support is untouched.\n+        f\"{_QUERY} engineering runbook notes extra pad filler words here to lengthen this document\",\n         project=_PROJECT,\n         agent_class=_AGENT_CLASS,\n         created_at=next(stamps),\n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_weight_discrimination.py -v",
      "tree_sha": "80b4248af326331869a69311914682b2c8b1b450",
      "exit_code": 1,
      "output_tail": "    def test_a_is_derived_only_and_b_is_the_known_sparse_rank(self, tmp_path: Path) -> None:\n        \"\"\"Fixture self-check: A has no sparse presence at all (arm_sizes.sparse\n        counts only the leader + B), and the derived arm holds exactly A.\"\"\"\n        result = run(seed_label=\"0\", weights=[1.00], data_root=str(tmp_path / \"wd-armcheck\"))\n        cell = result[\"cells\"][0]\n>       assert cell[\"arm_sizes\"][\"sparse\"] == 2, cell  # leader + B, never A\n               ^^^^^^^^^^^^^^^^^\nE       KeyError: 'arm_sizes'\n\nmcp-server/tests/test_weight_discrimination.py:141: KeyError\n----------------------------- Captured stdout call -----------------------------\n2026-08-06 13:33:13 [info     ] recall_ok                      after_compose=3 caller_tier=T1 candidates=3 evicted=0 filtered=3 k=3 layers=['episodic'] query_len=17\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_weight_discrimination.py::TestWeightDiscriminationOutcomes::test_ac317_distinct_outcomes_across_default_band\nFAILED mcp-server/tests/test_weight_discrimination.py::TestWeightDiscriminationOutcomes::test_ac375_wideband_positive_capability_control\nFAILED mcp-server/tests/test_weight_discrimination.py::TestWeightDiscriminationOutcomes::test_outcome_crosses_over_from_b_to_a_as_weight_rises\nFAILED mcp-server/tests/test_weight_discrimination.py::TestWeightDiscriminationOutcomes::test_scores_match_the_stated_rrf_arithmetic\nFAILED mcp-server/tests/test_weight_discrimination.py::TestWeightDiscriminationOutcomes::test_a_is_derived_only_and_b_is_the_known_sparse_rank\n========================= 5 failed, 4 passed in 4.47s ==========================\n",
      "restore": {
        "command": "git checkout -- evals/weight_discrimination.py && docker compose run --rm crystalium pytest mcp-server/tests/test_weight_discrimination.py -v",
        "exit_code": 0
      },
      "independently_reproduced": true
    },
    {
      "gate": "floor-sensitivity G-FLOOR (#48) -- evals/floor_sensitivity_gate.py [OPTIONAL SIXTH]",
      "unit": "W-G-FLOOR",
      "axis": "Attacks the FLOOR CONSTANTS themselves: run_seed's floor_high default changed from 1000 to 11. zzedgecomp sits at dense rank 11 (0-indexed); floor_high=11 gives dense_ranking[:11] (indices 0-10), which EXCLUDES rank 11 just like floor_low=10's [:10] does -- both floors now land in the same undiscovered-phantom regime.",
      "differs_from_maker_axis": "Maker RC-1 perturbs the VP-M1 positive control's phantom-target distinctness (edges rewired so c2/c3 also point at c1's phantom). Maker RC-2 repositions the edge-bearing competitor to dense rank 0 (rank-PLACEMENT axis within the fixture). Maker RC-3 deletes the fixture's LINKS_TO edge (graph-TOPOLOGY axis). FORGE's D8 table's suggested checker replacement for this row ('delete the edge from the edge-bearing competitor to its phantom') is VERBATIM IDENTICAL to maker RC-3 (already on file in red-evidence-wgfloor.json) -- another D8 replay trap, verified by diff. My perturbation touches neither the fixture's topology, its dense-rank placement, nor the VP-M1 control -- it changes the FLOOR VALUE itself (a parameter D8's own row description names as part of the axis space but which none of the 3 executed maker red-checks actually move). Note: the resulting failure value (0 != 0, both floors rank 0) coincidentally matches RC-3's value -- expected, since both floors excluding the competitor is the natural consequence of either lever -- but the patch/mechanism is verified non-identical (different file location, different lever).",
      "perturbation_patch": "diff --git a/evals/floor_sensitivity_gate.py b/evals/floor_sensitivity_gate.py\nindex a6e8d27..c377b9b 100644\n--- a/evals/floor_sensitivity_gate.py\n+++ b/evals/floor_sensitivity_gate.py\n@@ -491,7 +491,15 @@ def classify_disjoint(low_ranks: list[int], high_ranks: list[int]) -> bool:\n \n \n def run_seed(\n-    *, seed_label: str, floor_low: int = 10, floor_high: int = 1000,\n+    # RED-CHECK (kupo, checker axis -- NOT RC1's control-validity axis, NOT\n+    # RC2's dense-rank PLACEMENT axis, NOT RC3's graph-topology axis):\n+    # attacks the FLOOR CONSTANTS themselves. `zzedgecomp` sits at dense\n+    # rank 11 (0-indexed) in `_build_gate_fixture`; floor_high=11 gives\n+    # `dense_ranking[:11]` == indices 0-10, which EXCLUDES rank 11 just like\n+    # floor_low=10's `[:10]` does -- both floors now land in the SAME\n+    # regime (phantom undiscovered at either), collapsing the disjointness\n+    # AC-322 exists to prove.\n+    *, seed_label: str, floor_low: int = 10, floor_high: int = 11,\n     data_root: str = _DATA_ROOT,\n ) -> dict[str, Any]:\n     \"\"\"VP-M6 step 1 (verification-plan.amend-03.md Sec 3) / AC-322: one\n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_floor_sensitivity_gate.py -v",
      "tree_sha": "80b4248af326331869a69311914682b2c8b1b450",
      "exit_code": 1,
      "output_tail": "tmp_path = PosixPath('/tmp/pytest-of-root/pytest-0/test_run_seed_disjoint_ranks0')\n\n    def test_run_seed_disjoint_ranks(self, tmp_path) -> None:\n        row = run_seed(seed_label=\"0\", data_root=str(tmp_path / \"gate\"))\n        assert row[\"seed\"] == \"0\"\n        assert isinstance(row[\"floor10_target_rank\"], int)\n        assert isinstance(row[\"floor1000_target_rank\"], int)\n        assert row[\"floor10_target_rank\"] >= 0\n        assert row[\"floor1000_target_rank\"] >= 0\n>       assert row[\"floor10_target_rank\"] != row[\"floor1000_target_rank\"]\nE       assert 0 != 0\n\nmcp-server/tests/test_floor_sensitivity_gate.py:319: AssertionError\n----------------------------- Captured stdout call -----------------------------\n2026-08-06 13:36:24 [info     ] recall_ok                      after_compose=2 caller_tier=T1 candidates=16 evicted=0 filtered=2 k=2 layers=['episodic'] query_len=37\n2026-08-06 13:36:24 [info     ] recall_ok                      after_compose=2 caller_tier=T1 candidates=16 evicted=0 filtered=2 k=2 layers=['episodic'] query_len=37\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_floor_sensitivity_gate.py::TestGateFixture::test_run_seed_disjoint_ranks\n========================= 1 failed, 19 passed in 7.69s =========================\n",
      "restore": {
        "command": "git checkout -- evals/floor_sensitivity_gate.py && docker compose run --rm crystalium pytest mcp-server/tests/test_floor_sensitivity_gate.py -v",
        "exit_code": 0
      },
      "independently_reproduced": true
    }
  ]
}
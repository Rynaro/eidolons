{
  "gates": [
    {
      "gate": "#45 layer-merge fetch-shape (AC-348 / AC-377, K-C-N13)",
      "axis": "Attack the fetch-shape SELECTION logic rather than the merge or the backstop trigger: corrupt `_is_single_layer = len(target_layers) == 1` to `>= 1` in retrieve.py, so a 2-layer strict-subset request (`['semantic','procedural']`) is misrouted onto the single-layer branch (`layer = target_layers[0]`), silently dropping the second target layer entirely and skipping the global head + K-N12 backstop mechanism altogether.",
      "differs_from_maker_axis": "Maker's W-45 red-evidence (red-evidence-w45.json) used exactly two axes: (1) AC-342 -- whole-hunk revert of the entire retrieve.py fetch-shape diff back to tree_sha 973ab73 (a full feature removal, not a targeted logic corruption); (2) AC-355/AC-378 -- disabling the strict-subset starvation-BACKSTOP's own trigger via `if False and ...` in both the sparse and dense per-layer backstop loops. Neither maker axis touches the path-CLASSIFICATION booleans (`_is_no_exclusion`/`_is_single_layer`) at all -- both leave path selection intact and attack either the whole feature (revert) or the backstop's firing condition. My axis leaves 100% of #45's merge/backstop code untouched and corrupts only which BRANCH a strict-subset request is routed to. grep-checked: the literal mutated line/context is absent from red-evidence-w44.json and red-evidence-w42.json; it appears inside AC-342's red-evidence-w45.json entry only as CONTEXT within that patch's much larger (~150-line) full-hunk revert (the pre-mutation `== 1` line is part of what AC-342 deletes) -- the perturbation_patch texts themselves are not identical (single 1-line boolean-operator flip vs. a full multi-hunk revert), so this is not a replay under D8's identical-patch anti-replay test.",
      "perturbation_patch": "diff --git a/mcp-server/src/crystalium/aetheryte/retrieve.py b/mcp-server/src/crystalium/aetheryte/retrieve.py\nindex a44b3b6..6585819 100644\n--- a/mcp-server/src/crystalium/aetheryte/retrieve.py\n+++ b/mcp-server/src/crystalium/aetheryte/retrieve.py\n@@ -559,7 +559,7 @@ class Aetheryte:\n             _all_layers_set = set(_ALL_LAYERS)\n             _target_layer_set = set(target_layers)\n             _is_no_exclusion = _target_layer_set == _all_layers_set\n-            _is_single_layer = len(target_layers) == 1\n+            _is_single_layer = len(target_layers) >= 1  # RED-CHECK: fetch-shape SELECTION corrupted, strict-subset misrouted to single-layer branch\n \n             # Per-fetch raw-count capture (K-C-N13 / AC-377 / spec.amend-03.md\n             # \u00a715.4 \u2014 W-45 owns this so #44's top-up, W-44, can read, AT THE\n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_retrieve_layer_merge.py -v < /dev/null",
      "tree_sha": "d243789a4940418c48428ca0437777a4b8fef57c",
      "exit_code": 1,
      "output_tail": "FAILED mcp-server/tests/test_retrieve_layer_merge.py::test_subset_censoring_signal_is_the_global_fetch[censored]\n  assert fusion.get(\"n_sparse_cap\") == _CANDIDATE_K * 4, fusion\n  AssertionError: assert 15 == (15 * 4)   [AC-377: censoring signal reads the misrouted single-layer fetch's cap (15) instead of the global head's (60)]\nFAILED mcp-server/tests/test_retrieve_layer_merge.py::TestCallBudgetPerPath::test_call_budget_per_path[subset]\n  assert len(calls) == expected_calls, calls\n  AssertionError: ['semantic']\n  assert 1 == 3   [AC-348: only the single 'semantic' bm25_search call fired; 'procedural' and the K-N12 backstop never ran]\n========================= 2 failed, 5 passed in 4.04s =========================\nNOTE: test_subset_layer_recall_no_regression (AC-355, the maker's own suggested target) stayed GREEN under this axis --\nmeasured, not assumed: this fixture's semantic layer has exactly candidate_k(=15) rows total (14 fillers + 1 target),\nso the misrouted single-layer fetch (k=15) happens to still return the target by coincidence of corpus size. AC-377 and\nAC-348 are the criteria that actually catch this axis; reported as measured, per D9's own 'artifact, not adjective' rule.",
      "restore": {
        "command": "git checkout -- mcp-server/src/crystalium/aetheryte/retrieve.py",
        "exit_code": 0
      },
      "independently_reproduced": true
    },
    {
      "gate": "#44 status top-up censoring recompute / composition honesty (AC-346, D6/D7)",
      "axis": "Attack the CENSORING RECOMPUTE's DATA side rather than call presence, the backstop's own trigger, or the flag check: on the single-layer widen path, leave the `_topup_widen` bm25_search call firing, leave `raw_n_sparse_signal`/`cap_signal` HONESTLY updated to the widened fetch's true numbers (so the counter/signal fields are truthful), but comment out `bm25_hits = _merge_bm25_order(bm25_hits, wide_hits)` -- the widened rows are fetched and truthfully counted but never actually composed into the candidate set that reaches ranking.",
      "differs_from_maker_axis": "Maker's W-44 red-evidence (red-evidence-w44.json) used exactly three axes, all previously spent: (1) AC-347 -- delete the `_topup_widen` call's own bm25_search line, replacing it with an empty list, while leaving the `widened_fetches` counter append in place (dishonest counter, no call); (2) AC-379 -- disable ONLY the strict-subset per-layer BACKSTOP's own widen trigger via `if False and len(layer_hits) >= candidate_k:` (head-only simulation); (3) AC-356 -- delete the `if not self.recall_active_only: return True` early-return inside `_is_active`. None of these touch the MERGE step, and none produce an 'honest counter, wrong data' composition -- the closest, AC-347, makes the counter DISHONEST (fires=true with no call at all); my axis keeps the counter and the call fully honest and breaks only the data-routing step, the inverse failure mode. I first tested the task's literally-suggested axis (freeze `raw_n_sparse_signal`/`cap_signal` at their PRE-widen values across all three widen sites -- default, single-layer, strict-subset-head -- while leaving the merge/call/counter untouched) and, empirically, it reddened ZERO criteria in test_sparse_status_topup.py + test_retrieve_layer_merge.py + test_cross_layer_gate.py (22 passed, 1 xfailed both before and after) -- reported as a genuine, separate finding in checker-report-v2.1.0.md (a real coverage gap: no existing criterion checks the post-widen censoring signal on any of #44's own widen sites, only AC-377 checks it and AC-377's fixture runs with recall_active_only=False, never exercising #44's widen at all). I then pivoted to this merge-skip variant, which is on the same 'censoring recompute vs. the fetch actually performed' theme but targets the composition step, and verified it DOES flip AC-346 red. grep-checked: the literal mutated/removed line does not appear anywhere in red-evidence-w42.json, red-evidence-w44.json or red-evidence-w45.json.",
      "perturbation_patch": "diff --git a/mcp-server/src/crystalium/aetheryte/retrieve.py b/mcp-server/src/crystalium/aetheryte/retrieve.py\nindex a44b3b6..55950ec 100644\n--- a/mcp-server/src/crystalium/aetheryte/retrieve.py\n+++ b/mcp-server/src/crystalium/aetheryte/retrieve.py\n@@ -728,7 +728,13 @@ class Aetheryte:\n                         k_wide = min(candidate_k + n_inactive, HARD_TOPUP_CEILING)\n                         if k_wide > candidate_k:\n                             wide_hits = _topup_widen(\"head\", layer, candidate_k, k_wide, n_inactive)\n-                            bm25_hits = _merge_bm25_order(bm25_hits, wide_hits)\n+                            # RED-CHECK (axis: censoring recompute against the INITIAL fetch --\n+                            # the counter/signal fields below are left HONEST (correctly updated\n+                            # to the widened fetch's own numbers) but the composed candidate set\n+                            # (`bm25_hits`) is recomputed against the INITIAL, pre-widen fetch\n+                            # rather than the widened one -- the call fires, the counter is\n+                            # truthful, the data routed into ranking is not.\n+                            # bm25_hits = _merge_bm25_order(bm25_hits, wide_hits)\n                             raw_n_sparse_signal = len(wide_hits)\n                             cap_signal = k_wide\n \n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_sparse_status_topup.py -v < /dev/null",
      "tree_sha": "d243789a4940418c48428ca0437777a4b8fef57c",
      "exit_code": 1,
      "output_tail": "FAILED mcp-server/tests/test_sparse_status_topup.py::test_topup_recovers_active_hits\n    retrieved = [r.id for r in result.records]\n>   assert fixture[\"target_id\"] in retrieved, retrieved\nE   AssertionError: []\nE   assert 'active-target' in []\n----------------------------- Captured stdout call -----------------------------\nrecall_ok  after_compose=0 caller_tier=T1 candidates=15 evicted=0 filtered=0 k=5 layers=['semantic'] query_len=39\n==================== 1 failed, 6 passed, 1 xfailed in 3.66s ====================",
      "restore": {
        "command": "git checkout -- mcp-server/src/crystalium/aetheryte/retrieve.py",
        "exit_code": 0
      },
      "independently_reproduced": true
    },
    {
      "gate": "#42 seed-exclusion opt-in threading (AC-354, FORGE D2)",
      "axis": "Attack the PASS-THROUGH at `decaying_walk`'s inner `neighbor_expand` call (graph.py, D2's site table row 'graph.py:305') rather than the `:272` original-seeds subtraction the makers already used: hardcode `exclude_seeds=True` on that inner call, severing the walk-level flag from ever reaching it, so `decaying_walk(..., exclude_seeds=False)` still internally calls `neighbor_expand` as if the flag were True.",
      "differs_from_maker_axis": "Maker's W-42 red-evidence (red-evidence-w42.json) used exactly two axes: (1) AC-351 -- flip `neighbor_expand`'s own SIGNATURE default (`exclude_seeds: bool = True` -> `= False`), a different site (the function's own default parameter) exercised only through T1 with no explicit `exclude_seeds=` argument; (2) AC-354 (already D8-mandated / already run by the maker) -- sever the `:272` subtraction (`if exclude_seeds: hop_ids -= original_seeds` -> unconditional `hop_ids -= original_seeds`) inside `neighbor_expand` itself. My axis touches neither the signature default nor the `:272` subtraction -- it corrupts the SEPARATE pass-through call site inside `decaying_walk` (D2's ruling table lists this as its own site, `graph.py:305`, distinct from `:272`). grep-checked: the literal mutated line/context is absent from red-evidence-w44.json and red-evidence-w45.json, and absent from red-evidence-w42.json.",
      "perturbation_patch": "diff --git a/mcp-server/src/crystalium/storage/graph.py b/mcp-server/src/crystalium/storage/graph.py\nindex 00d5bf9..6dfa4f0 100644\n--- a/mcp-server/src/crystalium/storage/graph.py\n+++ b/mcp-server/src/crystalium/storage/graph.py\n@@ -335,7 +335,7 @@ class GraphStore:\n         visited: set[str] = set(seed_ids) if exclude_seeds else set()\n         frontier: set[str] = set(seed_ids)\n         for hop in range(1, max_hops + 1):\n-            nxt = self.neighbor_expand(sorted(frontier), depth=1, exclude_seeds=exclude_seeds)\n+            nxt = self.neighbor_expand(sorted(frontier), depth=1, exclude_seeds=True)  # RED-CHECK: pass-through severed at :305, walk-level flag ignored\n             new = {n for n in nxt if n not in visited}\n             if not new:\n                 break\n",
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_storage_graph.py -v -k \"exclude_seeds\" < /dev/null",
      "tree_sha": "d243789a4940418c48428ca0437777a4b8fef57c",
      "exit_code": 1,
      "output_tail": "mcp-server/tests/test_storage_graph.py::test_exclude_seeds_default_is_byte_identical[T1] PASSED\nmcp-server/tests/test_storage_graph.py::test_exclude_seeds_default_is_byte_identical[T2] PASSED\nmcp-server/tests/test_storage_graph.py::test_exclude_seeds_default_is_byte_identical[T3] PASSED\nmcp-server/tests/test_storage_graph.py::test_exclude_seeds_default_is_byte_identical[T3-variant] PASSED\nmcp-server/tests/test_storage_graph.py::test_exclude_seeds_false_expected_sets[T1] PASSED\nmcp-server/tests/test_storage_graph.py::test_exclude_seeds_false_expected_sets[T2] PASSED\nmcp-server/tests/test_storage_graph.py::test_exclude_seeds_false_expected_sets[T3] PASSED\nmcp-server/tests/test_storage_graph.py::test_exclude_seeds_false_expected_sets[T3-variant] FAILED\n>   assert scores == {\"t3v-s2\": 0.5}\nE   AssertionError: assert {} == {'t3v-s2': 0.5}\n================== 1 failed, 7 passed, 29 deselected in 0.55s ==================\nNOTE: AC-350 (True-branch byte-identity, all 4 topologies) stays fully GREEN, exactly as required.\nT3-variant reds; T3-proper stays green -- measured, not assumed: at hop >=2 the walk-level `visited` set\n(unaffected by this perturbation) already excludes previously-visited original seeds via `new = nxt - visited`,\nso this pass-through axis is only observable when a seed is reachable from ANOTHER seed within the SAME\nhop-1 frontier (the T3-variant shape), not at deeper hops (T3 proper) -- a precise, narrower finding than\n'T3 reddens', reported as measured.",
      "restore": {
        "command": "git checkout -- mcp-server/src/crystalium/storage/graph.py",
        "exit_code": 0
      },
      "independently_reproduced": true
    }
  ]
}
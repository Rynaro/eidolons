[
  {
    "gate": "test_corpus_rig.py::TestLiveness (test_liveness_measured_on_populated_edgeless_graph, test_liveness_measured_with_expected_edges)",
    "axis": "K-B16 / AC-305 -- graph_liveness's edge-count comparison reverted to the unwrapped `all_edges() == expected_edge_count` form (list-vs-int, ALWAYS False when expected_edge_count is an int) instead of `len(all_edges()) == expected_edge_count`. This is the exact anti-pattern spec.md's prose (`all_edges() == 0`) would have shipped -- a rig written this way can never conclude \"edgeless\", even on a genuinely populated, genuinely edgeless graph.",
    "perturbation_patch": "diff --git a/evals/_corpus_rig.py b/evals/_corpus_rig.py\nindex 915bc07..7acbafc 100644\n--- a/evals/_corpus_rig.py\n+++ b/evals/_corpus_rig.py\n@@ -364,7 +364,7 @@ def graph_liveness(\n     node_count = graph.node_count()\n     edge_count = len(graph.all_edges())\n     node_live = node_count == expected_node_count\n-    edge_live = edge_count == expected_edge_count\n+    edge_live = graph.all_edges() == expected_edge_count  # RED-CHECK: reintroduces K-B16\n     liveness = {\n         \"node_count\": node_count,\n         \"edge_count\": edge_count,\n",
    "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_rig.py -v",
    "tree_sha": "b7f1a477b4a0bda2c2ecd7c3383d036e316c5abc",
    "exit_code": 1,
    "output_tail": "lf = <test_corpus_rig.TestLiveness object at 0x7fade29bcb60>\ntmp_path = PosixPath('/tmp/pytest-of-root/pytest-0/test_liveness_measured_on_popu0')\n\n    def test_liveness_measured_on_populated_edgeless_graph(self, tmp_path: Path) -> None:\n        \"\"\"AC-305 (K-C-N12), the discriminating node. `all_edges() == 0` is\n        `[] == 0` -- ALWAYS `False` in Python -- so a rig written that way\n        can never conclude \"edgeless\" on a real, populated, genuinely\n        edgeless graph. This node builds exactly that graph and requires\n        the rig to say so.\"\"\"\n        stores = new_stores(str(tmp_path), \"edgeless-populated\")\n        seed_graph(stores.graph, nodes=[(f\"n{i}\", \"episodic\") for i in range(4)])\n    \n        result = graph_liveness(stores.graph, expected_node_count=4, expected_edge_count=0)\n    \n>       assert result[\"verdict\"] == \"measured\"\nE       AssertionError: assert 'confounded' == 'measured'\nE         \nE         - measured\nE         + confounded\n\nmcp-server/tests/test_corpus_rig.py:274: AssertionError\n___________ TestLiveness.test_liveness_measured_with_expected_edges ____________\n\nself = <test_corpus_rig.TestLiveness object at 0x7fade29c3dd0>\ntmp_path = PosixPath('/tmp/pytest-of-root/pytest-0/test_liveness_measured_with_ex0')\n\n    def test_liveness_measured_with_expected_edges(self, tmp_path: Path) -> None:\n        \"\"\"The general form also reports \"measured\" for a NON-edgeless\n        fixture whose edge count matches what was seeded -- `graph_liveness`\n        is not restricted to the edgeless case.\"\"\"\n        stores = new_stores(str(tmp_path), \"expected-edges\")\n        seed_graph(\n            stores.graph,\n            nodes=[(\"a\", \"episodic\"), (\"b\", \"episodic\"), (\"c\", \"episodic\")],\n            edges=[(\"a\", \"b\", \"LINKS_TO\"), (\"b\", \"c\", \"LINKS_TO\")],\n        )\n    \n        result = graph_liveness(stores.graph, expected_node_count=3, expected_edge_count=2)\n    \n>       assert result[\"verdict\"] == \"measured\"\nE       AssertionError: assert 'confounded' == 'measured'\nE         \nE         - measured\nE         + confounded\n\nmcp-server/tests/test_corpus_rig.py:343: AssertionError\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_corpus_rig.py::TestLiveness::test_liveness_measured_on_populated_edgeless_graph\nFAILED mcp-server/tests/test_corpus_rig.py::TestLiveness::test_liveness_measured_with_expected_edges\n========================= 2 failed, 34 passed in 3.23s =========================\n\nEXIT=1\n",
    "restore": {
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_rig.py -v",
      "exit_code": 0
    }
  },
  {
    "gate": "test_corpus_rig.py::TestLiveness::test_liveness_confounded_on_empty_graph, TestVerdictClassifier::test_confounded_axis_returns_no_numbers",
    "axis": "R-CONF -- assemble_result's axis-stripping branch (`if verdict == \"confounded\": result[\"axes\"] = None; return result`) removed, so a confounded verdict no longer suppresses the numeric axes a gate attaches. Demonstrates the rig's own R-CONF enforcement is load-bearing, not vestigial: with the guard removed, a deliberately mis-pinned axis (a real edge present while the gate pins expected_edge_count=0) still correctly resolves to verdict \"confounded\" via graph_liveness/resolve_verdict, but the numeric axes leak through instead of being suppressed -- exactly the defect R-CONF exists to prevent (retrieval_gate.py:301-310 precedent).",
    "perturbation_patch": "diff --git a/evals/_corpus_rig.py b/evals/_corpus_rig.py\nindex 915bc07..e0faac6 100644\n--- a/evals/_corpus_rig.py\n+++ b/evals/_corpus_rig.py\n@@ -472,9 +472,8 @@ def assemble_result(\n     result: dict[str, Any] = {\"verdict\": verdict}\n     if liveness is not None:\n         result[\"liveness\"] = liveness\n-    if verdict == \"confounded\":\n-        result[\"axes\"] = None\n-        return result\n+    # RED-CHECK: R-CONF axis-stripping disabled -- a confounded verdict must\n+    # NOT be able to leak numeric axes; this perturbation removes the guard.\n     result[\"axes\"] = axes\n     result.update(extra)\n     return result\n",
    "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_rig.py -v",
    "tree_sha": "b7f1a477b4a0bda2c2ecd7c3383d036e316c5abc",
    "exit_code": 1,
    "output_tail": "ctClassifier.test_confounded_axis_returns_no_numbers _________\n\nself = <test_corpus_rig.TestVerdictClassifier object at 0x7fe5868393d0>\ntmp_path = PosixPath('/tmp/pytest-of-root/pytest-0/test_confounded_axis_returns_n0')\n\n    def test_confounded_axis_returns_no_numbers(self, tmp_path: Path) -> None:\n        \"\"\"AC-305: a gate fixture with ONE pinned axis deliberately set to a\n        binding value must return verdict \"confounded\" and emit NO numeric\n        axes (R-CONF; retrieval_gate.py:301-310 precedent).\n    \n        Demonstrated end-to-end against REAL stores: the fixture actually\n        carries one edge, but the gate's OWN pinned axis asserts \"the graph\n        is edgeless\" (`expected_edge_count=0`) -- deliberately mis-pinned,\n        since this fixture is not edgeless at all. The axis is therefore\n        BINDING (the precondition it names is false), `graph_liveness` must\n        report \"confounded\", `resolve_verdict` must fold that into an\n        overall \"confounded\" verdict, and `assemble_result` must strip any\n        numeric axis a (hypothetical, buggy) gate tries to attach anyway.\n        \"\"\"\n        stores = new_stores(str(tmp_path), \"confounded-red-check\")\n        seed_graph(\n            stores.graph,\n            nodes=[(\"a\", \"episodic\"), (\"b\", \"episodic\")],\n            edges=[(\"a\", \"b\", \"LINKS_TO\")],\n        )\n    \n        graph_live = graph_liveness(stores.graph, expected_node_count=2, expected_edge_count=0)\n        assert graph_live[\"verdict\"] == \"confounded\"  # the pinned axis IS binding here\n    \n        verdict = resolve_verdict(pinned={\"graph_edgeless\": graph_live[\"verdict\"] == \"measured\"})\n        assert verdict == \"confounded\"\n    \n        result = assemble_result(\n            verdict=verdict,\n            liveness=graph_live[\"liveness\"],\n            axes={\"target_rank\": 0, \"corpus_per_layer\": 3, \"candidate_k\": 15},\n        )\n        assert result[\"verdict\"] == \"confounded\"\n>       assert result[\"axes\"] is None\nE       AssertionError: assert {'target_rank': 0, 'corpus_per_layer': 3, 'candidate_k': 15} is None\n\nmcp-server/tests/test_corpus_rig.py:430: AssertionError\n=========================== short test summary info ============================\nFAILED mcp-server/tests/test_corpus_rig.py::TestLiveness::test_liveness_confounded_on_empty_graph\nFAILED mcp-server/tests/test_corpus_rig.py::TestVerdictClassifier::test_confounded_axis_returns_no_numbers\n========================= 2 failed, 34 passed in 3.36s =========================\n\nEXIT=1\n",
    "restore": {
      "command": "docker compose run --rm crystalium pytest mcp-server/tests/test_corpus_rig.py -v",
      "exit_code": 0
    }
  }
]

# Acceptance Criteria — ESL change `generalist-eidolon` (rev 3, post-critique cycle 2)

> **Frozen document.** SHA-256 frozen at RAMZA Assemble. Self-contained: all terms, lexicons, the
> reference-extractor algorithm, and the machine-derived fixtures are defined here. Maker: ramza.
> Checker: pending (independent critic re-check + human go/no-go). Rev 3 dispositions
> CRIT-013..CRIT-021; see `spec.md` §Revision Log. The fixture table below was **generated by
> executing the reference extractor on each prompt** — no cell is hand-set (CRIT-013).

## Definitions (self-contained)

- **TBD_NAME** — placeholder for the member's persona name (human decision; MUST NOT ship).
- **`generalist`** — the proposed 9th `capability_class` enum value.
- **τ (tau)** — specialist dispatch threshold `0.6`.
- **Step-2(a)/(b)** — split of the Step-2 no-match branch: (a) actionable → dispatch generalist;
  (b) underspecified → `clarification_request`.
- **mission-contract / handoff-request** — inbound typed contract / emitted PROPOSE-upward artifact
  (`edge_origin: emitted-request`); the generalist never dispatches.
- **always-loaded region** — EIDOLONS.md bytes strictly between `<!-- always-loaded:start -->` and
  `<!-- always-loaded:end -->`.
- **Predicate input language** — **English only** (CRIT-019). Non-English, emoji-only, or
  code-fenced-only prompts route to `clarification_request` by design (the English lexicons do not
  match, so S1=0 → clarify).

### Closed lexicons (rev 3; presence-matched, case-insensitive, word-boundary)

A **phrase pre-normalization** pass runs first, replacing each multi-word phrase with an
underscore-joined single token: `set up→set_up, look into→look_into, figure out→figure_out,
look at→look_at, deal with→deal_with, work on→work_on, roll back→roll_back, limited to→limited_to,
so that→so_that, exits 0→exits_0, without error→without_error, no longer→no_longer,
the project→the_project, the codebase→the_codebase, the app→the_app, the repo→the_repo,
the system→the_system, entire codebase→entire_codebase`.

- **ACT_VERBS**: `add, create, write, implement, build, fix, edit, modify, update, rename, remove,
  delete, refactor, migrate, configure, wire, install, generate, apply, patch, replace, extend,
  rewrite, convert, bump, upgrade, set_up, scaffold, append, insert, enable, disable, revert,
  rollback, roll_back, seed, provision, compile, lint, format, export, import, publish, init,
  bootstrap, stub, deprecate` (broadened per CRIT-018).
- **EXCLUDED_POLYSEMOUS** (never satisfy S1): `make, improve, handle, deal_with, work_on, better`.
- **INVESTIGATE_VERBS**: `look_into, investigate, figure_out, explore, analyze, review, understand,
  explain, find, trace, map, diagnose, look_at, check, audit`.
- **DELIVERABLE_NOUNS**: `file, patch, function, method, class, module, endpoint, route, script,
  config, test, fixture, migration, workflow, schema, table, component, flag, cli, ci, pipeline,
  dockerfile, readme, docs, hook, rule, field`.
- **GENERIC_SCOPE**: `everything, everywhere, all, entire, whole, project_wide, repo_wide, codebase,
  throughout, across, anything, somewhere, the_project, the_codebase, the_app, the_repo,
  the_system, entire_codebase`.
- **LIMITERS**: `only, just, limited_to, solely, exclusively`.
- **ACCEPTANCE_MARKERS**: `so_that, until, passes, passing, matches, returns, expected, equal,
  green, exits_0, without_error, no_longer, fixes`; plus a numeric target = an `^[0-9]+%?$` token
  that is `%`-suffixed or immediately followed by a word token.
- **FILE_EXT** (closed): `ts tsx js jsx py rb go rs sh bash json yaml yml toml md txt sql bats java
  kt c h cpp cs php lock cfg ini env`.
- **CLAUSE_MARKERS**: `<start-of-prompt>, and, then, also, to, please`, or the previous token ended
  with `, ; : .`.
- **DET_BLOCKLIST**: `the, a, an, this, that, these, those, my, our, your, his, her, its, their,
  no, any, some, each, every`.

### PATH_OR_ID token (tightened; CRIT-015)

A token is `PATH_OR_ID` iff ANY holds (evaluated on the token core = strip surrounding backticks +
edge punctuation):
1. **path** — contains `/` and contains ≥1 `[A-Za-z]` (e.g. `cli/src/status.sh`, `src/legacy/`);
2. **known extension** — matches `^([A-Za-z0-9_.\-]*)\.([A-Za-z0-9]+)$` where the extension ∈ FILE_EXT
   **and** the stem contains ≥1 `[A-Za-z]` (so `30.5s`, `2.5.0`, `3.14`, `e.g.` do NOT match —
   verified excluded);
3. **fenced identifier** — the raw token was backtick-fenced and the fenced core matches
   `^(--)?[A-Za-z_][A-Za-z0-9_-]*$` (e.g. `` `getUserData` ``, `` `--json` ``, `` `retry` ``).
Bare acronyms/mixed-case words (`CI`, `REST`, `gRPC`) are NOT PATH_OR_ID.

### The reference extractor (deterministic; a third party MUST reproduce every vector)

Phrase-normalize → whitespace-tokenize (retain each token's backtick/trailing-punct info) → compute:

| Signal | Rule |
|---|---|
| **S1 act_verb** | `1` iff ∃ token `t` with core ∈ ACT_VERBS, core ∉ EXCLUDED_POLYSEMOUS, `t` in **imperative position** (previous token is a CLAUSE_MARKER or `t` is first), and previous-token-core ∉ DET_BLOCKLIST. Kills noun-sense hits ("the recent patch", "the update"). |
| **S2 deliverable** | `1` iff ∃ core ∈ DELIVERABLE_NOUNS OR ∃ PATH_OR_ID token |
| **S3 named_target** | `1` iff ∃ PATH_OR_ID token (a generic word alone never satisfies S3) |
| **S4 acceptance** | `1` iff ∃ core ∈ ACCEPTANCE_MARKERS OR a numeric target token |
| **S5 bounded** | if no GENERIC_SCOPE token → `1`; else `1` iff (∃ LIMITER token) **AND** (∃ PATH_OR_ID token), else `0` (a co-occurring path alone does NOT neutralize a generic scope — CRIT-016) |
| **S6 no_specialist_ge_tau** | (precondition, from Step-1 scorer) max specialist score `< τ` |
| **S7 no_chain** | (precondition, from Step-1 scorer) fewer than 2 capability classes have a trigger-verb hit |
| **tie-break** | any signal indeterminate → `0` (bias to `clarification_request`) |

**Predicate** (only when S6 ∧ S7 hold): `actionable = S1 ∧ S2 ∧ S3 ∧ S4 ∧ S5`.

### Normative predicate fixtures (machine-derived; frozen)

S1..S5 are the reference-extractor output; S6,S7 are declared preconditions. Two independent
conforming extractors MUST produce identical S1..S5 vectors. No cell below is hand-set (CRIT-013).

| Fixture | Prompt (verbatim) | S1 | S2 | S3 | S4 | S5 | S6 | S7 | Route |
|---|---|----|----|----|----|----|----|----|---|
| P1 | Make the project better. | 0 | 0 | 0 | 0 | 0 | 1 | 1 | clarification_request |
| P2 | Improve performance somewhere in the codebase. | 0 | 0 | 0 | 0 | 0 | 1 | 1 | clarification_request |
| P3 | Rename `getUserData` to `fetchUserProfile` everywhere it appears and update all call sites and the docs. | 1 | 1 | 1 | 0 | 0 | 1 | 1 | clarification_request |
| P4 | Look into the flaky checkout test and figure out what's going on. | 0 | 1 | 0 | 0 | 1 | 1 | 1 | clarification_request |
| P5 | Set up CI for the project. | 1 | 1 | 0 | 0 | 0 | 1 | 1 | clarification_request |
| P6 | Weigh whether we should use REST or gRPC for the new service and set it up. | 0 | 0 | 0 | 0 | 1 | 1 | 0 | chain (decide-then-implement) |
| P7 | In `services/auth/Login.ts`, explain what the recent patch does and why the update matters, until it's clear to me. | 0 | 1 | 1 | 1 | 1 | 1 | 1 | clarification_request |
| P8 | Configure the timeout to 30.5s until the healthcheck returns healthy. | 1 | 0 | 0 | 1 | 1 | 1 | 1 | clarification_request |
| P9 | Refactor `src/legacy/` across the entire codebase to remove deprecated calls, until the test suite is green. | 1 | 1 | 1 | 1 | 0 | 1 | 1 | clarification_request |
| P10 | Crea el archivo `src/app.py` y anade la funcion `login` para que el test pase. | 0 | 1 | 1 | 0 | 1 | 1 | 1 | clarification_request |
| P11 | Bump the dependency to 2.5.0. | 1 | 0 | 0 | 0 | 1 | 1 | 1 | clarification_request |
| C1 | Add a `--json` flag to `cli/src/status.sh` and update `cli/tests/status.bats` so the new case passes. | 1 | 1 | 1 | 1 | 1 | 1 | 1 | generalist |
| C2 | Refactor the auth layer. | 1 | 0 | 0 | 0 | 1 | 1 | 1 | clarification_request |
| C3 | Create `scripts/backup.sh` that dumps the DB to `./backups/` and exits 0 on success. | 1 | 1 | 1 | 1 | 1 | 1 | 1 | generalist |
| C4 | Update everything. | 1 | 0 | 0 | 0 | 0 | 1 | 1 | clarification_request |
| C5 | Append a `retry` field to `config/http.yaml` so requests retry 3 times. | 1 | 1 | 1 | 1 | 1 | 1 | 1 | generalist |
| C6 | Replace the deprecated call across `src/auth/` only, until the auth tests pass. | 1 | 1 | 1 | 1 | 1 | 1 | 1 | generalist |

P7 (noun-trap), P8 (version/decimal), P9 (generic-scope + path), P10 (non-English), P11 (version) are
the CRIT-014/015/016/019 regression fixtures. C6 exercises the LIMITER rescue (bounded generic scope).

---

## Track A — Capability-class schema

### AC-A01 (event-driven)
GIVEN `schemas/roster-entry.schema.json`
WHEN the schema is loaded
THEN its `capability_class.enum` SHALL contain the value `"generalist"`
VERIFY: `jq '.properties.capability_class.enum | index("generalist")'` returns a non-null index

### AC-A02a (ubiquitous)
THEN the `capability_class.enum` SHALL retain every one of the eight prior values as a superset
VERIFY: `jq` assertion that the eight original values are each present in the new enum

### AC-A02b (ubiquitous)
THEN the `capability_class.enum` length SHALL equal exactly 9
VERIFY: `jq '.properties.capability_class.enum | length'` equals `9`

### AC-A03 (unwanted-behavior)
GIVEN `schemas/routing.schema.json`
IF a change enumerates `capability_class` as a closed enum there
THEN the change SHALL be rejected, keeping the field an open `"type":"string"`
VERIFY: `jq '.. | .capability_class? | select(.!=null) | .type'` equals `"string"`

---

## Track B — Roster member row (in_construction)

### AC-B01 (event-driven)
GIVEN `roster/index.yaml` with the TBD_NAME entry
WHEN `make schema` runs
THEN the TBD_NAME entry SHALL validate with `capability_class: generalist`
VERIFY: `make schema` exits 0

### AC-B02 (ubiquitous)
THEN the TBD_NAME entry `status` SHALL equal `in_construction` at introduction
VERIFY: `yq '.eidolons[] | select(.name=="TBD_NAME") | .status'` equals `in_construction`

### AC-B03 (ubiquitous)
THEN the TBD_NAME cortex row SHALL declare zero positive trigger verbs
VERIFY: the EIDOLONS.md trigger cell for TBD_NAME is empty; router unit shows TBD_NAME absent from Step-1

### AC-B04 (ubiquitous)
THEN the TBD_NAME refuse set SHALL name every specialist-owned intent class
VERIFY: refuse-set checklist maps each specialist class to a refuse token

### AC-B05 (ubiquitous)
THEN `handoffs.downstream` for TBD_NAME SHALL equal `[]`
VERIFY: `yq '.eidolons[] | select(.name=="TBD_NAME") | .handoffs.downstream'` equals `[]`

### AC-B06 (ubiquitous)
THEN `handoffs.upstream` for TBD_NAME SHALL be `[orchestrator]`
VERIFY: `yq` equals `["orchestrator"]`

### AC-B07 (ubiquitous)
THEN `security.writes_repo` for TBD_NAME SHALL equal `false`
VERIFY: `yq '.eidolons[] | select(.name=="TBD_NAME") | .security.writes_repo'` equals `false`

### AC-B08 (event-driven)
GIVEN `.github/workflows/roster-health.yml`
WHEN the roster-health matrix expands
THEN the `eidolon:` matrix array SHALL contain the resolved TBD_NAME
VERIFY: `grep 'eidolon: \[' .github/workflows/roster-health.yml` includes the name

### AC-B09 (unwanted-behavior)
GIVEN TBD_NAME `status: in_construction`
IF roster-health runs the EIIS conformance step
THEN the EIIS conformance step SHALL be skipped for TBD_NAME
VERIFY: workflow conditional gates conformance on `status != in_construction`

### AC-B10 (ubiquitous)
THEN the `.claude/agents/<name>.md` tool allowlist SHALL omit a generic `Bash(*)` grant
VERIFY: `grep -c 'Bash(\*)' .claude/agents/<name>.md` equals 0

---

## Track C — Dispatch Step-2 mechanical predicate (cortex)

### AC-C01 (event-driven)
GIVEN no Eidolon scores ≥ τ for a prompt (S6=1, S7=1)
WHEN the predicate `S1∧S2∧S3∧S4∧S5` evaluates true
THEN the router SHALL select TBD_NAME as a bounded-authority fallthrough worker at standard tier
VERIFY: fixtures C1,C3,C5,C6 each produce `selected: [TBD_NAME]`

### AC-C02 (unwanted-behavior)
GIVEN no Eidolon scores ≥ τ for a prompt
IF the predicate evaluates false
THEN the router SHALL emit `clarification_request` rather than dispatch TBD_NAME
VERIFY: fixtures P1,P2,P3,P4,P5,P7,P8,P9,P10,P11,C2,C4 each produce `clarification_request`

### AC-C03 (unwanted-behavior)
GIVEN a prompt whose labeled ground-truth owner is a specialist
IF Step-2 is reached
THEN TBD_NAME SHALL NOT be dispatched for that prompt
VERIFY: over-capture count equals 0 against the blind-frozen labeled-owner key (see AC-G03)

### AC-C04 (ubiquitous)
THEN TBD_NAME SHALL never appear in the Step-1 candidate scoring set
VERIFY: router unit asserts TBD_NAME is excluded from Step-1 candidates

### AC-C05 (state-driven)
GIVEN the Step-2 signal extractor
THEN the extractor SHALL resolve S1..S5 from the closed lexicons with no model-discretionary call
VERIFY: the no-LLM reference extractor emits deterministic vectors for the seventeen normative fixtures

### AC-C06 (event-driven)
GIVEN the seventeen normative fixtures
WHEN the deterministic extractor runs prompt-text → S1..S5
THEN each produced signal vector SHALL exactly match the frozen fixture-table vector
VERIFY: extractor output equals the frozen S1..S5 columns row-for-row (exact match, exit 0)

### AC-C07 (unwanted-behavior)
GIVEN the fixture P1 "Make the project better."
IF the router evaluates it
THEN it SHALL resolve to `clarification_request`
VERIFY: fixture P1 routes to `clarification_request`

### AC-C08 (unwanted-behavior)
GIVEN a prompt carrying a GENERIC_SCOPE token
IF no LIMITER token co-occurs with a PATH_OR_ID token
THEN S5 SHALL be 0 so the prompt routes to `clarification_request`, never to the `writes_repo:false` worker
VERIFY: fixtures P3,P9,C4 yield S5=0; C6 (LIMITER + path) yields S5=1

### AC-C09 (unwanted-behavior)
GIVEN a prompt where an ACT_VERBS lexeme appears in noun position (preceded by a DET_BLOCKLIST word or not in imperative position)
IF the extractor computes S1
THEN that lexeme SHALL NOT set S1
VERIFY: fixture P7 ("the recent patch", "the update") yields S1=0

### AC-C10 (unwanted-behavior)
GIVEN a token that is a version, decimal, measurement, or Latin abbreviation (`30.5s`, `2.5.0`, `e.g.`)
IF the extractor evaluates PATH_OR_ID
THEN the token SHALL NOT match PATH_OR_ID
VERIFY: fixtures P8,P11 yield S3=0 for such tokens

### AC-C11 (event-driven)
GIVEN the frozen fixture table and the published lexicons/extractor rules
WHEN a derivation self-check regenerates the S1..S5 columns from the rules
THEN the regenerated columns SHALL equal the frozen columns with zero hand-edited cells
VERIFY: a self-check script re-derives the table and diffs against the frozen columns (exit 0)

### AC-C12 (unwanted-behavior)
GIVEN a non-English or emoji-only prompt
IF the extractor evaluates S1 against the English lexicons
THEN S1 SHALL be 0 so the prompt routes to `clarification_request` by design
VERIFY: fixture P10 (Spanish) yields S1=0 and routes to `clarification_request`

---

## Track D — Cortex budget precondition

### AC-D01 (unwanted-behavior)
GIVEN the token-budget CI check
IF the always-loaded region proxy-count exceeds the ceiling
THEN the check SHALL exit non-zero
VERIFY: token-budget script returns exit 1 on an over-budget fixture

### AC-D02 (event-driven)
GIVEN the post-change EIDOLONS.md
WHEN the token-budget CI check counts `ceil(char_count / 4)` of the bytes strictly between `<!-- always-loaded:start -->` and `<!-- always-loaded:end -->`
THEN the proxy-count SHALL be ≤ 850 (a conservative margin under the I-C4 900-token ceiling)
VERIFY: the script prints a `chars/4` proxy-count ≤ 850 for the delimited region

### AC-D03 (ubiquitous)
THEN the token-budget check SHALL be bash 3.2 compatible
VERIFY: `shellcheck -x -S error` clean; no `declare -A` / `mapfile` / `&>>` constructs

### AC-D04 (ubiquitous)
THEN each deep table relocated out of the always-loaded region SHALL sit outside the `<!-- always-loaded:start -->`/`<!-- always-loaded:end -->` markers under `methodology/cortex/`
VERIFY: grep confirms relocated tables are outside the delimited region

### AC-D05 (unwanted-behavior)
GIVEN the rollout
IF the TBD_NAME roster-index row lands before the token-budget CI is green
THEN the change SHALL be blocked
VERIFY: rollout gate requires token-budget CI green on the same-or-earlier commit as the row

### AC-D06 (event-driven)
GIVEN a reference token count from a named BPE tokenizer on a fixed sample
WHEN the `chars/4` proxy runs on the same sample
THEN the proxy-count SHALL be within +/- 15% of the reference
VERIFY: a checked-in fixture asserts proxy-vs-reference agreement within tolerance

---

## Track E — Member methodology (external repo; nexus NAMES only)

### AC-E01 (optional-feature)
GIVEN the `Rynaro/TBD_NAME` repo exists
THEN it SHALL satisfy EIIS-1.0 install conformance
VERIFY: the EIIS conformance checker exits 0 against the member repo

### AC-E02 (ubiquitous)
THEN the member methodology SHALL define a structured TaskState schema
VERIFY: a TaskState schema artifact is present in the member repo

### AC-E03 (ubiquitous)
THEN the member SHALL define a typed delegation contract carrying fields objective, scope, deliverables, evidence_required, stop_conditions
VERIFY: schema field-presence check on the delegation-contract artifact

### AC-E04 (ubiquitous)
THEN the member SHALL define a capability-authority table with a default column plus an escalation column
VERIFY: table-presence check over read/write/exec/network/secrets/deploy rows

### AC-E05 (ubiquitous)
THEN the member methodology SHALL gate results through externalized verification, never self-report
VERIFY: methodology section names tests/parsers/diffs/env-feedback as the result gate

### AC-E06 (ubiquitous)
THEN the member SHALL declare an explicit stopping policy over the set {continue, recover, escalate, terminate}
VERIFY: the stopping-policy artifact enumerates exactly those four states

### AC-E07 (ubiquitous)
THEN the member SHALL declare a bounded loop budget over time/turns/tokens
VERIFY: budget fields present in the TaskState schema

### AC-E08 (unwanted-behavior)
GIVEN the nexus repo
IF nexus files contain the member's methodology prose
THEN HC1 SHALL be treated as violated
VERIFY: grep of the nexus tree finds no TaskState/stopping-policy methodology prose

### AC-E09 (ubiquitous)
THEN the member SHALL express delegation as an emitted handoff-request artifact rather than by dispatching a subagent
VERIFY: methodology cross-checks `downstream: []`; no spawn primitive is invoked

---

## Track F — ECL edge contracts + composition regen

### AC-F01 (event-driven)
GIVEN the eidolons-ecl contracts for TBD_NAME are merged
WHEN `compose-gen` regenerates `methodology/composition.md`
THEN the nexus `composition-drift.yml` CI SHALL pass with no manual edits
VERIFY: `composition-drift.yml` exits 0 post-regen

### AC-F02 (unwanted-behavior)
GIVEN a TBD_NAME edge contract
IF it names a performative outside the closed 10-set
THEN the ECL conformance checker SHALL reject it
VERIFY: ECL conformance checker exits non-zero on an out-of-set performative

### AC-F03 (ubiquitous)
THEN every declared TBD_NAME hand-off edge SHALL have exactly one directed-edge YAML contract
VERIFY: one `contracts/*.yaml` file exists per edge in the declared handoffs set

### AC-F04 (ubiquitous)
THEN every new TBD_NAME artifact kind SHALL carry a `schema_ref` under `schemas/per-eidolon/`
VERIFY: each artifact `kind` referenced by the edge contracts resolves to a schema file

### AC-F05 (ubiquitous)
THEN each outbound `TBD_NAME→{atlas,kupo,vigil,idg,forge}` edge SHALL set `edge_origin: emitted-request` with a PROPOSE-upward performative
VERIFY: each outbound contract's `edge_origin` is `emitted-request`, reconciling `downstream: []`

---

## Track G — Two-arm measurement gate (maker≠checker)

### AC-G01 (event-driven)
GIVEN a frozen, SHA-pinned Arm-1 holdout stratified across easy/medium/hard mission difficulty, drawn from instrumented clarification_request traces plus a frozen synthetic set, size N ≥ K
WHEN Arm 1 (capability-expansion) runs
THEN TBD_NAME SHALL meet or exceed the precedent-pegged verified-completion RATE floor over the holdout
VERIFY: verified-completion rate ≥ the frozen floor X% (X human-owned, OQ-1); baseline rate is 0

### AC-G02 (state-driven)
GIVEN the Arm-1 holdout evaluated across three independent runs/seeds
THEN the verified-completion rate SHALL clear the floor on each of the three runs (repetition discipline, Kupo pass³ precedent)
VERIFY: 3/3 runs report a rate ≥ the frozen floor

### AC-G03 (unwanted-behavior)
GIVEN the Arm-2 corpus with each prompt owner-labeled before replay by an independent labeler, the label file SHA-frozen alongside the corpus
IF TBD_NAME fires on any prompt whose blind-frozen label is a specialist
THEN the `shipped` flip SHALL be blocked
VERIFY: over-capture count equals 0 on the strongly-matched (≥ τ) subset against the frozen label key (hard MUST bar)

### AC-G04 (state-driven)
GIVEN the Arm-2 near-threshold subset (specialist top-score in `[τ−0.15, τ)`) plus paraphrase-perturbed specialist prompts plus the P1..P11 class
THEN generalist capture on this subset SHALL be within the frozen non-inferiority bound
VERIFY: measured capture rate on the near-threshold denominator ≤ the frozen bound (numeric OQ-1)

### AC-G05 (unwanted-behavior)
GIVEN the measurement-gate attestation
IF the gate author identity equals the gate checker identity
THEN the gate verdict SHALL be rejected
VERIFY: maker≠checker attestation on the gate record

### AC-G06 (unwanted-behavior)
GIVEN the two arms
IF either arm fails its pass criterion
THEN `status` SHALL remain `in_construction`
VERIFY: rollout gate holds status until both arms report pass

### AC-G07 (unwanted-behavior)
GIVEN the Arm-2 owner-labels
IF a label is assigned by the maker or the generalist-builder, or after the replay outcome is known
THEN the labeling SHALL be rejected as non-independent
VERIFY: labeler ≠ maker and ≠ generalist-builder, labels SHA-frozen pre-replay per a written rubric, disagreements adjudicated by a second blind labeler

---

## Track H — Rollout sequencing + checker gates

### AC-H01 (unwanted-behavior)
GIVEN `requires_checker: true`
IF a `shipped` flip is attempted without a recorded human go/no-go
THEN the release SHALL be blocked
VERIFY: change.json carries `requires_checker:true`; a human go/no-go attestation is required before the flip

### AC-H02 (unwanted-behavior)
GIVEN the shipped roster
IF the placeholder token `TBD_NAME` still appears
THEN the release SHALL be blocked
VERIFY: `grep -R 'TBD_NAME' roster/ EIDOLONS.md` returns no matches at ship time

### AC-H03 (event-driven)
GIVEN the rollout PR chain
WHEN it proceeds
THEN Phase 1 (schema + in_construction row) SHALL land before Phase 5 (shipped flip)
VERIFY: commit/PR ordering shows the enum+row commit precedes the shipped-flip commit

### AC-H04 (unwanted-behavior)
GIVEN the five reversal-conditions, with frequency-null redefined as "Arm-1 stratified coverage-rate below its floor"
IF one triggers
THEN the `shipped` flip SHALL be blocked
VERIFY: each reversal item resolves to a mechanically-evaluable signal (Arm-1 rate, Arm-2 bound, host-slot check, token-budget CI) in the pre-flip checklist

---

*RAMZA — acceptance criteria (EARS form, lintable, frozen at Assemble). Rev 3.*

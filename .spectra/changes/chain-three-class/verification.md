# Verification — `chain-three-class`

> ESL change `chain-three-class` · tier **full**
> Maker: `vivi` · Checker: `kupo` (maker ≠ checker, C4 — mechanically enforced)

## Checker round 1 — **REJECT**

An independent checker (distinct actor, clean context) was dispatched against
the shipped code at `f152c4c` and told explicitly what the maker was least sure
of. It rejected. Every finding was produced by *running* the kernel, not by
reading this record.

| # | Severity | Finding | Status |
|---|---|---|---|
| F1 | **BLOCKER** | The ambiguity pin used set-**equality** where the kernel uses **subset**; the expected list was generated with the same expression, so both sides were wrong identically. True count **5**, not 7. Parent was **6**, not 9. Two pinned "ties" are provably resolved by `plan-before-build`. | **FIXED** |
| F2 | none | The stable-sort premise is **real** — verified by reordering templates in a live roster and observing routes flip. The 4-class entry is load-bearing exactly as claimed. | confirmed |
| F3 | **MAJOR** | The pin was blind to a **duplicate class set**: a rival template with identical `requires_classes` kept all six assertions green while the live route flipped to `idg>kupo`. | **FIXED** |
| F4 | **MAJOR** | Two undisclosed step-drops introduced by the change: **scriber** dropped (`scout-diagnose-plan-fix` lacked the trailing `idg` that `plan-before-build` carries) and **reasoner** dropped on a reasoner+debugger+planner+coder prompt. | 1 **FIXED**, 1 **DISCLOSED** |
| F5 | minor | AC-5 overstated: 4 mutations recorded for 6 tests; the "routes unchanged" test has no falsifying mutation. CHANGELOG claimed "all six assertions were falsified". | **CORRECTED** |
| F6 | none | `files_touched` matches the merged diff exactly. | confirmed |

The checker's summary is worth preserving verbatim in substance: *the routing
data and behaviour are correct and need no re-release; what fails is the
record* — a false safety invariant, propagated into a shipped CHANGELOG and a
methodology doc, certified by a test whose expected side was generated by the
same wrong expression that computed the actual side.

## Remediation round 1

- **Coverage test corrected to subset semantics** in `cli/tests/routing_chains.bats`:
  a pair is resolved iff some `C` has `C.req ⊆ (A ∪ B)` **and** `|C| > |A|`.
  This closes F1 and F3 with one fix — duplicate class sets now surface as
  unresolved automatically, because no strict superset can exist inside their
  own union.
- **Pinned set corrected 7 → 5**, and the newly-visible fact recorded: this
  change *resolved* a pre-existing tie (`ship-fast` vs `forensic-then-fix`),
  moving the set 6 → 5.
- **`scout-diagnose-plan-fix` now ends in `idg`**, restoring the scriber step
  that `plan-before-build` carried. New test: *"the widest pipeline does not
  drop the scriber step"*.
- New regression test for the duplicate-class-set case.
- Corrections propagated to `roster/routing.yaml`,
  `methodology/cortex/chain-templates.md`, and `CHANGELOG.md` (the v2.19.0
  entry annotated in place, not rewritten).

## Checker round 2 — **REJECT**

The same checker re-verified and rejected again. The engineering was closed —
it validated the subset fix against its own independent all-subsets model and a
**250-roster fuzz with 0 disagreements**, stronger evidence than this change
ever had for anything — but **the record still disagreed with the tree**:

| # | Finding | Status |
|---|---|---|
| R-F3 | The new duplicate-class-set test re-implemented the predicate **inline** instead of calling the helper, so it asserted a property of its own frozen copy. Proven inert: reverting the helper to the equality form left it green. Its comment claimed to be "the falsifying case for the pin itself"; it was not. | **FIXED** |
| R-F4 | The reasoner-drop disclosure cited a prompt that **no longer reproduced** — round-1's `read_only_question` had matched `"is the"` inside "why **is the** deploy broken", so it dispatched to `vigil` alone. The record's own witness demonstrated something worse than the text claimed. | **FIXED** (witness replaced) |
| R-F5 / R-DOCS | The false count survived at **five** sites, including a self-contradiction inside `acceptance-criteria.md` (5 on one line, 7 on another) and the CHANGELOG sentence reading "all six assertions" beside four mutations. `spec.yaml`'s evidence block was stale in five places and its `safety_invariant` still claimed routes were unchanged. | **FIXED** |

Round 2's lesson, distinct from round 1's: a *correct fix* and a *correct
record* are separate deliverables, and this change kept shipping the first
while failing the second. The engineering was right from the remediation
onward; the ledger describing it was wrong three times.

## Remediation round 2

- Test 6 now points `EIDOLONS_ROOT` at a doctored roster and **calls**
  `_actual_ambiguous_pairs` — exactly one copy of the predicate exists in the
  file. Verified by reverting the helper to the equality form and watching the
  test go **RED** (it stayed green before).
- Reasoner-drop witness replaced with one that reproduces, plus an explicit
  note that form-gating does **not** rehabilitate the original prompt.
- Full repo sweep of the false count and every stale figure; `spec.yaml`'s
  `safety_invariant` and `acceptance-criteria.md`'s AC-3 rewritten to state the
  disclosed reasoner drop rather than claim routes are unchanged.

## Evidence after remediation

| AC | Result | Evidence |
|---|---|---|
| AC-1 | **PASS** | `N-C01..N-C03` → `[vigil, ramza, vivi]` |
| AC-2 | **PASS** | `N-C04` → `[atlas, vigil, ramza, vivi, idg]` via `scout-diagnose-plan-fix` |
| AC-3 | **PASS** | `public` 15/15; exhaustive 57-subset differential — only supersets of `{coder,debugger,planner}` move |
| AC-4 | **PASS (corrected)** | unresolved set = exactly the **5** remaining pairs, by subset test |
| AC-5 | **PASS (scoped)** | 5 mutations red incl. the duplicate-class-set case; the "routes unchanged" test's lack of a mutation is now stated, not implied |
| AC-6 | **PASS** | every step exists in `roster/routing.yaml` and `roster/index.yaml` |
| AC-7 | **PASS** | `make lint` 0 · `make schema` 0 · token budget 836/850 · self-test 88 tasks · recall-mutation gate **8.2 %** vs `v2.17.0` |
| AC-8 | **PASS** | `bats cli/tests/` → **1712/1712**, 0 failures, 7 skipped (counted against the plan) |

`routing_chains.bats`: **8/8**. Recall suite: **69/69**.

## Residual — disclosed, not fixed

- **Reasoner drop.** Witness that reproduces on the shipped tree:
  `"compare the two designs, diagnose the flaky test, spec the fix and implement it"`
  → `diagnose-then-plan-then-fix` (`vigil → ramza → vivi`), FORGE dropped;
  parent `13013ae` gave `decide-then-implement` (`forge → ramza → vivi`).
  Root-causing before deliberating is defensible when a live failure is
  present, so this is accepted — but it is the same class of step-drop the
  change removes, and is recorded rather than left for a later checker.

  *An earlier revision of this bullet cited
  `"why is the deploy broken and what should we build instead"`. A checker
  found that prompt no longer demonstrates what the bullet claims — the
  round-1 `read_only_question` signal matched `"is the"` inside "why **is
  the** deploy broken" and knocked the coder to 0.30, so it dispatched to
  `vigil` alone with no chain at all. The cited witness was showing something
  worse than the text described, so it is replaced with one that reproduces.
  Note that form-gating does NOT bring the original prompt back: it is itself
  interrogative ("why …"), so `requires_question_form` still admits the signal
  and the coder is still suppressed — re-measured on this tree, it dispatches
  to `vigil` alone. Do not restore it as the witness.*
- **`scout+debugger+coder`** still selects `forensic-then-fix`, dropping the
  scout step.
- **Five order-dependent ties remain** among the 2-class templates.
- **The "routes unchanged" assertion has no falsifying mutation.**

## What this round cost, and why it was worth it

The change shipped as v2.19.0 with a green suite, a mutation-falsified test
set, and a written claim that its own invariant had been audited. All three
were true, and the invariant was still wrong — because the audit reused the
instrument it was auditing. Nothing short of a distinct actor re-deriving the
property from the kernel would have caught it.

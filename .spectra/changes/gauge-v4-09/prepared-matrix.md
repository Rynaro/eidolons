# V4-09 independent checker preparation

Status: PREPARATION ONLY. No implementation, product test, host probe, model call, credential inspection, network access, or comparative trial ran. This is a spec-derived review matrix, not an acceptance receipt. All implementation checks below are NOT RUN. Source is read-only; review artifacts live only in this scratch directory. CRYSTALIUM is unavailable.

Routing: selected VIGIL, standard tier, checker role, composition edge to orchestrator. Authority: read-only source plus explicitly authorized scratch artifacts. VIGIL's reproduction and counterfactual requirements apply if an actual defect is observed later; none is attributed here. Maker and checker must remain distinct.

## Inputs and gates

Read canonical `EIDOLONS.md`, installed `.eidolons/vigil/agent.md`, `SPEC.md`, `skills/verify.md`, and `skills/verify-incoming.md`. The five supplied artifacts had no sibling `.envelope.json`. No mutable V4-07 source, future V4-09 implementation, maker conversation, or maker tests were inspected. The installed FORGE ECL schema was read solely to format this handoff.

Input paths are relative to `/private/tmp`; SHA-256 values identify preparation inputs:

| Input | SHA-256 |
|---|---|
| gauge-v4-09-review-mission.md | 1b5d1fe370b944b2502da144c8e90eddd274c3ce271fdcc1152b496ba309c8ef |
| gauge-v4-09-spec-draft.md | 0bce0c10e5ccff81cbe60096b3fccdba931c6f18bdd1bfed82516e0fe08113e0 |
| gauge-v4-09-decision.md | 34be27fce6e5d7c5559d4a4b4a23137accf2482254db3ddb047614ad082165d3 |
| gauge-v4-09-integration-scout.md | 04af687cb3a973a2b57d40ac44556d2d706d075890a0f01fd53c7fe5bef56384 |
| gauge-v4-09-codex-discovery.md | 84a3376a51ab2f7a44453dab2e7465f8725e4f89fcc19b2276bc038f86cd81f2 |

Start actual review only after orchestrator GO, accepted V4-08, and an identified frozen V4-09 source plus frozen spec/decision/anchors. Confirm exact revision and scoped fixture authority then; do not infer acceptance from this draft. Preserve accepted V4-06 authoritative store, V4-07 authorization/policy boundaries, and V4-08 root resource accounting. Production live authorization intentionally remains unqualified; file records, `approved:true`, and supplied timestamps cannot grant it. Review can establish instrument code and fixture mechanics while explicitly leaving live qualification blocked.

## Independent matrix

All rows require a narrow positive control as well as rejection evidence so an implementation that rejects everything cannot pass. Fixture success means genuine injected fixture authorization and fake transports; it grants no actual host qualification. Capture total transport calls across every adapter, separately including API transport calls. Never instantiate an actual live transport for these probes.

| Anchor | Independent fixture/input | Expected oracle and positive control |
|---|---|---|
| V4-09-T01 / R01 | Record two otherwise identical synthetic configurations with distinct control granularity; enumerate session, turn, request, tool, process. Vary required capability evidence independently of registration/help labels. | Exact tested tuple and capability granularity remain visible; narrower evidence cannot establish broader execution or caps. A complete fake tuple qualifies only its fixture scope. Registration/help/catalogue presence alone never qualifies live execution. |
| V4-09-T02 / R02 | Starting from complete fixture capability and billing evidence, remove cancellation, child visibility, enforcement, then billing eligibility one at a time. Keep technical capability, maturity, and billing permission independent. | Every missing requirement rejects before dispatch and all transports remain at zero. Restore the missing field under genuine fixture authorization to admit the exact fixture request. Complete technical evidence with unverified billing still rejects. An explicitly authorized API fixture is a separate positive configuration. |
| V4-09-T03 / R03 | Subscription allowance exhausted, fixture credentials marked present, API capability otherwise available, no distinct API allowance. Repeat with a fake adapter proposing fallback and with API allowance for a different tuple. | No subscription-to-API switch: API requests = 0 and all dispatch transports = 0. Credentials, adapter fallback, and sibling allowance cannot authorize. Genuine exact API fixture authorization admits only that API configuration and increments only its fake counter. |
| V4-09-T04 / R04 | Same immutable request/event stream with shadow off/on; success, rejected request, observer exception/failure, and observer attempted mutation variants. | Route inputs and selected route, complete prompt, tools, model, settings, billing mode, and ordered execution requests are deeply identical off/on. Observation artifacts alone may differ. Observer failure adds no dispatch, reroute, retry, spend, or fallback. No equal-runtime/zero-overhead claim. |
| V4-09-T05 / R05 | Freeze one complete synthetic protocol through controller store. Try dispatch/outcome before freeze, forged old timestamp, `approved:true`, absent/wrong protocol identity, and changed referenced manifest. Reopen store before continuing. | Only a genuinely persisted synthetic freeze admits subsequent matching fixture events. Controller-local order demonstrates freeze before dispatch and outcome admission after reopen; caller dates are insufficient. Live allowance/prospective/evaluator authorization remains blocked even when synthetic freeze works. Immutable identity covers the full field set below. |
| V4-09-T06 / R06 | Successful fake capability probe; help/version discovery; registration/readiness receipt labelled live/passed; complete fake evidence with no actual authorized live probe. | Each remains fixture/discovery evidence and live criterion is blocked with missing evidence identified. Supplied codex-cli 0.154.0 discovery is only interface discovery. Separate explicit fixture success is preserved; no fake host, receipt label, or evidence string self-promotes to live qualification. |
| V4-09-T07 / R07 | Feed native and original-v3 synthetic arms the same all-attempt vectors below; terminal failure/cancellation/abandonment, retries, duplicate acceptance, unknown cost. Duplicate an identical immutable attempt; then reuse its identity with changed content. Reopen store. | Same versioned record shape and arithmetic for both arms. Execution termination is independent from acceptance evidence. Every attempt survives with original task root, even failure or exclusion; repeated acceptance deduplicates within its trial/protocol/arm/task identity. Identical retry is idempotent; conflicting immutable record rejects without replacing history, before/after reopen. Unknown costs never become zero. |
| V4-09-T08 / R08 | Sandboxed versus privileged sibling method; preview/experimental versus stable maturity; change effective execution boundary independently of method label. | Support maturity and effective boundary remain explicit. No sibling inheritance or maturity/billing/capability conflation. Fully pinned sandbox fixture retains eligibility while unqualified privileged sibling rejects with zero transport calls. |
| V4-09-T09 / R09 | Two independently pinned fixture tuples A/B; mutate A's host version, integration/protocol, mode, method, permissions, boundary, granularity, maturity, evidence, and billing binding one at a time before dispatch. | A's affected evidence is invalidated/revalidated before any new dispatch and cannot be reused under its old identity. Unaffected pinned B remains usable under fixture authorization. An unchanged brand or caller-supplied old qualification digest never masks drift. |
| V4-09-T10 / R10 | Freeze matched arms, then individually alter CPU, memory, network, dependencies, cache state, timeout, resource coverage, task split/stratum, acceptance definition, evaluator/holdout owner, or control/baseline identity. | Mismatch rejects comparison or is accounted for in a previously declared explicit stratum; no hidden pooling or post-hoc relabeling. Equal frozen arms remain comparable as synthetic fixtures. Changed manifests require a new immutable protocol/trial identity. |
| V4-09-T11 / R11 | Structural arm with prerequisite V4-10 absent; managed arm before V4-15; missing native/v3 qualified configuration; available genuine fixture-authorized native/v3 arms. | Ineligible arms produce eligibility records and zero fabricated attempts or calls. Structural before V4-10 and managed before V4-15 stay ineligible. Available native/v3 fixtures can run under genuine fixture authorization; live controls stay blocked without real qualification and authorization. |

## Complete identity and ordering adversaries

Begin with one valid synthetic frozen protocol. Apply one-field mutations independently, restoring the positive fixture between probes. Identity must cover protocol version, arm definitions, untouched native identity, original-v3 code baseline, task manifests/strata/splits, evaluator and holdout ownership, terminal/outcome/acceptance definitions, resource coverage, CPU/memory/network/dependencies/cache/timeouts, exclusions/censoring/stratification rules, margins, allocation/sample rules, stopping/statistical rules, mode, allowance and operator authorization reference. Referenced immutable manifest contents are included, not merely mutable paths or caller assertions. Synthetic scientific values are explicitly fixture inputs, never recommendations or claimed live parameters.

Original-v3 identity is exactly `752194ef5ceaa8cee1f5995fd0d374888696d8fc`. Native is a distinct untouched control. Prior H-WIN prompt-changing harness arms cannot acquire either label by renaming. Wrong commit, changed instructions/cache/control, and structurally different arm with the same display label must fail identity/comparability checks.

Probe prefreeze dispatch and outcome independently; freeze after a prior outcome cannot retrospectively legalize it. Assert freeze admission, dispatch admission, and outcome admission ordering from the controller's durable sequence, not caller wall-clock values. After store reopen, replaying a matching immutable freeze/event is either documented idempotent behavior or explicit duplicate rejection; conflicting freeze/event must never overwrite admitted history. A new valid protocol uses a new identity and cannot attach old outcomes by relabeling. Local persistence ordering does not establish that humans never saw external outcomes: prospective live/evaluator-access claims remain unqualified.

## Independent accounting oracle (predeclared; not executed)

These are pure arithmetic fixture vectors, not product test results or benchmark observations. Use exact integer units in a single declared synthetic currency/unit/window scope.

| Vector | Attempts and accepted distinct task roots | Expected result |
|---|---|---|
| K | task A failed cost 2; task A accepted cost 3; task B cancelled cost 1; task B accepted cost 4; replay acceptance for task A | Attempt count 4; known complete total 10; distinct accepted task roots 2; cost per accepted task 5. Duplicate acceptance leaves denominator 2. |
| Z | task A failed cost 2; task A cancelled cost 1; task B abandoned cost 3; no acceptance | Attempt count 3; complete total 6; accepted roots 0; ratio undefined, never zero or infinity. |
| U | task A failed known cost 2; task A accepted unknown cost; task B abandoned known cost 3 | Attempt count 3; known subtotal 5; total unknown; ratio unknown even though accepted roots are observed. Unknown attempt remains present. |
| W | arm/stratum group 1 total 2, accepted roots 1; group 2 total 18, accepted roots 3, compatible scopes | Combined ratio 20 / 4 = 5; never unweighted mean (2 + 6) / 2 = 4. Cross-arm comparison still preserves arm identity. |

Check estimated resources remain distinguishable from known and unknown values; do not prescribe a derived numeric complete total where policy marks it estimated. Incompatible currency/unit/window or coverage is rejected or shown separately, never pooled as if comparable. Failed, cancelled, abandoned, censored, and excluded attempts remain immutable audit records; frozen analysis rules determine any separately disclosed analytical exclusions. No unobserved cost becomes zero. Acceptance must be independently evidenced; successful process termination alone is not acceptance. Attempt, invocation, configuration, environment, trial, protocol, arm, task root, timestamps, terminal status, acceptance/evidence, resources/coverage, and censor/exclusion fields must persist under the same schema for both synthetic arms.

## Later execution and evidence rules

At GO, inspect only the frozen source/spec/decision/tests and accepted predecessor contracts provided by the orchestrator. Locate all eleven named anchors before execution; bind each to independent probes above and report absent coverage as a gap. Use the qualified isolated Linux fixture environment for checker runs; label native macOS root checks separately. Run full Go regression and verify local CI integration as applicable. Hosted CI is pending/blocked until explicit publication approval; no publication retry, live host/model call, or credential inspection is authorized here.

For each actual failure: retain two consistent deterministic reproductions, bounded competing hypotheses and at most five counterfactual interventions under authorized scratch isolation; localize actionable findings to frozen source path and lines, with narrow passing controls. If reproducibility fails, label the uncertainty. Do not manufacture findings from absent live authorization, which is an intentional boundary. Do not edit protected tests, source, or accepted history. Preserve raw failure and pass logs and state the exercised environment precisely.

Final review must separate instrument-code/fixture acceptance, actual live host qualification (blocked), live protocol authorization/prospective boundary (unqualified), comparative trials (not run), and performance (unmeasured). An ECL handoff uses a relative artifact path and SHA-256. This preparation grants no approval to start; await root GO after accepted V4-08 and frozen V4-09.

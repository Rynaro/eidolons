# V4-08 Go seams — V4-06 snapshot, requires V4-07 re-anchor

Static ATLAS preparation against accepted commit `aa155c8738209f1672cc3fc663b0019740ba54a6` in `/private/tmp/eidolons-gauge-v4-06`, read through `git show`/commit-scoped searches only. No mutable V4-07 source inspected. No code changes, tests, runtime probes, network/model calls or private data. CRYSTALIUM unavailable. Only this requested scratch report was written. H means direct source/contract evidence; all relative source anchors below resolve under the absolute frozen checkout above **at aa155c8**, not its future working state.

Fixed requirement/decision authority: `/private/tmp/gauge-v4-08-spec-draft.md:15–24,39–57,62`; existing legacy map: `/private/tmp/gauge-v4-08-integration-scout.md`. This report maps existing interfaces and gaps; it proposes no replacement reconciliation, authority or persistence design. **Re-anchor every schema, typed policy/binding, transaction and public mutator seam against accepted V4-07 before V4-08 implementation.** The snapshot here still has base schema1 (`gauge/internal/store/store.go:47–55`), generic policy records, and no V4-07 APIs.

## Identity, root creation and replacement

| Actual interface (H) | Safe reuse / V4-08 gap |
| --- | --- |
| `contract.Binding` — `gauge/internal/contract/types.go:15–27`; `Binding.Validate() error` — `:29–35` | Separate Root, Assignment, Profile, Method, Worker, Invocation, Context, Authority, Candidate, Receipt, Environment strings. Invocation is only a field, **not** a standalone durable Invocation type/registry. Validation requires nonempty values; it does not prove ancestry, enforce bounded lengths or check cross-root identity ownership. |
| `contract.Root` and `Root.Validate() error` — `types.go:71–88` | Root holds current Binding/Manifest plus PolicyRefs, Intents, Evidence. No route/task/parent/causal-event/descendant membership fields or invocation history. Root.Validate checks ID syntax, staged/active phase and nonempty generation/inventory only. |
| `controller.New(project string,opts Options) *Service`; `Options{Timeout,Clock,IDs,Adapter,Cut}` — `controller.go:33–67` | Injected clock/IDs/fixture adapter support deterministic fixtures. Default IDs is `crypto/rand.Text`, not a UUID allocator. Project path is canonicalized. No lineage resolver or resume API in Options. |
| `(*Service).newRoot(id,inventory string,imported bool) contract.Root` — `controller.go:76–86` | Root ID is supplied by caller; IDs() allocates generation/assignment/worker/invocation/context, not the root. Binding.Authority equals writer generation, not resource-pool/operator/policy authority. Imported harness/model/method provenance remains unknown. |
| `(*Service).InitRoot(id string) error` — `controller.go:89–136` | Caller supplies root ID. Existing root is validated through active() and returned unchanged; absent root is staged/promoted. This is idempotent initialization, **not explicit resume**: using it to resolve an unknown resume ID would create a root. No fresh-task/route separation contract exists here. |
| `(*Service).Replace(id,worker,environment string,reconstruct bool) error` — `controller.go:325–363` | Fixture-only. Worker replacement changes Worker/Invocation/Context; environment replacement changes Binding/Manifest environment; root and durable refs survive. Imported roots are refused even if adapter labels look like fixture. It overwrites current binding, records no previous-invocation or successor edge. Environment-only replacement does not allocate a new invocation in this code. |

The selected fresh UUID per new task, distinct route digest, explicit existing-root resume and validated descendant/reviewer/retry/successor inheritance are additions, not guarantees of these APIs (draft `:41,53`; source anchors above, H). Preserve accepted root across role/provider/context/environment changes; do not infer ancestry from route equality or reuse mutable current Binding to label late historical observations.

## Configuration and causal observation surface

- `contract.Manifest` carries Version, RequestedModel, ObservedModel, Harness, Adapter, Methods, Environment, PolicyRefs. `Manifest.Identity() (string,error)` validates selected nonempty fields/version and hashes its JSON representation; `contract.Digest([]byte) string` is SHA256. H: `gauge/internal/contract/types.go:38–65`. Reuse requested-versus-observed distinction and content hash helper. It has no separately persisted immutable configuration-version registry, timestamp, route or causal parent. Methods/refs array order participates in its hash; this is not a general V4-08 stream/coverage canonicalizer.
- `Adapter.Observe(context.Context,contract.FixtureInput) (contract.ObservationResult,error)` accepts only pass/fail/cancelled fixture input; result contains Outcome, ObservedModel and ModelCalls. H: `controller.go:21–30`; `types.go:112–127`. There is no native usage collector, pricing/allowance provider or token/transfer/environment/human measurement interface. `ModelCalls:0` qualifies this fixture path's no-model-call behavior, not universal zero consumption or complete resource coverage.
- `(*Service).ExecuteFixture(ctx context.Context,id,eventID string,in contract.FixtureInput) (contract.Receipt,error)` hashes input plus **current** Manifest identity, checks existing receipt identity, invokes adapter outside the DB transaction, then transactionally writes receipt/replay marker/current root receipt/evidence. H: `controller.go:237–320`. `Receipt` persists root, event ID, outcome, observed model, model-call count, one clock timestamp, fixture-only grade, accepted=false and input digest (`types.go:129–139`; `controller.go:294`). It does not persist assignment/invocation/configuration ID/candidate/intent/causal parent links. The manifest enters an opaque digest, not a recoverable historical causal binding.
- Existing fixture retry compares the current manifest-based digest before returning the stored receipt (`controller.go:254–278`). After environment/configuration changes, an old event ID can conflict even when the caller repeats the old fixture input. Therefore this replay path is not evidence that V4-08 late observations remain attributed to their original invocation/configuration. H.

## Persistence and pure reduction

| Actual API (H) | Contract relevant to observations |
| --- | --- |
| `store.Inspect(path string,timeout time.Duration) (Snapshot,error)`; `store.Open(path string,timeout time.Duration) (*Store,error)` — `gauge/internal/store/store.go:131–170` | Read-only inspection followed by writable-open schema recheck; no auto-create on read. Snapshot contains StoreID, Roots, Legacy, Records. |
| `(*Store).Snapshot() (Snapshot,error)` — `:173–230` | Copies/parses records in a bbolt View transaction; useful captured input boundary. No causal ordering, observation query/index or stable stream ordering supplied; record iteration is not observation-time/causal sorting. Operations are omitted. |
| `(*Store).Update(id,generation string,fn func(*Tx) error) error` — `:320–333` | Root-scoped dependent-state atomicity, active phase/generation guard. New root-linked metadata must keep this transaction boundary plus controller guards below. |
| `(*Tx).Root() contract.Root`; `(*Tx).SetRoot(r contract.Root) error` — `:336–350` | Detached transaction-current root; SetRoot prevents changing ID/generation/inventory/phase. Preserve unrelated references using current transaction state. No lineage invariant validator. |
| `(*Tx).Put(r contract.Record) error` — `:353–375` | Immutable family/root record ID; identical replay allowed, changed content conflicts. It compares parsed Record plus RawMessage with reflect.DeepEqual; not a general canonical semantic equality contract. Root/family-scoped IDs alone do not supply observation namespace, stream identity or correction graph. |
| `(*Tx).Once(id string,payload []byte) (bool,error)` — `:380–400` | Per-root operation payload SHA256 marker, committed/rolled back with dependent writes. No durable result getter, global observation dedup, correction/fork/cycle validation or coverage reconciliation. |

`contract.Record{ID,Family,Kind,Body json.RawMessage}` accepts history/receipt or history/event, context/context-reference, knowledge/note, policy/policy. Validation is family/kind plus valid JSON, not metadata allowlisting (`types.go:91–103`). These are generic storage primitives; no typed resource observation/correction/stream/coverage/retention record exists in this snapshot. H. Do not interpret availability of history/event as an implemented V4-08 collector.

There is an existing pure function, `contract.Project(p ProjectionInput) Projection`, in `gauge/internal/contract/projection.go:52–114`. Its `Observation` type (`:13–22`) means **completion-check evidence** with candidate identity, check/oracle/outcome and maker/checker invocation strings. It picks greatest Sequence per check, reports self-attested provenance and never qualifies acceptance. H. Reuse only the structural separation between captured inputs and pure derived output; its latest-sequence rule, free-form identity maps and evidence labels are not the chosen consumption reducer. No delta/cumulative/epoch/correction/inclusive-parent reconciliation exists there (`:68–96`). Do not conflate the existing type name with V4-08 usage observations or extend this into protected-oracle authority.

The fixed decision already supplies explicit consumption identity, coverage frontier, pending corrections, late-covered-prefix invalidation/replacement and independent totals (draft `:39–57`). The store/reducer map above supplies integration points, not additional arithmetic decisions. New reduction must not derive coverage from receipt timestamps, maximum counters, current Binding, map iteration or latest completion-check Sequence.

## Privacy, export and retention

- `(*Service).Status(id string) (Status,error)` returns **all** root Records plus imported Legacy events (`controller.go:203–234`). `LegacyEvent.Raw []byte` retains original source bytes (`types.go:106–110`). CLI JSON-encodes the entire returned result (`gauge/cmd/eidolons-gauge/main.go:100–118`). H. This raw historical view is not a V4-08 allowlisted export; arbitrary Record.Body and legacy bytes cannot be forwarded as managed observation metadata merely because they are inside the controller DB.
- No production export or retention CLI command exists: supported commands are init/import/promote/recover/status/fixture/replace (`main.go:39–42`), with direct error printing (`:120–124`). `gauge/internal/store/export_test.go:1–6` is only a test-build `BackendForTest() *bolt.DB` accessor, not an exporter. H.
- The existing public Tx API has Put/Once/SetRoot but no deletion/redaction/compaction/retention operation (`store.go:315–400`). H. The selected retention promise—eligible detail removed while totals/lineage/exposure survive and lost coverage becomes explicit—is wholly unimplemented here (draft `:49`). Deleting roots, operations or historical evidence is not a supported substitute.
- Neither Binding/Record validation nor the CLI error output constitutes the selected bounded metadata allowlist (`types.go:29–35,98–103`; `main.go:120–124`). New storage/errors/exports require the same typed boundary; secret canaries must include supplied identity/model/diagnostic fields as well as forbidden payload bodies (draft `:49`). This observation does not propose changing legacy historical preservation.

## Inherited guards and compatibility anchors

- Single shared controller DB path is `.eidolons/.ledger/.gauge-controller-v1/state.db`; per-root persistent claims are adjacent `authority-<id>.json` (`gauge/internal/controller/authority.go:17–46`). Base schema1 required buckets and exact version check are at `store.go:19–20,47–72`. H. Re-anchor schema/migration/typed-policy APIs after V4-07; do not open a second observation DB or assume V4-06 can read the successor schema.
- Root mutators follow `lock → active → store.Open → Store.Update` (`controller.go:245–253,299–318,332–363`). `lock(id string,create bool) (func(),error)` uses bounded per-root append ownership and refuses ambiguous takeover (`authority.go:194–238`). `active(id string) (store.Snapshot,contract.Root,error)` checks persistent pending claim, active run marker, store/root/generation/inventory agreement and current legacy inventory (`authority.go:241–298`; `controller.go:182–200`). Store.Update alone checks only phase/generation/schema. H. Every new root-linked observation/lineage/retention mutator must preserve this composition; read-only current-root inspection also checks unresolved append ownership (`controller.go:212–234`).
- Preserve deterministic ID role and manifest identity tests: `gauge/internal/contract/contracts_test.go:14–22,36–44`; pure completion parity/non-acceptance: `:46–63`. H.
- Preserve fixture exact replay/conflict/no-model-call/clock/unknown-model assertions: `gauge/internal/controller/conformance_test.go:105–115`; worker/environment replacement retains root/policy/intents/evidence/candidate references and refused reconstruction changes nothing: `:118–127`. H.
- Preserve transaction rollback/concurrency/bounded lock and process-kill replay definitions: `gauge/internal/store/store_test.go:49–90`. These definitions were read, not executed. Their scope is local process interruption, not power-loss/network-filesystem qualification (`:70`). H.
- Bash `cli/src/gauge.sh:1–10` remains optional compiled-binary dispatch with explicit failure and no legacy write fallback. H. Preserve opt-out shell behavior and the legacy compatibility fixtures already listed in `/private/tmp/gauge-v4-08-integration-scout.md`; none establishes managed coverage or consumption arithmetic.

→ Orchestrator/spec handoff: after V4-07 acceptance, re-anchor actual typed policy/configuration binding and schema/read/write APIs, then attach this map plus the fixed FORGE contract. Bounded open gaps are fresh-root/resume/lineage APIs; durable historical invocation/configuration references; typed causal resource observations; pure coverage/correction reducers; and allowlisted inspection/export/retention. No Go06 primitive currently closes those gaps. No implementation, live-host qualification or new architecture verdict is claimed.

# V4-08 policy seams — provisional V4-07 API delta

**NOT ACCEPTED; final V4-07 re-anchor required before implementation.** Inspected only production files in immutable `/private/tmp/v4-07-review-probes/harness` (module root, so paths below begin `internal/` or `cmd/`). Parent identifies this as first-repaired07 with two remaining bugs: empty Allowed/Billing omitted during serialization and negative underflow. Those defects were not retested here. No mutable07, checker tests, private08 vectors, executions, network or repository writes. CRYSTALIUM unavailable. Fixed08 scope remains `/private/tmp/gauge-v4-08-spec-draft.md:39–62`; baseline06 map is `/private/tmp/gauge-v4-08-go-seams.md`. H below means direct static source evidence, not behavioral acceptance.

| Delta from accepted06 | Actual public seam and V4-08 constraint (H) |
| --- | --- |
| Schema2 and explicit migration | Ordinary `store.Inspect(path string,timeout time.Duration) (Snapshot,error)` / `Open(path string,timeout time.Duration) (*Store,error)` remain (`internal/store/store.go:148,168`). `guard` returns migration_required for schema1, otherwise requires schema2 plus typed guard (`:79–86`). Base eight buckets remain; new required namespaces are `preferences`, `effective_policies`, `policy_bindings`, `amendment_results` (`internal/store/policy.go:15`). `meta.migration_receipt` must identify controller, version1, to2 and valid new-store0→2 or migration1→2 provenance; user/project documents and typed policies/bindings/results are checked (`:32–128`). Fresh Create initializes them in its DB transaction (`store.go:112–125`). |
| Predecessor validation | `store.InspectPredecessor(path string,timeout time.Duration) (Snapshot,error)` and `store.Migrate(path string,timeout time.Duration,validate func(Snapshot) error) error` (`internal/store/migration.go:15,91`) validate schema1, reject preexisting typed namespaces/receipt, check imported event identities/grades, operation digests and orphan family state (`:32–85`). Migration creates namespaces/receipt and schema2 atomically, then checks unchanged legacy snapshot (`:110–147`). Use controller `(*Service).Migrate() error` (`internal/controller/policy.go:105–190`) to retain sorted per-root append locks, layout identity, filesystem inventory and claim/marker checks. Raw store migration alone does not supply those external proofs. |
| Rootless typed preferences | Both Store and Service expose `ReadPreferences() (map[string]contract.PreferenceDocument,error)` and `UpdatePreferences(layer string,expected uint64,p contract.Preferences) (contract.PreferenceDocument,error)` (Store parameter is named replacement). Both layers are read in one View; validated normalized replacement performs revision CAS and increment in one Update (`internal/store/policy.go:165–210`; `internal/controller/policy.go:21–35`). Documents carry Version, Controller, Layer, Revision, Digest, Values (`internal/contract/policy.go:19–26`). Service validates the existing controller but needs no root; its reads use `preferencesStore()` which opens a writable DB handle (`controller/policy.go:15–43`), so do not assume read-only lock behavior. |
| Typed compile and historical lookups | Service `CompilePolicy(p contract.PolicyPatch) (contract.Policy,error)` delegates Store `Compile(patch contract.PolicyPatch) (contract.Policy,error)`; compilation is a View with no grant, not persistence (`controller/policy.go:37–43`; `store/policy.go:435–445`). Store lookups: `ReadPolicy(id string) (contract.Policy,error)`, `ReadBinding(root string) (contract.PolicyBinding,error)`, `ReadAmendment(authorizer,id string) (contract.AmendmentResult,error)` (`store/policy.go:462–501`). Service wrappers: `ReadPolicy(id,policyID string)`, `ReadPolicyBinding(id string)`, `ReadAmendment(id,authorizationID string)`, with those respective return types; wrappers call Status and check root association where applicable (`controller/policy.go:63–103`). There is no public combined root+binding+policy transaction read. |
| Binding identity usable by08 | `Policy` now carries Controller, Root, Predecessor, Activation, preferences/requirements/permissions/billing/ceilings/registry/strategies/provenance; `Policy.Identity() string` hashes normalized JSON excluding ID (`internal/contract/policy.go:287–348`). `PolicyBinding{Version,Root,PolicyID,Predecessor,ActivePolicyID,Activation}` distinguishes latest bound policy from active policy (`:351–358`). `ReadBinding` errors if no07 binding (`store/policy.go:474–486`); migration does not invent one. Pending latest PolicyID must not be treated as ActivePolicyID. This supplies typed policy references, not08 historical invocation/configuration/causal lineage or resource observations. |
| Root transaction and authority composition | `Store.Update(id,generation string,fn func(*Tx) error) error`, `Tx.Root() contract.Root`, `Tx.SetRoot(r contract.Root) error` remain, now running schema2 typed guard; root authority fields still cannot change (`store.go:343–374`). Service `ApplyPolicy(id string,request contract.PolicyRequest) (contract.AmendmentResult,error)` composes append lock → active → Open → Store `ApplyPolicy(id,generation string,request contract.PolicyRequest)` (`controller/policy.go:45–62`). `active` still checks claim, marker, store/root/generation/inventory and revalidates inventory (`internal/controller/authority.go:269–298`). Store.Update/ApplyPolicy alone do not check filesystem proofs. Retain this composition for08 root mutators. |
| Replay and production boundary | Typed ApplyPolicy validates root/generation, retrieves exact historical result before comparing current predecessor, conflicts on root/digest/predecessor reuse, then atomically persists policy/result/binding (`store/policy.go:505–616`). Result key is controller-wide `["policy-amendment@1",authorizationID]`; no production constructor supplies private authorizer; fresh apply returns authorizer_boundary_unqualified (`:213–215,525–538`). This is policy amendment replay, not resource observation dedup or08 pool-alias authorization. |
| CLI init compatibility | Original `init [--root ID]` remains: Init always runs; InitRoot only with explicit root (`cmd/eidolons-gauge/main.go:93–102`). New policy commands dispatch before legacy command validation (`:53–58`); migrate/preferences/preferences-set/policy-compile are rootless; root-bound commands require root (`cmd/eidolons-gauge/policy.go:15–20,46–85`). An existing schema1 controller requires explicit migrate; init is not implicit migration. Missing resume IDs must not go through InitRoot's create behavior (06 baseline map). |

Inspected production file SHA256 (paths relative to the immutable module root above):

```
internal/store/store.go          4ff99fdb267ec92b43c4c48eb8ad58c9f88b2e900a8098e75cb38490269d1c98
internal/store/migration.go      ee472034744ff3bacb69c102c56c70a44c9ff15cdd76a473a553634a278b8253
internal/store/policy.go         5b672546aa10923cad9f1579bf36ebb73291515fb39c2e23808a45e95fd05265
internal/controller/policy.go    bae004f13d867bbc71faf46cc3501d3c049d712f67c753a42e83407c8d7b5819
internal/controller/authority.go 774f377b3c456f7e87c48ac19a2c86222ce2a94a3ccf3f86d7d078b797cdb436
internal/contract/policy.go       38b124243b88a356601076636bd67afbea02f607423baa2970429dc0dc633f04
cmd/eidolons-gauge/main.go        e1ab33d42a2f169c6053e048689f02be2b0126826582b3df6fa1d2302d52fd2f
cmd/eidolons-gauge/policy.go      1dab5e52c625ad7e7dd556183bc93cb76ff442e945509924eacfdb9502849016
```

→ Orchestrator: re-anchor the final accepted07 bytes, serialization/numeric validators, schema guards and all listed public methods before08 maker dispatch. No additional design, implementation or qualification is supplied by this provisional map.

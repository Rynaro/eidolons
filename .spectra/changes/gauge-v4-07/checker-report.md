# V4-07 distinct review — scoped local acceptance

**Verdict:** accept the frozen V4-07 local fixture/controller contract after repair of F01 and F02. No unresolved actionable implementation finding remains from this review. This is not hosted-CI, production authorization, account-wide coordination, live-worker revocation, release, publication or merge approval. Production activation remaining `authorizer_boundary_unqualified` is the binding design decision.

Checker `v4_07_review` is distinct from maker and both repair actors. Initial preparation used the canonical spec, actual FORGE binding decision and accepted06 seams before mutable07 source was read. The first review used a source freeze and independent tests. Subsequent repair verification used the same checker, its own findings, frozen repair artifacts and disclosed root status coordination; this is **not a new fresh-context reviewer**. Maker reasoning/conversations were not read. Existing test commands were executed and their harness definitions inspected only after independent probes were designed; no maker tests were used to invent the original defect probes. CRYSTALIUM unavailable. All reviewer files and harnesses are outside the candidate repository; candidate source was read-only throughout.

## Closed findings

**F01, empty permission/billing restrictions and authorization identity collision:** the unchanged independent regressions pass twice under `-race`. Persisted explicit `allowed:[]` and `billing:[]` remain explicit empty arrays, retain deny-all meaning after reload, and have distinct identities from omitted restrictions. An authorization bound to an explicit-empty patch now rejects substitution of omitted sets. Positive nonempty allow/deny/billing controls continue to pass. The localized production repair is `gauge/internal/contract/policy.go` using `omitzero` for Allowed/Billing.

**F02, negative-limit underflow:** both `-1` and `-1e-1000` now reject, twice, through `DecodePreferences`. `math.Signbit` retains the negative indication after float conversion. The frozen independent test file is byte-identical to the pre-repair handoff and to `gauge/internal/store/reviewer_regressions_test.go`; none of its assertions was weakened.

Original failure logs, report and envelopes remain in this directory. The prior numeric-identity probe's initial oracle was invalid because an empty user document contributed no field. That was corrected before handoff to provide a genuine preset contributor; the initial oracle failure is not a product defect. Revision 9007199254740993 and canonical policy identity survive persistence/reopen in the corrected repeated probe.

## Independent coverage and outcomes

| Contract area | Executed evidence and result |
| --- | --- |
| Preference provenance, precedence, rootless storage and strict input | Rootless Unicode/spaced projects work; controller-local labels appear; second DB stays independent; unknown source/version/enum and duplicate keys reject while previous preferences remain. Read-only inspection cannot create authority. Full replacement clears old restrictions; concurrent same-revision CAS has exactly one winner; failed write leaves prior revision and unrelated root/history snapshot intact. |
| Intersected authority | Actual fixture-authorized transaction retains task/project/account/window/concurrency bounds and a distinct USD bound; same-scope token bound applies minimum. Nonempty permission restrictions intersect, denies accumulate, billing narrows; missing designated permission authorizer rejects. Named R02/R03/R07 controls also pass. |
| Snapshot identity and amendments | Canonical identity survives reopen. Exact original authorization retry after a later amendment returns the original result while active policy remains later; retry survives reopen without new authorization. Cross-root ID reuse rejects. Snapshot/result/consumption/binding rollback cuts leave no result or active-binding leak. |
| Presets and strategy contract | Conserve/Balanced/Accelerate preserve required checks/criteria/grade, permissions, billing and ceilings. Optional strategy sets vary. Actual `SelectStrategy` receipt preserves selected ID, alternatives, registry/rule, parameters and supplied observable reason. Unregistered strategy, out-of-bound breadth, and proposals containing code/required/permissions/registry edits reject. |
| Genuine predecessor migration | Built real old06 CLI and seed helper from production bytes verified against `aa155c8738209f1672cc3fc663b0019740ba54a6`. Old CLI creates active roots and fixture history, imports/promotes a real jq-validated legacy event retaining formatted original bytes and ID. Old store APIs add generic JSON in all record families, policy refs and operation replay state. Complete recursive bbolt key/value dump before/after proves old namespaces are unchanged except base schema; only prescribed typed namespaces/receipt are added. All external claim/marker/legacy JSON hashes remain exact. Legacy policy does not become a typed active binding. |
| Migration refusal and interruption | New ordinary old-schema opens reject `migration_required` without changing DB bytes. Inventory drift, missing ownership marker and malformed old operation digest reject migration. Missing schema2 preference namespace/receipt, future base and future typed versions reject ordinary opening without recreation. Old06 binary rejects reading/writing schema2. Existing fixture exact retry preserves original status/history. Validator failure and transaction cuts preserve complete schema1. Real child processes killed at namespaces-written/version-written reopen as complete old state; killed after commit reopens as complete new state, twice. |
| CLI and ownership boundaries | Normal bind returns intentional `authorizer_boundary_unqualified`; missing root marker and changed imported inventory reject earlier in controller guards. Strict file import cannot carry source labels. Sending preferences-set output to `/dev/full` returns output failure while the committed canonical revision remains present; no rollback is falsely claimed. Help/inspection describe local-only layers. |

Static ordering inspection confirms controller `ApplyPolicy` holds the root lock and calls `active` before store mutation; store transaction validates current schema/root/generation and checks exact durable replay before today's predecessor CAS. Controller migration obtains sorted root locks, rechecks roots/controller identity under the shared DB write lock, verifies external proofs/inventory, then commits typed namespaces/receipt/schema together. Migration preserves historical generic policy refs rather than treating them as grants. Both layer reads occur inside the same bbolt transaction used by compilation.

## Commands, full logs and exits

All Linux execution used image `sha256:d9b4537412ed447276fbf3a745c3c953f254d412e70da57b5888831a0d61c795`, Go `go1.27.1 linux/arm64`, UID501:GID20, network none, read-only root/source, drop ALL, no-new-privileges, executable tmpfs, existing module cache read-only, `GOTOOLCHAIN=local` and `GOPROXY=off`. Exact commands are retained in scripts. The binaries were built from read-only source mounts; temporary state stayed outside source.

- `run-review.sh` → `independent-final.log`, `independent-final.exit=0`: unchanged repair regressions and paired controls twice with race detector, refreshed repaired production-only harness.
- `run-full.sh` → `full-final.log`: full repository Go race suite, `go vet`, unique named V4-07 T01–T08 anchors, controller/CLI/migration anchors, nonroot package-repeat, and **29 Bats compatibility tests** all passed. The final external policy.py stage initially lacked its required predecessor mount; overall `full-final.exit=1` records that setup error accurately, not a product failure or an all-zero composite run.
- `run-policy-conformance.sh` → `policy-conformance-complete.log`, exit 0: completed inherited real06 migration with the required read-only predecessor mount. The Bats runner deliberately fails an intermediate assertion despite a succeeding final command; expected exit1 was observed and wrapper succeeds.
- `run-completion.sh` → `completion-kill-final.log`, exit 0: independent real06 migration/dumps/proof preservation/CLI checks and **13 independent Go test groups twice**, including real-process migration kills before/after commit. Earlier successful completion without kill extension remains in `completion-final.log`, exit 0.
- `/opt/homebrew/bin/shellcheck --version` and `/opt/homebrew/bin/shellcheck -S error cli/src/gauge.sh scripts/gauge-build.sh` → `native-shellcheck.log`, exit 0, ShellCheck 0.11.0. This is host static analysis, not a Linux or native-Go claim. Bash wrapper/build helper are unchanged from accepted06.

Setup exclusions retained: initial image entrypoint and module-cache configuration errors (`independent-1/2`), missing predecessor mount in the first full script, and absent ShellCheck inside the image (`policy-conformance-final.exit=127`, after inherited migration had passed). ShellCheck was successfully executed separately on the host; inherited migration and runner qualification completed in the corrected script. No failed setup attempt is presented as successful qualification. Host Bats was not used.

## Integrity and boundaries

`acceptance-source-hashes.json` verifies **all 288 source/test/build file hashes** against `/private/tmp/gauge-v4-07-empty-set-review-freeze.json`, before and after review. It also records that all original06 Go tests are unchanged versus accepted commit aa155c8, and old-harness production exactly matches that commit. Original review regressions remain frozen; additional completion tests are outside the repository. `acceptance-evidence-hashes.json` pins the final scripts, logs, exits and independent tests.

Root separately reported native Go and protected repair-loop results. Those reports are acknowledged coordination context and are not substituted for the independently executed Linux/static evidence above. No external publication occurred. No power-loss durability or protected live-authorizer boundary qualification is claimed. The result is scoped local fixture acceptance of R01–R08 plus predecessor/ownership integration, suitable for the orchestrator's remaining lifecycle and explicitly blocked publication handling.

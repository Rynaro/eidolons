# V4-07 accepted-seam re-anchor

Static ATLAS mapping of the parent-designated FINAL FROZEN V4-06 source at `/private/tmp/eidolons-gauge-v4-06`. This report supersedes the API/guard anchors in `/private/tmp/gauge-v4-07-go-integration-scout.md`, which inspected the earlier commit `13d383b`. No source changes, tests, runtime probes, network/model calls or private logs. CRYSTALIUM unavailable. This is integration preparation, not an independent V4-06 acceptance verdict or a new architecture decision. Only this requested scratch report was written.

Selected contract authority: `/private/tmp/gauge-v4-07-binding-decision.md:9–35` (H): controller-local user/project layers; no root required for preferences; explicit schema 1→2 migration; typed immutable policy/binding/amendment records; controller-wide authorization consumption; production activation remains `authorizer_boundary_unqualified` (`:1`). All source paths below are relative to the absolute frozen-source prefix above. H means directly inspected static evidence; constraints drawn from the binding decision are explicitly identified.

Frozen source SHA256 identities:

- `gauge/internal/controller/authority.go`: `774f377b3c456f7e87c48ac19a2c86222ce2a94a3ccf3f86d7d078b797cdb436` (matches parent pin).
- `gauge/internal/controller/controller.go`: `8c2a7d37ff2a1278f4360b208169d3610d90ad2649d67a9a0eccea2c390aac75`.
- `gauge/internal/store/store.go`: `1258b0b2ec76ec9bcb9b8aaa7fe66683dfdff758db616656ed579366e72c8dd3`.
- `gauge/internal/contract/types.go`: `750b785a6b5e929b3448ad4b3a9e23d53e0cf0f8f3c679e4ad88ee23bece734b`.

## Shared DB, open and actual schema validation

1. **One controller-instance DB, same path after migration.** `Service.storePath() string` resolves `.eidolons/.ledger/.gauge-controller-v1/state.db`; `Service.claimPath(id string) string` resolves sibling `authority-<root>.json`. Root run marker is `.eidolons/.ledger/<root>/.writer-authority.json`. H: `gauge/internal/controller/authority.go:17–46,241–245`. The `v1` directory name, `layout.json.schema_version=1`, marker version 1 and DB `meta.schema="1"` are separate existing interfaces, not a single version switch. H: same file `:148–184,257`; `gauge/internal/store/store.go:54–55,105–108`. Selected schema2 migration must preserve existing paths/claims and controller identity; changing all these version strings merely because DB schema advances is unsupported by the binding decision (`:24–29`).

2. **Exact signatures:** `store.Create(path, id string) error` (`store.go:77`), `store.Inspect(path string, timeout time.Duration) (Snapshot, error)` (`:131`), `store.Open(path string, timeout time.Duration) (*Store, error)` (`:151`), `(*Store).Snapshot() (Snapshot,error)` (`:173`), `(*Store).Close() error` (`:172`). H. `Create` only creates absent files with O_EXCL; all eight required buckets and schema/store ID are committed together (`:75–108`). It creates no root. `Inspect` requires a regular non-symlink file and opens bbolt read-only; timeout must be >0 and ≤300 seconds (`:30–44,130–146`). `Open` calls full `Inspect`, opens writable under bbolt's lock, then reruns `guard` to close the schema race between lock acquisitions (`:149–170`). No migration API exists in this source.

3. **Exact schema1 layout:** top-level buckets `meta`, `roots`, `legacy`, `history`, `context`, `knowledge`, `policy`, `operations`; record families are history/context/knowledge/policy (`store.go:19–20`). `meta` holds schema and store_id. `roots` maps root ID to JSON; `legacy` maps root ID to a JSON array of original events. Families have per-root child buckets mapping record ID to JSON (`:184–222,259–274,353–375`). `operations` has per-root child buckets mapping operation ID to payload SHA256 bytes (`:380–399`). H. Preserve the legacy operations bucket independently of new typed authorization consumption; it is not included in public `Snapshot` (`:23–28,179–230`).

4. **Validation boundaries are narrower than a full predecessor audit.** `guard(*bolt.Tx) error` checks required buckets, exact schema1, nonempty store ID, root key/ID agreement and `Root.Validate` (`store.go:47–72`). `Root.Validate` checks only root-name syntax, staged/active phase and nonempty generation/inventory (`contract/types.go:84–88`); it does not validate Manifest/Binding. `snapshot` requires each root's legacy array to exist and parse, and validates recognized family records/key agreement (`store.go:179–230`). It does not recompute imported inventory digests, inspect filesystem claims, inspect operations or establish typed policy authority. `Stage` validates unique/nonempty imported event IDs, raw presence and self-attested grade before storing, but Snapshot does not repeat those checks (`:234–250`). H. Thus the selected migration's predecessor structural/inventory validation (`binding-decision.md:24–26`) cannot be claimed satisfied merely by calling `Store.Open`.

5. **Migration locking and refusal constraint.** The existing ordinary path is schema1-only; all `Stage`, `Activate` and `Update` transactions call `guard` (`store.go:259–262,298–302,320–324`). It has no required-feature manifest enforcement. H. The selected schema2 implementation therefore needs its explicit predecessor-aware migration entry separate from ordinary Inspect/Open, with schema/receipt/typed namespaces atomic in one DB transaction and new ordinary schema1 opens returning migration_required (`binding-decision.md:20–27`). The old binary will reject schema2 through the exact equality check. Keep regular-file checks, bounded bbolt lock acquisition and validation after acquiring the writable lock. No existing public controller-wide inventory-validation/migration-lock API is supplied: root ownership lock is per root, while bbolt locks the shared DB (`authority.go:194–238`; `store.go:40–44,151–170`). External claims/legacy files are outside the DB transaction; do not describe migration as atomically republishing them (`authority.go:41–44`).

## Non-root preference seam

- `(*Service).Init() error` already initializes the shared store without an execution root (`authority.go:161–184`); CLI `init` calls `InitRoot` only when --root is supplied (`gauge/cmd/eidolons-gauge/main.go:75–84`). `(*Service).inspect() (store.Snapshot,error)` validates real directories, typed layout and matching DB store ID without requiring a root (`authority.go:136–158`). These are usable controller scope/identity checks for the selected local layers. H.
- Existing `(*Store).Update(id,generation string,fn func(*Tx) error) error` requires a real active root (`store.go:320–333`). There is no preference read/CAS operation or non-root public update callback; no preference namespace in the fixed buckets. H: `store.go:19–28,320–333`. Therefore the binding decision's narrow typed layer reads, single-transaction dual-layer read, and validated replacement+revision CAS are additions, not calls currently available. Do not invent a root, activate an imported root, or reuse root generation as the preference revision. Decision: `binding-decision.md:10–16`.
- Existing CLI universally requires --root except init (`main.go:39–64`). This check must distinguish the selected rootless preference/migration commands; keeping it unchanged would contradict the selected contract. User/project are both local logical provenance layers; no current account/home identity API exists in these controller interfaces (`controller.go:42–67`; `main.go:25–27`). H; selected scope: decision `:10–16`.

## Repaired root ownership and inventory guards to preserve

- `(*Service).lock(id string,create bool) (func(),error)` acquires per-root `.append-lock` with bounded mkdir retry and an owner token; ambiguous ownership requires explicit quiescent recovery, never PID-based takeover (`authority.go:194–238`). H. Root mutations take this lock before authority checks and hold it until return.
- `(*Service).active(id string) (store.Snapshot,contract.Root,error)` now inspects the shared store, obtains root, loads the persistent claim, binds claim to DB store/root/generation/inventory, loads and binds the run marker, requires active marker plus active root, and calls `verifyInventory` (`authority.go:269–298`). Claim intentionally remains **pending** forever; `readClaim` refuses any other phase (`:244–249`). Do not “complete” the claim by changing it to active.
- `(*Service).verifyInventory(r contract.Root) error` reruns `legacy.Read(project,rootID)` and compares digest for imported roots; controller-created roots reject any legacy events (`controller.go:182–200`). `legacy.Read(project,run string) (Inventory,error)` verifies contiguous numbered regular event files, sequence, original-byte inventory hash and jq canonical event digests (`gauge/internal/legacy/journal.go:46–129`). Imported-root checks still require jq and preserve exact source bytes; policy canonicalization must not rewrite historical event digests. H.
- **Guard composition matters:** Store.Update checks schema, active root and generation only (`store.go:320–333`). It has no project path, claim or filesystem inventory checks. `ExecuteFixture` and `Replace` compose `lock → active → store.Open → Store.Update` (`controller.go:245–253,299–318,332–363`). The selected amendment path must retain the controller checks plus transaction-time root identity/generation checks; using `Store.Update` alone silently bypasses the repaired claim/inventory guard. H. Preference writes remain rootless and should not manufacture an execution-authority proof (decision `:13–14,33`).
- **Public root seams:** `InitRoot(id string) error` refuses occupied claim/marker paths and existing legacy data, stages then promotes (`controller.go:89–136`); `Import(id string) error` locks, verifies legacy inventory and immutable existing import, validates active repeats through `active`, and refuses unregistered/pending proofs (`:139–179`). `Promote(id string) error` and `Recover(id string) error` lock then call `promoteLocked(id,recovery)` (`authority.go:390–407`), whose claim/marker disagreement, missing-proof and drift checks precede activation (`:301–343`). H.
- Promotion publishes persistent pending claim → pending run marker → DB activation → active run marker; claim is never removed to re-enable legacy writes (`authority.go:41–46,344–385`). Existing active promotion retries and recovery recheck inventory (`:326–327,341–343`). DB schema migration must preserve these filesystem proofs, including exact StorePath. H.
- `Status(id string) (Status,error)` also calls `active` and checks append-lock absence before/after (`controller.go:212–232`). A new policy root-inspection route must not mislabel a bare store snapshot as currently owned execution state. H.

## Transaction primitives versus selected typed contracts

| Actual signature and anchor | Exact reuse/limitation (H) |
| --- | --- |
| `(*Store).Stage(root contract.Root,events []contract.LegacyEvent) error`, `store.go:234–275` | Atomic immutable import identity; not a preference or typed policy writer. |
| `(*Store).Activate(id,generation,inventory string) error`, `:298–312` | Checks generation/inventory and sets active; controller promotion supplies external proofs. Not a policy grant or authorization method. |
| `(*Tx).Root() contract.Root`, `:336–340` | Detached current-transaction copy; suitable basis for preserving unrelated root state. |
| `(*Tx).SetRoot(r contract.Root) error`, `:342–350` | Refuses ID/generation/inventory/phase changes. No policy CAS or typed binding validation; whole-root replacement can overwrite unrelated fields if caller supplies stale data. |
| `(*Tx).Put(r contract.Record) error`, `:353–375` | Immutable family/root record ID, identical replay allowed; different content conflicts. Raw JSON body equality uses reflect.DeepEqual, not semantic canonicalization. |
| `(*Tx).Once(id string,payload []byte) (bool,error)`, `:380–399` | Same-transaction digest replay/rollback, but scope is root+operation ID, result is fresh/not-fresh only. Not controller-wide authorizer+authorization ID consumption; cannot retrieve a past amendment result. |
| `contract.Digest(data []byte) string`, `contract/types.go:65` | SHA256 helper; canonical input construction is caller responsibility. |

`Root.PolicyRefs` and `Manifest.PolicyRefs` are string lists (`contract/types.go:38–47,71–82`), and `Record.Validate` accepts any valid JSON for policy/policy (`:91–103`). No typed policy snapshot/binding/amendment-result, closed strategy contract, preference revision or designated-authorizer interface is present in the inspected contract/Options (`controller.go:33–40`). H. The selected typed namespaces cannot treat old generic policy JSON, source labels, Binding.Authority or Root.Generation as grants: generation is assigned as writer ownership (`controller.go:76–86`; decision `:29–35`).

The existing transaction mechanism can hold dependent writes together, but narrow typed read/result/consume APIs and validation are absent. Existing `Snapshot` omits operation markers and there is no Tx getter (`store.go:23–28,179–230,315–400`). H. Preserve the selected database-wide authorizer+authorization ID key, root+canonical request digest binding, replay-before-current-predecessor ordering, durable original-result retrieval after later amendments, and all-or-nothing snapshot/consumption/result/binding change (`binding-decision.md:31–35`). Do not substitute `Once` or the fixture receipt pre-scan for that contract: fixture replay is tied to the currently computed manifest and precedes its write transaction (`controller.go:254–278,304–318`).

`decodeFile(path string,v any) error` rejects unknown fields/trailing objects but has no duplicate-key traversal (`authority.go:78–99`); it is not the full closed policy input validator. H. No production authorizer boundary is qualified by these store/ownership primitives; retain the selected unqualified activation state (decision `:1,33,53–55`).

## Compatibility evidence to carry forward (definitions inspected, not run)

- Schema/future/missing/corrupt refusal without file changes, plus per-mutation schema recheck: `gauge/internal/store/store_test.go:29–46` (H).
- Callback rollback, concurrent dependent family writes, bounded DB lock, identical/conflicting replay: same file `:49–70`; real process kill before/after commit and retry: `:73–90`; generic family persistence: `:93–98` (H).
- Marker loss blocks Go/Bash writes; restored marker cannot hide inventory drift; unrelated/staged roots retain legacy authority: `gauge/internal/controller/repair_test.go:19–43` (H).
- Imported roots cannot gain fixture reconstruction by labels; refusal preserves state and fixture positive controls remain: same file `:46–56` (H).
- Actual killed promoter/contender and explicit lock recovery cases: same file `:59–103`; added exact claim-published/marker-published windows and original-byte retention: `gauge/internal/controller/publication_cuts_test.go:19–67,74–99` (H; latter assertions continue beyond excerpt).
- New schema2 migration/layer/amendment tests remain required by decision `:49–55`; the definitions above do not establish those new contracts. Required boundary: real schema1 fixtures, old/new version refusal, missing receipt/namespaces, independent DB local layers, rootless CAS, exact replay after advancement, cross-root consumed-ID conflict, and preservation of every existing ownership/inventory guard.

→ Specification/implementation handoff: use these frozen signatures and preserve the selected contract; especially keep external controller authority checks separate from Store.Update's limited transaction guard. No new architecture decision is proposed. Remaining implementation detail to make explicit is how migration validates all predecessor inventory/claims under the existing root locks and shared DB lock without treating filesystem proofs as DB-transactional or repairing incomplete transfer state implicitly (anchors above; decision `:24–26,55`).

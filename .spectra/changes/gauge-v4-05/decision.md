# V4-05 completion representation and trust floor

FORGE actor: forge_v4_05. Two reasoning passes, reasoning from provided ATLAS execution evidence and pinned requirements; no independent execution. CRYSTALIUM unavailable. Decision preparatory until V4-04 acceptance.

Question: can completion be reproduced from a validated journal, current candidate snapshot, explicit criteria/environment/configuration, and verified evidence without trusting labels or a rendering clock?

Selected narrow hybrid: embed every acceptance-relevant observation and captured provenance in the canonical event; reference bulky evidence by digest. Full embedding is viable but enlarges history; all-reference objects are viable but add a second publication/retention boundary. A digest proves bytes, not independent execution.

Candidate identity covers tracked and required untracked source/tests/docs, modes, symlink targets, integration base, criteria, explicit environment/configuration. External symlink targets must be captured or make the candidate ineligible. No user-configurable exclusions hiding acceptance inputs. Keep feature-owned history/evidence outside the candidate tree; fixed operational exclusions explicit.

Latest applicable observation in sequence order wins per required check and full identity tuple. Empty set, missing/stale/cancelled/wrong-candidate/invalid evidence withholds acceptance. Rehash referenced bytes; absent objects cannot qualify. Capture volatile facts once; renderer derives them from canonical records. Expose artifact integrity, execution provenance and acceptance separately. Transport/process completion is termination only.

Production manual/imported observations remain self-attested. A supplied maker/checker label, independent/trusted field, environment switch or copied fixture receipt cannot upgrade trust. A pure evaluator test harness may supply authenticated producer context in memory; this must not be a CLI trust flag or reconstructed from serialized claims. Positive fixture output stays visibly fixture-only. Until a real qualified producer exists, production independent acceptance is unavailable; protected execution belongs V4-14.

Falsifiable gates: pass-fail-pass/cancel sequence; mutate every candidate constituent; missing/empty/stale/wrong-identity checks; changed/deleted evidence; forged labels/fields/switches; trusted fixture positive plus identical serialized production negative; deterministic re-render and edited-view drift; native/A2A/MCP termination with absent/failed acceptance checks.

Reverse if decision metadata cannot remain embedded, evidence verification cannot fail closed, or practical volume defeats hybrid. Object storage then needs publication/retention contracts. Revisit production trust only when actual independently authenticated execution exists.
